# 23 6.X 版本差异与适配

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。
- 2026-06-16 已建立 `6.6修正版` 资源布局 profile：本地基准作品为 `基底\新改曹操傳6.6修正版`，`Ekd5.exe=1130496` 字节；当前缺 6.6 HexTable 时显示 `CrossVersionFallback`。2026-07-04 后新口径为默认只读/预览，MCP 表写入必须显式 `write_mode=CrossVersionFallbackWrite`。
- 2026-06-16 追加：6.6 `Item.e5` 物品/宝物图标字段值 `N` 对应小图 `#2N+1` 和大图 `#2N+2`，宝物设定预览默认大图；6.1-6.5 DLL 图标逻辑不变。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认资源路径；已确认的 6.6 修正版路径必须走独立 profile。当前 6.6 缺少独立 HexTable 时只给出 `CrossVersionFallback` 风险提示，默认不允许 UI 直接保存；MCP 表写入必须显式承认 `CrossVersionFallbackWrite` 风险。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

记录时间：2026-06-05

本页只整理 6.X 版本信息；5.X 及更早资料不进入本次适配口径。外部资料用于确认版本边界和功能差异，具体读取/写入仍以对应版本 `HexTable.xml`、本地样本、旧工具和烟测为准。

#### 已复核来源

- 轩辕春秋文化论坛：`6.3和6.4系统的笔记（star原著）`
  https://xycq.org.cn/forum/thread-309777-1-1.html
- 轩辕春秋文化论坛：`发布6.5版`
  https://xycq.org.cn/forum/thread-309778-1-1.html
- 轩辕春秋文化论坛：`发布6.6修正版，看三楼`
  https://www.xycq.org.cn/forum/thread-310256-1-1.html
- 轩辕春秋文化论坛：`发一个6.5的p版扩展引擎`
  https://www.xycq.org.cn/forum/thread-309946-1-1.html
- 轩辕春秋文化论坛：`6.5pl版本不支持4083的指令单挑模式吗`
  https://www.xycq.org.cn/forum/viewthread.php?authoruid=548101&tid=310893
- 轩辕春秋文化论坛：`发一个EXE`
  https://www.xycq.org.cn/forum/thread-155401-1-1.html
- 本地离线批次：`90-来源归档/制作区帖子备份-抽取草稿/`，覆盖 star175 5.6-6.1、godtype 扩展引擎、旧 EXE 研究、剧本变量和资源美工离线页。该批次是外部佐证 / 待证 / 旧口径，不替代本页已有联网复核和本地样本证据。

#### 2026-07-06 制作区帖子备份版本线索

本批已登记来源：

| 来源组 | 版本范围 | 当前处理 |
|--------|----------|----------|
| `制作区帖子备份/star175/*.htm`、`star175/star6.1.docx` | star175 5.8、5.9、6.0、6.1 和旧 EXE 线索 | 进入 6.x 版本差异和整型变量矩阵；不覆盖当前 6.5 默认写回规则 |
| `制作区帖子备份/star175/*.doc`、`5.9版更新 bystar175.doc` | star175 5.6-6.0 文档 | 当前环境缺少 legacy `.doc` 转换器，只登记来源；补转换器后重跑 `extract_make_zone_posts.py` |
| `制作区帖子备份/godtype/*.htm`、`godtype的最终引擎.doc` | godtype 旧扩展引擎 | 作为旧引擎能力、地址和变量线索；必须与 6.5 未加密基底逐项复核 |
| `制作区帖子备份/用star175 5.6版exe做自己的曹操传MOD*.htm` | 5.6 制作流程 | 只作为术语、R/S 结构和旧制作流程参考 |

处理规则：star175/godtype 资料中的 `4080..4087`、商店扩展、特殊单挑、整型变量、章节扩展、头像/音效/资源迁移等内容，都必须标明版本来源。没有对应核心文件尺寸、HexTable、工具配置、脚本样本和实机验证前，不进入 6.5 默认实现路径。

#### 版本差异归纳

##### 6.3 / 6.4

- 6.4 已有大范围扩充：物品到 255、策略到 144、必杀/军师技扩充、战场人数到 250、特效号到 180，并涉及 Data、Imsg、Ekd5.exe 多处结构和偏移变化。
- 6.4 的战场扩充引入复制人/战场编号口径，资料中明确 4031 用于切换 Data 号与战场编号解释；46/47/4B 等出场相关指令也随扩充工具改变可输入范围。
- 6.4 已有 S 插图/自定义图像一类功能线索，但 6.6 发布贴说明 6.4 原发布贴因论坛回档缺失；因此对 6.4 的适配不能只靠转述，仍必须以 6.4 HexTable 和样本验证。
- 物品扩充资料提到 `4080` 曾作为旧剧本编辑器读取新道具的兼容变量；当前 6.5/6.6 适配不得沿用这种旧口径，应以对应版本表结构、旧工具配置和样本验证为准。

##### 6.5

- 6.5 在 6.4 基础上继续扩展异常状态、Data 出装、音乐内置、地形百分比、装备/兵种/人物特效、R 插图和剧本整型变量输入等能力。
- R 插图是 6.5 的明确新增能力：资源文件为 `Tr.e5`，通过 72 信息传送的 28 子功能控制；插图编号、坐标、上下层、透明度、图片序号和取消规则均与 `Tr.e5` 绑定。
- 6.5 支持在剧本可输入数字处填负数来引用整型变量，但资料也强调主要测试集中在坐标；因此命令模板只可作为只读候选，不应直接推导完整写回规则。
- 6.5 发布资料可复核到 `4017/4018` 集气、`4020/4021` 自由 R 鼠标行为、`4023` 负面状态恢复模式等脚本变量线索。它们是引擎能力边界，不是当前工具的可写规则。
- 6.5 对 `77/78/79` 变量命令有扩展线索：`77` 增加取模，`79` 增加比较运算；这些必须与本地 docx、旧 MFC 对话框 和样本命令逐槽比对。
- 6.5 的写入能力必须继续绑定本地已验证核心尺寸和偏移。本仓库当前拥有 6.5 HexTable、6.5 基底和多项烟测，因此只对 6.5 保持写入开放。

##### 6.5p / 6.5pl 分支

- 2026-06-08 扩大检索确认存在 `6.5p`/`6.5pl` 分支资料，外部页面命中 `6.5p`、`6.5pl`、`4083`、`4087` 等关键词。
- `发一个6.5的p版扩展引擎` 显示该分支涉及装备/道具扩展、特技书、策略书等能力，属于 Puluo/分支扩展线索，不是当前 `曹操传加强版6.5（未加密）` 的默认能力。
- `6.5pl版本不支持4083的指令单挑模式吗` 和旧 `发一个EXE` 变量资料能提供 `4083` 指令单挑模式、`4087` 练武场单挑模式等变量线索，但它们需要分清旧 EXE、6.5p、6.5pl 与当前 6.5 基底的差异。
- 在没有对应样本、HexTable、核心文件尺寸、脚本模板和实机验证前，6.5p/6.5pl 不能复用当前 6.5 写入护栏，也不能把变量说明直接加入默认命令模板。

##### 6.6

- 6.6 把 72 信息传送 12 从旧 S 插图改为动态图机制，RS 均可适用，资源文件改为 `DT.e5`；旧 `ts.e5` 写法被废弃。
- 6.6 新增 72 信息传送 32 作为动态图控制指令，可调整延时、停止播放、指定播放次数区间，并把停止时的当前显示次数保存到整型变量 4038。
- 6.6 新增 R 文字能力，使用 72 信息传送 30，最多同时显示 40 个；资料给出文字编号、文字/指针变量、颜色/字体/样式、坐标和取消规则。
- 6.6 对 6.5 的 R 插图写法做了微调：第三行第一个数字改成类似战前四按钮的二进制组合，影响鼠标手形和上下层绘图；6.5 剧本直接运行到 6.6 时，R 插图不会自动获得手形效果。
- 6.6 资料继续提到 `4031` 与战场编号解释相关，且 `72 信息传送1` 仍使用该变量控制。该线索说明脚本执行语义会跨命令共享状态，未来支持 6.6 时必须把变量开关纳入命令上下文解析，而不是逐条孤立解释。
- 6.6 图标和动态图资料出现资源边界变化：发布贴三楼说明 `Itemicon.dll` 改为 `E5\Item.e5`，`MgcIcon.dll` 改为 `E5\Mtem.e5`，舌战图标仍在 `CmdIcon.dll`；`Mapatr.dll` 仍供地形编辑器使用，运行时地形图迁到 `U_select.e5 #32`。
- 6.6 `Item.e5` 的发布贴导入口径为小图、大图成对排列；结合本地 `新改曹操傳6.6修正版` 索引可确认字段 `0 -> #1/#2` 为空白小/大图，字段 `1 -> #3/#4` 为第一件实际道具小/大图。工具侧宝物预览默认使用大图 `#2N+2`。
- 6.6 修正版发布贴还说明 `DT.e5` 用于动态图，`Fb.e5` 用于对话半身像，`Pmap.e5` 为场景地形相关资源；`U_select.e5` 的 #22、#25、#26-32 包含自定义 R 数字、buff 箭头、命令图标和地形图等槽位。
- 清儿吕布传本地样本证明 6.6 物品表不能只按 6.5 HexTable 行宽复用。该项目物品行仍为 25 字节，但正确图标字段在 `row+0x0F`，不是 6.5 旧布局的 `row+0x14`；辅助/道具段的 `row+0x12=2/3` 多为类别标记，实际特效候选常在 `row+0x11`。具体字段证据见 `07-数据表与资源/宝物物品设定.md`。
- 6.6 的这些变化说明资源边界和剧本命令语义已经变化。当前仓库尚无完整 6.6 `HexTable.xml`，但已有 `B形象指定器\6.6xEXE\Ekd5 6.6.exe`、`6.6x形象指定器\System.ini` 和本地 `新改曹操傳6.6修正版` 基准作品作为证据；取消旧写入保护后，跨版本 HexTable 只提示风险，不阻断写入。
- 2026-07-09 增量：`Star6.6X 引擎.cmf` 已建立独立人工 CM seed，不复用 6.5 seed。6.6 基底 `基底\新改曹操傳6.6修正版\Ekd5.exe` 长度 `1130496`，SHA256=`4A4FD8DDBF83E5F0B769D1B97BF8F6E6431C3AB42892024A354228212D3D06A4`；CMF 主证据 `老版游戏制作工具\Star6.6X 引擎.cmf` SHA256=`EBE2DD8A336EB83654114A913581684C6AA62908F03E63C1E38C58020C13F297`。
- 2026-07-09 增量：6.6 `全局设定` 当前接入 25 个 CM 字段：成长 3 项、装备经验 10 项、战斗公式 3 项、异常状态 9 项；本批未提供 6.6 地形策略地址，因此 6.6 不显示或不保存地形策略。宝物质变/飞跃等级使用 `0x1F50F/0x1F557`，用户列表中重复出现的 `0x1F53E/0x1F56D` 在 6.6 基底中记录为冲突候选，不进入可写 seed。
- 2026-07-09 增量：6.6 异常能力幅度仍按裸十六进制显示；异常持续回合在基底中仍为 2-bit 位段编码，UI 显示解码后的 `0..3`，不得把 `0xC0/0x10/0x08` 直接显示为十进制原始字节。
- 2026-07-09 增量：6.6 装备类型名称表和显示表继续使用 `Ekd5.exe@0x8AC70` 与 `Ekd5.exe@0x81827`，正式编辑入口为 `宝物设定 / 装备类型`。可装备部队写入必须以当前 6.6 项目的兵种成长/许可槽解析结果为准，不把 6.5 槽位规则无条件套用。

#### 6.6x 本地文件解析

- `Ekd5 6.6.exe`：大小 `1130496`，SHA256=`13152054F34BFA277C3170B0E596BE4A3CC483760D5F8471A7D9679BDBC8120A`。
- `基底\新改曹操傳6.6修正版\Ekd5.exe`：大小 `1130496`，当前工具识别为 `6.6`，作为本轮 6.6 修正版验收基准。
- 本地基准作品资源验证：`E5\Item.e5=512`、`E5\Mtem.e5=176`、`E5\DT.e5=60`、`E5\Fb.e5=673`、`E5\U_select.e5=32`、`E5\Pmap.e5=477`；`Cmdicon.dll` 仍可读 29 个 DLL 图标。
- `E5\Item.e5` 前四项验证：`#1` 为 16x16 空白小图，`#2` 为 32x32 空白大图，`#3` 为 16x16 第一件实际道具小图，`#4` 为 32x32 第一件实际道具大图。因此字段值不是 `字段+1` 单图映射。
- `Itemicon.dll`、`Mgcicon.dll`、旧 `ts.e5` 在 6.6 修正版下标记为 obsolete，不进入缺失摘要；`Smlmap.e5/Spalet.e5` 只作为旧版可选资源显示。
- `形象指定器66x.exe`：大小 `1519616`，Version=`1.00.0066`，Product=`形象指定器6.6修正版`，SHA256=`BA12B7A0CF3BB2805B0DCB7F27BEE51669D3DC5AABCE965642A4C0C86A12C55A`。
- 6.6x `System.ini` 的 `FileHead=D2800`、`RFileHead=E1000` 与 6.5 相同；直接读 `Ekd5 6.6.exe` 这两段可得到 UInt16 风格 R/S 指定表数据。因此 R/S 指定表读取可以沿用 6.5 内置表作为临时只读兜底。
- 6.6x `System.ini` 的 `SCount=368`，6.5 为 `96`；6.6x 还新增 `MarkCount=32` 与 `MgID/MgAIYN1/MgAIYN2/MgHit/MgHurt/MgHurtYN/MgMeff/MgMcall/Mg8/MgMF` 等策略扩展地址。这些只作为 6.6 读取研究线索，不开放写入。
- 2026-07-04 增量：MCP 已接入 `B形象指定器` 官方 oracle。6.6x `System.ini` 中 `MgID/MgAIYN1/MgAIYN2/MgHit/MgHurt/MgHurtYN/MgMeff/MgMcall/Mg8/MgMF` 会通过 `read_image_assigner_oracle_config` 和 `compare_image_assigner_oracle` 返回为 `StrategyExtensionReadOnlyCandidates`，仅用于后续 6.6 原生表和策略扩展 writer 的验证线索，不进入 6.5 默认写入规则。

#### 适配策略

- 当前临时兜底：读取非 6.5 项目时优先使用项目提供的对应版本 `HexTable.xml`；若 6.6 缺表，允许回退工具内置 6.5/6.3 HexTable，并显示 `CrossVersionFallback`。该兜底必须提示偏移风险，默认只读/预览；MCP 需要 `write_mode=CrossVersionFallbackWrite` 才能继续。
- 读取侧兼容 6.X：表名解析不再固定 `6.5-` 前缀，而是按 6.X 版本号后的语义后缀匹配，例如 `6.5-0 人物` 可解析到 `6.6-0 人物`。
- 写入侧当前护栏：6.6 原生表可在结构校验通过后保存；跨版本 HexTable 回退状态下 UI 默认禁用保存，MCP 必须显式 `CrossVersionFallbackWrite`，并保留写前备份、结构化报告、复读校验和风险提示。
- 版本检测扩展到 `CczRSX 6.*\\ConfigTable\\HexTable.xml`，并优先按实际 `Ekd5.exe` 尺寸、再按项目路径中的 6.X 版本提示选择匹配表；若项目表缺失，当前仍允许内置 6.5 HexTable 兜底。
- 跨表引用、剧本参数候选、物品表、装备特效名称表和派生索引表都按同一语义解析器读取，避免 界面、MCP、诊断之间出现一个能读一个不能读的分裂状态。
- 2026-06-13 起新增 `CczEngineProfileService` / `CczEngineProfile`：按 `Ekd5.exe` 尺寸、版本资源和路径提示识别 6.3/6.4/6.5/6.6，输出 `TableVersionPrefix` 与 `TableHints`。`6.4` 因当前工具包没有独立离线表，表读取暂用 `6.5` 语义兜底；`6.6` 优先尝试 `6.6` 表名，缺表时再走语义兜底。
- `TableHints` 明确区分 `PersonTable`、`ItemLowTable`、`ItemHighTable`、`JobSeriesTable`、`DetailedJobTable`、装备特效名称表、兵种特效说明/分配表、个人/套装专属表、战役名称表和商店数据表。旧字段 `JobTable` 保留为兵种系表兼容字段，不再用来读取 80 行详细兵种名。
- `HexTableNameResolver.ResolveForProject/TryResolveForProject` 是所有项目感知读表入口；`ResolveItemTables(project, tables)` 先按当前引擎的低/高物品表解析，再回退到 6.X 语义匹配。

#### 本轮落地

- 新增 `HexTableNameResolver`：识别任意 `6.*` 表名前缀，支持精确表名、6.X 语义后缀、去范围后缀和包含匹配兜底。
- `HexTableParser`、`HexTableReader`、主界面表查找、MCP 表读取、人物 R/S 指定、物品/装备特效名称、剧本引用候选和表格跨表诊断均改为通过解析器定位表。
- 6.X 表定位改为使用解析器和项目版本护栏；非 6.5 核心尺寸作为写入风险警告，缺核心文件或无 6.X 表仍为错误。
- `ProjectVersionGuardService` 已降级为兼容报告入口，不再按 6.5 表版本或核心文件尺寸阻断保存；实际安全边界转为备份、报告、复读校验和用户确认。
- `ProjectDetector` 能搜索 `CczRSX 6.6`、`CczRSX 6.5`、`CczRSX 6.4` 及其它 `CczRSX 6.*` 的 `HexTable.xml`。
- `ProjectDetector` 会优先按 `Ekd5.exe` 尺寸选择 B形象指定器，再使用项目路径版本提示兜底：6.6/6.6x 或 `1130496` 字节优先 `6.6x形象指定器`，6.5 或 `1196032` 字节优先 `形象指定器6.5`。
- 2026-06-13 增量：`LegacyMfcDialogDataSources`、`ShopEditorService`、`EffectPackageService`、`ItemEffectNameReader`、`GlobalSettingsService`、`ExclusiveSetScenarioService`、`BattlefieldEditorService`、`BattlefieldUnitStatusWriteService`、`ImageAssignmentService`、`HexTableReader` 派生索引表、剧本参数提示、结构探针、跨表引用导航和 MCP 表工具均改为项目感知表解析。
- MCP `detect_project` / `list_tables` 会返回当前 `Engine` profile；`read_table` 与 `write_table_rows` 通过 `FindTable(project, tables, tableName)` 解析表名，避免无头工具仍固定到 `6.5-` 字面表名。
- 2026-06-16 增量：新增 `Ccz66RevisedLayout`，6.6 修正版必查资源改为 `E5\Item.e5`、`E5\Mtem.e5`、`E5\DT.e5`、`E5\Fb.e5`、`E5\U_select.e5`、`E5\Pmap.e5`；`Itemicon.dll/Mgcicon.dll/ts.e5` 不再计入缺失。
- 2026-06-16 增量：道具图标预览/AI 图标预设在 6.6 下默认走 `E5\Item.e5`，策略图标走 `E5\Mtem.e5`；6.1-6.5 仍走 `Itemicon.dll/Mgcicon.dll` 的 RT_BITMAP 路径。
- 2026-06-16 增量：修正 6.6 宝物图标公式。`Item.e5` 字段值 `N -> 小图 #2N+1 / 大图 #2N+2`，宝物设定预览默认显示大图；AI `dll_icon` 字段 `N` 会生成小/大两份目标文件。`Mtem.e5` 暂保持 `字段+1`，除非后续实机或发布资料证明它也成对。
- 2026-06-16 增量：补齐 6.6 相关脚本模板 `72-2/3/5/12/14/28/30/31/32/34/35/36` 和 `7A`，其中 `72-5` 按 4 人物位、2 兵种位、独立特效值提示，`72-2/3` 增加阵营参数提示。
- 2026-07-06 增量：`基底\清儿吕布传` 已按真实 6.6 引擎纳入诊断和烟测。该项目 `Ekd5.exe` 大小为 `1413120`，版本资源低字为 `6`，SHA256=`88EC708A24718868090F70256D09086E83C92FAB0613C7B8D3742EF974D3564D`；版本识别必须采用旧扳手低字映射 `1..6 -> 6.1..6.6`，不得再按 EXE 大小或未知低字兜底成 `6.1`。
- 2026-07-06 增量：新增 `Qinger66DiagnosticsService`，MCP `detect_project` 在清儿 6.6 项目中返回 `engine.tableStatusSummary` 与 `engine.qinger66Diagnostics`。诊断覆盖人物、人物列传、暴击/撤退台词、R/S 形象、物品低/高段、物品说明、兵种系、详细兵种、兵种成长、兵种特效、个人/套装专属、战役名称、商店数据，并输出 `Native66/CrossVersionFallback/ReadOnlyEvidenceOnly`。
- 2026-07-06 增量：`Ccz66HexTableAugmentationService` 对 6.6 项目生成显式 `6.6-*` 兼容表。只有通过目标文件存在、文件长度覆盖、行宽/字段字节和不越界检查的表标记为 `Native66` 并默认可写；未通过证据表标记为 `ReadOnlyEvidenceOnly`；直接使用原始非 6.6 表仍为 `CrossVersionFallback`，MCP 写入必须显式 `CrossVersionFallbackWrite`。
- 2026-07-12 增量：角色台词不再适用“只要 6.5 表偏移在 6.6 文件内就克隆为 Native66”的通用规则。两份本地 6.6 样本确认暴击表为 `Imsg.e5 @ 140000`、撤退表为 `Imsg.e5 @ 491600`；旧 `440200` 在 6.6 仅作冲突诊断，不允许写入。`Ekd5.exe @ 0x89C30` 的 21 槽特殊暴击人物表仍由执行代码引用，6.6 修改前必须验证 PE 可执行段对对应 VA 的引用，否则降为 `ReadOnlyEvidence`。
- 2026-07-06 增量：清儿 6.6 资源读取以 `E5\Item.e5`、`E5\Mtem.e5`、`E5\DT.e5`、`E5\Fb.e5`、`E5\U_select.e5`、`E5\Pmap.e5` 为必查资源；`Itemicon.dll/Mgcicon.dll/ts.e5` 在 6.6 项目中保持 obsolete 口径。`Item.e5` 条目用途文案统一为 `field value N`，其中字段 `0 -> #1/#2`。
- 2026-07-06 增量：清儿 6.6 物品字段完成针对性定位。低段基址 `Data.e5 @ 0x818C`，高段基址 `Star.e5 @ 0`，行长 `0x19`；正确图标字段为 `row+0x0F`，例如 `ID=0/1/2` 分别为 `1/2/3`，`ID=104` 为 `105`。旧 6.5 图标位置 `row+0x14` 会导致图标号跳脱和预览错配，不得在清儿 6.6 下继续作为 `图标`。
- 2026-07-06 增量：清儿 6.6 宝物/辅助特效读取需区分“类别标记”和“实际效果候选”。辅助装备和道具段中 `row+0x12=2/3` 不应直接解释成普通特效；当该字节为类别标记时，优先使用 `row+0x11` 作为实际效果候选，例如 `铜盾 row+0x11=0x3D`、`风神盾=0x3F`、`遁甲天书=0x53`、`玉玺=0x5C`。
- 2026-07-06 增量：清儿 6.6 `Ekd5.exe` 存在名称槽池 `0x9E800 + id * 16`，16 字节 GBK 定宽。形象指定器 6.6 清单仍作为高可信绑定表；若清单未覆盖但名称槽有效，可作为 `6.6 EXE 名称槽候选`，避免误报“未定义特效”。只有绑定表与名称槽都无有效文本时，才显示未定义/未验证。

#### 待验证项

- 仍需要真实 6.6 `HexTable.xml`，验证人物、物品、兵种、策略、商店、R/S 指定和剧本引用候选是否完全匹配；当前 6.6 表写入不阻断，但属于跨版本 HexTable 风险场景。
- 需要比较 6.5 与 6.6 核心文件尺寸、表数量、表名后缀、字段名和偏移差异，再决定是否为 6.6 建立独立 HexTable，而不是恢复写入阻断。
- 需要为 6.5p/6.5pl 建立独立版本指纹：核心文件尺寸、HexTable 来源、装备/道具扩展表、特技书/策略书资源、4083/4087 变量与脚本命令支持状态。
- 6.6 的 `DT.e5` 动态图、R 文字和 R 插图微调已进入资源识别与命令模板提示；完整脚本结构写回和实机语义仍需后续验证。
- 6.x 整型变量 `4017/4018/4020/4021/4023/4031/4038/4080` 需要按版本建立“外部口径、脚本命令样本、实机状态变化、是否可写”的矩阵。
