﻿# 06 验证与烟测

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认写入路径；如需适配，必须另建版本护栏和样本验证。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

本页记录当前项目的验证入口，避免旧的 `.E5S` 假设继续阻塞 R/S eex 主线。

#### 当前主线验证

| 命令 | 用途 | 是否写入 | 说明 |
| --- | --- | --- | --- |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll` | 默认核心烟测 | 否 | 默认只跑 R/S eex 主线，只读验证剧本索引、人物 R/S 表、头像预览和 R/S E5 索引表预览，不再进入旧 SV004.E5S 全量断言 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-smoke` | R/S eex 只读烟测 | 否 | 明确验证 `RS\R_*.eex/S_*.eex` 索引不混入 E5S，验证 B形象指定器偏移、头像预览和 E5 `110` 索引表预览 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-scenario-depth-smoke` | 旧版完整剧本深度烟测 | 否 | 遍历全部 R/S eex，使用迭代方式统计旧版完整树命令数和最大子事件嵌套深度，防止递归枚举再次引入 StackOverflow |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --write` | R/S eex 核心写入烟测 | 只写测试副本 | 当前等价于 R/S 核心写入验证：复制测试副本，写 `RS\R_00.eex` 文本和 `Ekd5.exe` 人物 R/S 指定表，备份并复读校验 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-write-smoke` | R/S eex 核心写入烟测 | 只写测试副本 | 与 `--write` 主线相同，名称更明确；若 `S_00.eex` 战场标题容量为 0，输出 `BATTLEFIELD_TITLE_WRITE_SMOKE_SKIPPED` 后继续验证胜败条件、部署、地图和 Hexzmap |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-text-write-smoke` | 战场标题/胜败条件写回烟测 | 只写测试副本 | 单独验证 S 剧本文本写回；标题容量不足只跳过标题子项，不阻断胜败条件结构保存和复读 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-deployment-write-smoke` | 战场部署写回烟测 | 只写测试副本 | 单独验证 `46/47/4B` 部署、空槽自动绑定、AI/坐标和单位状态写回 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --standalone-semantic-smoke` | 独立单关语义命令烟测 | 否 | 验证 typed event 编译为真实命令草稿，禁止 `turn_start/area_entered` 继续用文本标签冒充触发 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --standalone-golden-samples-smoke` | 独立单关黄金样例烟测 | 否 | 固定三英战、救援战、Boss 决战，验证角色识别、R/S 命令图、资源阻断和报告等级 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --strict-playable-preview-smoke` | 严格可玩预览烟测 | 否 | 验证 `strict_playable_preview` 会因缺资源、结构草稿和 runtime gate 阻止 playable/runtime 等级 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --map-terrain-consistency-smoke` | 地图/Hexzmap 一致性烟测 | 否 | 校验 Map JPG、Hexzmap 索引、格数、decoded length 和可编辑块一致 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --e5-image-replace-smoke` | E5/动态链接库 图片资源目录与条目替换烟测 | 只写测试副本 | 先复制常见图片 E5 和图标 动态链接库 到测试副本，校验 `Face/Pmapobj/Unit_*/Hitarea/Effarea/Logo/Mmap/U_select/Gate/Weather` 等 E5 资源目录与 `Itemicon/Mgcicon/Cmdicon` 动态链接库图标编号预览，再生成小 PNG 替换 `Unit_mov.e5` 单个 `110` 索引图号，复读条目字节并验证备份/JSON 报告 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-mfc-dialog-smoke` | 旧版 MFC 对话框 复刻烟测 | 否 | 从旧 `OnEditModify()` 解析 101 个命令号到 71 个对话框 的分派，校验 C# 分派表、旧 `.rc` 资源、71 个显式行为目录、`Dialog_114` 34 个模板，并逐个实例化旧式对话框 执行初始化/提交 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --battlefield-unit-status-write-smoke` | 战场单位状态写回烟测 | 只写测试副本 | 复制 `S_00.eex` 到测试副本，直接从旧版脚本树构造 `46/47` TargetKey，验证等级/兵种级/AI、`48` 装备、`52` 兵种改变和 5 条 `38` 五维写回；断言 `48` 在部署段，`52/38` 在同 Section 的 `1C 绘图` 下方，并校验 `4081=1/0` 包裹 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --role-default-equipment-smoke` | 角色默认装备写回烟测 | 只写测试副本 | 验证 `6.5-0 人物` 尾三字节字段 `武器/防具/辅助` 无未命名 padding；抽样人物 ID `162` 读取 `46/90/143`；角色设定聚合表派生 `武器名/防具名/辅助名`；测试副本写入 `0/70/109` 后直接校验 `Data.e5 @ 0x18C + id*0x20 + 0x1D..0x1F`，再恢复原值并复读 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --effect-package-smoke` | 特效包读写烟测 | 只写测试副本 | 验证 EffectPackage 的宝物特效、兵种特效、个人/套装专属和补丁草案入口；覆盖宝物绑定自动发现删除、兵种分配固定清空、个人/套装专属只改指定槽位和删除清空 14 列 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --shop-smoke` | 商店/物品读取烟测 | 否 | 验证商店数据、人物名、物品低/高段、装备/道具下拉、物品详情和 CSV/批量编辑路径；用于确认引擎感知物品表解析没有回退 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --exclusive-set-smoke` | 剧本专属/套装文本烟测 | 否 | 扫描 R/S eex 的 72 信息传送子功能 10，验证专属/套装文本解析、人物/物品/特效名映射和格式容错 |

#### 2026-06-13 引擎识别与特效包验证

- `dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj --no-restore -p:OutputPath=bin\CodexVerify\CCZModStudio\`：通过，0 警告，0 错误。
- `dotnet build 工具整合包\CCZModStudio.McpServer\CCZModStudio.McpServer.csproj --no-restore -p:OutputPath=bin\CodexVerify\McpServer\`：通过，0 警告，0 错误。
- `dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj --no-restore -p:OutputPath=bin\CodexVerify\SmokeTests\`：通过，0 警告，0 错误。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --effect-package-smoke`：通过，输出 `EFFECT_PACKAGE_SMOKE_OK ... templates=4`。该项确认宝物特效绑定/删除、兵种特效替换/删除、个人/套装专属指定槽位写入/删除和补丁草案安全门。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --legacy-mfc-dialog-smoke`：通过，输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 ...`，用于确认旧扳手/旧对话框候选数据源改为引擎感知读表后没有破坏旧式命令编辑。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --shop-smoke`：通过，输出 `SHOP_EDITOR_SMOKE OK`，确认商店、人物名和物品低/高段映射正常。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --exclusive-set-smoke`：通过，输出 `EXCLUSIVE_SET_ENTRIES=68`、`EXCLUSIVE_SET_MALFORMED=0`、`EXCLUSIVE_SET_SMOKE=OK`，确认剧本专属/套装文本解析和名称映射正常。
- 2026-06-13 追加复核：`ImageAssignmentService` 和 `HexTableReader` 派生索引表也改为项目感知解析后，主程序、MCP、SmokeTests 顺序构建均通过。`--rs-smoke` 当前会在既有战场候选断言 `Battlefield editor did not include the first 46 friend deployment record.` 提前停止，未作为 R/S 形象读取验收依据。
- 注意：本轮不要用 `dotnet run --no-build -p:OutputPath=...` 作为最终验收命令；实测会运行默认 Debug 旧产物，导致 EffectPackage 个人专属旧逻辑仍显示 14 个预览变更。应直接运行独立输出目录里的 `CCZModStudio.SmokeTests.dll`。

#### 2026-06-13 战场单位状态写回验证

- `dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj --no-restore -p:OutputPath=bin\CodexVerify\SmokeTests\`：通过，0 警告，0 错误。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --battlefield-unit-status-write-smoke`：通过，输出 `BATTLEFIELD_UNIT_STATUS_WRITE_SMOKE_OK file=S_00.eex target=Scene=1;Section=1;Command=8;Offset=000091;Id=47;Record=0 person=146 ... inserted=4 updated=3`。该项确认 `48` 写在出场部署段后，`52/38` 写在 `1C 绘图` 下方，且 `52` 有 `4081=1/0` 能力重算包裹。
- `dotnet 工具整合包\CCZModStudio.SmokeTests\bin\CodexVerify\SmokeTests\CCZModStudio.SmokeTests.dll --legacy-mfc-dialog-smoke`：通过，输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 sortedCombos=Dialog_33.IDC_COMBO1`。该项用于复核 `52` 仍为旧 `Dialog_82` 的人物/兵种两参数，不加入第三个刷新参数；`38` 仍按旧能力枚举和操作/数值槽写入。
- `dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj --no-restore -p:OutputPath=bin\CodexVerify\CCZModStudio\`：通过，0 警告，0 错误。
- 当前 `--rs-write-smoke` 在完整主线前置阶段仍可能被 `Battlefield editor did not include the first 46 friend deployment record.` 拦住；因此新增窄入口 `--battlefield-unit-status-write-smoke` 专门验证本次状态写回链路，避免被无关预览候选前置条件遮蔽。

#### 2026-06-06 旧版 MFC对话框修改链路验证

- `dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj`：通过，0 警告，0 错误。
- `dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj`：通过，0 警告，0 错误。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --legacy-mfc-dialog-smoke`：通过，输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 sortedCombos=Dialog_33.IDC_COMBO1`。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --legacy-script-edit-smoke`：通过，输出 `LEGACY_SCRIPT_EDIT_SMOKE_OK file=R_00.eex ... command=09/延时 param=12345 count=8528->8529->8528`。
- 2026-06-06 界面/入口统一增量：
  - 剧本制作、战场制作 S 剧本、R 场景制作 R 剧本均使用旧式可视树：`文件路径 -> Scene -> Section -> 真实指令节点`。
  - 静态检查重点：旧版完整树路径不再创建 `测试条件`、`正文根`、`事件正文`、`文本参数`、`子事件主体/载体` 包装节点；这些字符串只允许出现在兼容探针、历史说明或内部详情。
  - 三处双击命令均进入 `LegacyCommandEditDispatcher`，保存按钮分别为 `完整保存剧本`、`完整保存S剧本`、`完整保存R剧本`，均走 `LegacyScenarioWriter.Save`。
  - 剧本制作页右侧界面无对象预览、无详情大块、无批量修改按钮；参数区为内嵌旧式对话框，底部为项目侧 `备注`。
- 注意：不要并行执行 `dotnet build` 和 `dotnet run` 指向同一 SmokeTests 输出目录；Windows 会因 动态链接库 被占用出现 `CS2012/MSB3027`，应先构建再 `--no-build` 运行，或使用独立 `OutDir`。

#### 2026-06-06 R 场景 30 坐标写回验证

- `dotnet build 工具整合包\CCZModStudio.sln`：通过，0 警告，0 错误。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke`：通过，输出 `RS_SMOKE OK`。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-write-smoke`：通过，新增输出 `RSCENE_POSITION_WRITE_SMOKE_OK file=R_00.eex command=Scene=2;Section=1;Command=14;Id=30 coord=(40,5)->(39,4) ... changedBytes=2`，确认测试副本中 `30 武将出现` 的 X/Y 32 位槽可完整保存、备份并复读。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --legacy-mfc-dialog-smoke`：通过，输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 sortedCombos=Dialog_33.IDC_COMBO1`。
- Hexzmap 地形同步已拆成专用烟测：`--hexzmap-sync-smoke` 验证当前 `Ls12 + 0x110` 索引表、地图槽绑定和可编辑块；`--hexzmap-write-smoke` 在测试副本中写入 M000 一个地形格并复读校验。

#### 2026-06-03 E5 索引表形象预览验证

- 2026-06-04 图片处理增量：`dotnet build .\CCZModStudio.sln` 通过，0 警告，0 错误。
- 2026-06-04 图片处理增量：`dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `IMAGE_RESOURCE_CATALOG files=18 face=293 markIndex=False hit=33 eff=12 itemIcons=170 mgcIcons=49 cmdIcons=41` 和 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`，验证图片处理页资源目录、`Mark.e5` 只定位边界、`Face.e5 #1` 预览渲染、动态链接库图标编号预览与测试副本单条目替换。
- `dotnet build .\CCZModStudio.sln`：通过，0 警告，0 错误。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke`：通过，输出 `E5_IMAGE_REPLACE_PREVIEW file=Unit_mov.e5 entries=556 image=554 ...`、`RS_IMAGE_PREVIEW ... mapped=554 ... colorPixels=...` 与 `RS_SMOKE OK`。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`，验证测试副本单条目写入、备份和结构化报告。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj`：通过，默认核心烟测同样验证 `S=0 职业1 -> Unit#4/#5/#6`、`S=1 -> #241/#242/#243`、`S=32 -> #334/#335/#336`、`S=33 -> #337`、`S=250 -> #554`、`S=253 -> #557 越界`。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --hexzmap-sync-smoke`：通过时应输出 `HEXZMAP_SYNC_SMOKE_OK entries=59 blocks=58 editable=58`，用于确认 `M100.jpg` 等额外地图不会被绑定到 Hexzmap 地形块。
- 视觉确认：生成的 `Smoke_RS_S250_UnitPreview_*.png` 仅包含三格彩色代表帧，图片内不叠加动作名/图号文字，按新映射读取 Unit 图 554；`Smoke_RS_R00_FacePreview_*.png` 中 R0 可显示 `Pmapobj.e5 #1`，JPG 洋红透明底已按阈值剔除。

#### 2026-06-02 剧本页旧版界面同步验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，`LEGACY_SCENARIO_READ file=R_00.eex scenes=5 sections=7 commands=8528 jumps=462 texts=712`。
- `--rs-smoke`：通过，`BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=133 located=友军出场设定@000091 compare=S_01.eex`，用于防止 S 关卡切换后战场候选不刷新、TargetKey 无法回查旧版命令树。
- `--rs-write-smoke`：通过，`LEGACY_SCENARIO_WRITE_OK file=R_00.eex ... changedBytes=0`。
- `--legacy-scenario-depth-smoke`：通过，`files=119/119 failed=0 commands=107462 maxDepth=13 maxFile=R_00.eex uiFoldDepth=64`。
- 静态自检：剧本页不再订阅 `_scriptCommandGrid.SelectionChanged`、`_scriptTextGrid.SelectionChanged`、`_scriptSearchResultGrid.SelectionChanged`；用户导航入口收敛为 `_scriptTree.AfterSelect`。

#### 2026-06-05 战场 S 读取同步与真实写回验证

- `dotnet build .\工具整合包\CCZModStudio.sln`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，`BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=329 deployment=友军出场/我军出场/敌军出场 located=友军出场设定@000091 compare=S_01.eex map=S_00.eex:M000:M000.JPG:True:True:3DA`。
- `--rs-smoke` 现在同时校验三类签名：不同 S 剧本的战场命令候选不同、出场候选不同、地图预览签名不同。该项用于防止切换 `S_XX.eex` 后左侧 S 树、右侧候选和地图预览停留在上一关。
- `--rs-smoke` 同时校验我军候选出战位叠加：输出 `allySlots=S_01.eex:7 first=#1@(19,9) forced=2`，确认 `S_01.eex` 可从 `R 4057 + 4B` / `S 4B` 中解析 7 个候选位，首位顺序与坐标正确，并能从 `06/4A` 强制列表绑定 2 个强制出战角色。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_TEXT_WRITE_SMOKE_OK file=S_00.eex ...`，确认战场标题/胜败条件在 S 剧本测试副本中走旧版完整结构保存、备份、报告和复读。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_DEPLOYMENT_WRITE_SMOKE_OK file=S_00.eex target=...Id=46;Record=0 ...`，确认已有 `46/47` 出场记录可真实写回原 S 剧本测试副本并复读。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_DEPLOYMENT_EMPTY_SLOT_WRITE_OK file=S_00.eex target=...Id=46;Record=19 ...`，烟测会在测试副本中临时制造空 `46/47` 槽，验证纯拖放式自动绑定空槽后可写人物、坐标和 人工智能，并在复读后成为可见候选。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_DEPLOYMENT_ALLY_SLOT_WRITE_OK file=S_00.eex target=...Id=4B;Record=0 ...`，确认 `4B` 我军出战位可写回候选 X/Y、方向、隐藏标志并复读；不写人物号、不改第一个出战顺序槽。
- 当前样本 `Hexzmap.e5` 为 `44840` 字节，已确认其索引表与地图格数同步；地形写入不再依赖 `45254` 文件大小基准，而是要求 decoded length = 地图格数 + 2 字节前缀、测试副本备份、写后复读。

#### 2026-06-05 战场拖放左右指令预览同步验证

- `dotnet build 工具整合包\CCZModStudio.sln`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，仍输出 `BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=329 deployment=友军出场/我军出场/敌军出场 ... compare=S_01.eex map=S_00.eex:M000:M000.JPG:True:True:3DA`，确认读取/切换 S 剧本的左侧 S 树、右侧候选和地图预览签名未回退。
- `--rs-write-smoke`：通过，仍输出 `BATTLEFIELD_TEXT_WRITE_SMOKE_OK` 与 `BATTLEFIELD_DEPLOYMENT_WRITE_SMOKE_OK ... Id=46;Record=0 ... coord=(14,1)->(0,1)`，确认界面预览同步改动没有破坏测试副本真实写回、备份和复读闭环。
- 交互边界：拖放绑定已有 `46/47/4B` 脚本记录时，右侧出场候选、右侧命令表、左侧 S 树节点显示“地图预览已调整”；纯拖放会自动匹配同阵营同人物、空 `46/47` 槽或可用 `4B` 位，只有找不到可用槽时才只保存项目侧 JSON。

#### 2026-06-02 宝物页旧版界面同步验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，保留 `ITEM_TYPE_CATALOG`、`ITEM_ICON_PREVIEW`、`ITEM_ACCESSORY_EFFECT_MODEL`。
- `--rs-write-smoke`：通过，`ITEM_WRITE_SMOKE_OK low=0:短剑->烟测宝物 price=5->1 high=104:虎豹嘶风铠->烟测扩展 saves=4`，确认界面派生列收敛不影响 Data.e5、Star.e5、Imsg.e5 写回。
- 静态自检：宝物页表头顺序为 `ID`、`名称`、`图标`、`类别`、`初始能力`、`能力成长`、`价格（/100）`、`特效号`、`特效名`、`特效值`、`介绍`；隐藏分段/大类/图鉴/类型说明/价格显示/实际效果/特效提示/来源文件；`类型` 显示为 `类别` 下拉但底层仍绑定原始 `类型` 字段；`装备特效名` 查中文特效名称。

#### 2026-06-03 宝物特效目录验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过后，使用同一输出目录运行 `CCZModStudio.SmokeTests.dll --rs-write-smoke`。
- `--rs-write-smoke`：应额外输出 `ITEM_EFFECT_CATALOG_OK`，验证项目侧 UTF-8 宝物特效目录可读写、长中文可保真、重复特效号可合并显示。
- 静态自检：
  - `宝物设定 -> 宝物特效` 保存到 `CCZModStudio_Notes\{项目名}_ItemEffectCatalog.json`。
  - `装备特效名`、字段说明面板、跨表引用解释中的装备特效规则，均优先使用项目侧目录，未命中时再回退基础特效名称表。
  - `0` 解释为 `无`，`255` 解释为 `普通装备/无扩展特效`，不被项目侧目录覆盖。

#### 2026-06-03 MCP 接入验证

- 2026-06-05 配置包验证：新增 `工具整合包\MCP配置`，运行 `powershell -NoProfile -ExecutionPolicy Bypass -File ".\工具整合包\MCP配置\generate-mcp-config.ps1" -Build` 通过，生成 Codex TOML、通用 `mcpServers` JSON、Claude Desktop JSON 和 direct-dotnet 备用配置到 `MCP配置\_generated`；该目录已加入 `.gitignore`。
- 2026-06-05 stdio 启动入口验证：`powershell -NoProfile -ExecutionPolicy Bypass -File ".\工具整合包\MCP配置\validate-mcp-config.ps1"` 通过；脚本经 `start-ccz-mcp.ps1` 启动 MCP Server，发送 `initialize`、`notifications/initialized`、`tools/list`，返回 `MCP_VALIDATE_OK server=CCZModStudio.McpServer protocol=2025-06-18 tools=32`。
- 2026-06-04 增量：`dotnet build .\工具整合包\CCZModStudio.McpServer\CCZModStudio.McpServer.csproj -v:minimal`：通过，0 警告，0 错误。
- 2026-06-04 增量：`dotnet build .\工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -v:minimal`：通过，0 警告，0 错误。
- 2026-06-04 增量：`dotnet run --project .\工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`。
- 2026-06-04 增量：临时 SDK client 使用 `StdioClientTransport` 连接 `CCZModStudio.McpServer.dll`，验证 E5 图片、知识库搜索、剧本指令模板、R/S 剧本结构、Hexzmap 读取和地图专用索引等入口可返回；旧维护类入口已不作为当前验收目标。
- `dotnet build .\工具整合包\CCZModStudio.McpServer\CCZModStudio.McpServer.csproj -v:minimal`：通过，0 警告，0 错误。
- `dotnet build .\工具整合包\CCZModStudio.sln -v:minimal -p:BaseOutputPath=..\_BuildCheck\`：通过，0 警告，0 错误。
- 2026-06-03 MCP stdio `initialize` + `tools/list`：通过，返回 11 个 tools。
- MCP `detect_project`：通过，能识别当前基底、HexTable 和旧工具资源路径。
- MCP `read_table`：通过，能读取 `6.5-0 人物` 指定列。
- MCP `write_table_rows`：通过，在测试副本写入 `6.5-0 人物` 的 `级别` 字段，生成 `Data.e5` 备份和 `数据表保存_WriteOperationReport.json`。
- `--rs-smoke`：通过，输出 `RS_SMOKE OK`。
- `--hexzmap-write-smoke`：复制测试副本，写入 `M000` 一个地形格，验证备份、结构化报告和复读。若该 smoke 失败，不能宣称地图地形同步已完成。

#### 旧 E5S 兼容验证

| 命令 | 用途 | 是否写入 | 说明 |
| --- | --- | --- | --- |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-e5s-smoke` | E5S 存档/旧探针兼容检查 | 否 | 只检查 `SV\*.E5S` 是否存在、`B形象指定器` 中 `CountSV`、文本线索和旧命令探针；输出会标注 `compat_only_not_rs_script` |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-all-smoke` | 历史全量探针 | 可能按旧逻辑继续运行 | 仅用于追溯旧实现，不作为当前主线成功标准；其中仍可能包含未清理的 SV004.E5S 历史断言 |

#### 规则

1. 后续提交或阶段自检优先运行：根工程 build、集成版 build、默认烟测、`--write`。
2. `.E5S` 只能作为“E5S 存档/旧探针兼容”检查对象，不能重新作为 R/S 剧本主格式。
3. 写入烟测必须只写测试副本，不能修改当前游戏本体；这只是测试策略，不代表工具功能必须以测试副本为前置条件。
4. 若某次旧全量探针失败，不得因此回退 R/S eex 主线；应拆分失败项并明确归入 E5S 兼容或高级调试。

#### 2026-06-01 补充：SmokeTests 输出目录被占用的 build 失败规避

在 Windows 上，如果某个旧的 SmokeTests/调试进程仍占用 `bin\\Debug\\...` 下的 `CCZModStudio.dll`，`dotnet build` 可能出现：

- `MSB3027/MSB3021 无法复制 ... because it is being used by another process`

这不是代码错误。建议改用独立输出目录构建 SmokeTests，再用该目录的 动态链接库 运行烟测：

```powershell
dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -c Debug -p:OutDir=..\..\CCZModStudio_TestBins\SmokeTests\
dotnet CCZModStudio_TestBins\SmokeTests\CCZModStudio.SmokeTests.dll --rs-smoke
dotnet CCZModStudio_TestBins\SmokeTests\CCZModStudio.SmokeTests.dll --write
```

#### 2026-06-05 R 场景制作页验证记录

- 根工程构建：`dotnet build 工具整合包\CCZModStudio.sln` 通过，0 警告，0 错误。
- R/S 主线烟测：`dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke` 通过，输出 `RS_SMOKE OK`。
- 本次未运行写回烟测作为 R 场景验收项，因为 `R场景制作` 页当前只保存项目侧 `CCZModStudio_Notes/*_RSceneDrafts.json` 草稿，不直接写回 `RS\R_XX.eex`。
- 需要人工界面复核的点：读取 R 剧情、选择 `Mmap.e5` 背景、拖放人物、切换方向和动作帧、保存草稿、重新读取后恢复草稿。

#### 2026-06-05 R 场景制作页二次修正验证

- 根工程构建：`dotnet build 工具整合包\CCZModStudio.sln` 通过，0 警告，0 错误。
- R/S 主线烟测：`dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke` 通过，输出 `RS_SMOKE OK`。
- 覆盖点：主入口顺序调整、核心创作卡片新增 `R场景制作`、R 形象 20 帧缩略图选择、`640x400` 斜菱形坐标换算、R 场景命令与剧本制作页定位联动。
- 仍需人工界面复核：缩略图点击是否符合创作者预期、拖放到边缘半格时坐标是否与旧工具一致、`30/32/33/34` 指令参数槽确认后再开放真实写回。

#### 2026-05-31 08:05 验证记录

- 根工程构建：通过，0 警告，0 错误。
- 集成版工程构建：通过，0 警告，0 错误。
- `--rs-smoke`：通过，确认 R/S eex 主线索引不混入 E5S。
- `--write`：通过，只在测试副本中验证 R/S eex 文本短写回与人物 R/S 表写回。
- `--legacy-e5s-smoke`：通过，仅为 E5S 旧兼容检查，输出仍标记 `compat_only_not_rs_script` 与 `old_probe_only`。
- `--legacy-all-smoke`：不作为当前主线验收；其中仍含旧 SV/E5S 假设，后续需单独降级或拆除。

#### 2026-05-31 08:30 宝物/物品写入烟测

- --write 已扩展复制 Star.e5 与 Imsg.e5 到测试副本。
- 新增 ITEM_WRITE_SMOKE_OK：验证 Data.e5、Star.e5、Imsg.e5 中物品名称、价格和说明文本可写回、可备份、可复读。

#### 2026-05-31 08:40 宝物解释列增强验证

- `宝物设定` 已新增 `大类`、`类型说明`、`价格显示`、`特效提示` 只读解释列。
- `dotnet build .\CCZModStudio.sln --no-restore -v:minimal /p:UseAppHost=false`：通过。
- `dotnet build .\CCZModStudio集成版\CCZModStudio\CCZModStudio.csproj --no-restore -v:minimal /p:UseAppHost=false`：通过。
- `--write`：通过，仍输出 `ITEM_WRITE_SMOKE_OK`。

#### 2026-05-31 08:43 本地知识库制度化后复核

- 重新读取 `B形象指定器\形象指定器6.5\System.ini`：确认 `FileHead=D2800`、`RFileHead=E1000`、`CountSV=900`、`UserPath2=...\RS\`。
- 重新读取 `CczRSX 6.5\ConfigTable\HexTable.xml`：确认 `6.5-0-4 R形象` 为 `Ekd5.exe:E1000`，`6.5-0-5 S形象` 为 `Ekd5.exe:D2800`。
- 重新扫描 RS 目录：`*.eex=119`，其中 `R_*.eex=60`，`S_*.eex=59`。
- `dotnet build .\CCZModStudio.sln --no-restore -v:minimal /p:UseAppHost=false`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，输出 `RS_SCENARIO_INDEX count=119 R=60 S=59 E5S=0` 与 `RS_SMOKE OK`。
- `--write`：通过，只写测试副本，输出 `RS_WRITE_SMOKE OK` 与 `ITEM_WRITE_SMOKE_OK`。

#### 2026-05-31 08:55 宝物图标预览验证

- 新增 `ItemIconPreviewService`：优先尝试标准图标枚举；若 `Itemicon.dll` 无标准图标，则解析 PE 资源目录中的 `RT_BITMAP`。
- 已确认 `Itemicon.dll` 中 `RT_BITMAP ID=100-439`，共 340 个资源，按 16x16/32x32 成对出现，当前候选图标数为 170。
- `--rs-smoke` 新增 `ITEM_ICON_PREVIEW`，可导出短剑图标 PNG。
- 根工程构建：通过，0 警告，0 错误。
- 集成版工程构建：通过，0 警告，0 错误。
- `--rs-smoke`：通过，输出 `ITEM_ICON_PREVIEW item=短剑 icon=0 count=170`。
- `--write`：通过，仍只写测试副本，并保留 `ITEM_WRITE_SMOKE_OK`。

#### 2026-05-31 12:30 当前主线写入烟测扩展

- `--rs-smoke` 新增 `ITEM_TYPE_CATALOG`：验证宝物类型目录关键修正。
- `--write` 新增 `ROLE_WRITE_SMOKE_OK`：验证人物基础字段和人物列传测试副本写回。
- `--write` 新增 `JOB_WRITE_SMOKE_OK`：验证详细兵种名称、说明、成长、穿透测试副本写回。
- `--write` 新增 `JOB_TERRAIN_WRITE_SMOKE_OK`：验证兵种系名称、地形发挥、移动消耗测试副本写回。
- `--write` 新增 `JOB_EFFECT_WRITE_SMOKE_OK`：验证兵种特效说明和分配测试副本写回。
- 2026-05-31 12:28 复跑：根工程 build、集成版 build、`--rs-smoke`、`--write` 均通过。



#### 2026-05-31 12:35 地形编辑写入烟测

- `--write` 新增 `HEXZMAP_WRITE_SMOKE_OK`：验证 `Hexzmap.e5` 在测试副本中可按地图分辨率/48 读取变量尺寸地形块、修改两个地形格、自动备份、生成结构化报告并复读。



#### 2026-05-31 地图/地形变量尺寸验证

- 手工尺寸校验确认：地图格数由 `Map\Mxxx` 图片分辨率除以 48 得出，`Hexzmap.e5` 地形数据长度为 `gridWidth * gridHeight`。
- 已验证样例：
  - `M000 image=960x960 grid=20x20 cells=400 expectedSegment=402 hexSegment=402 dataOffset=3E6`
  - `M003 image=1344x960 grid=28x20 cells=560 expectedSegment=562 hexSegment=562 dataOffset=89C`
  - `M004 image=960x960 grid=20x20 cells=400 expectedSegment=402 hexSegment=402 dataOffset=ACE`
  - `M018 image=1536x1536 grid=32x32 cells=1024 expectedSegment=1026 hexSegment=1026 dataOffset=2A8A`
  - `M056 image=1776x1440 grid=37x30 cells=1110 expectedSegment=1112 hexSegment=1112 dataOffset=9E5A`
- `Hexzmap.e5` 目录解释已更新：目录表从文件绝对偏移 `110` 开始，每项 12 字节，大端字段为 `segmentLength`、长度副本、`fileOffset`；地形数据从 `fileOffset + 2` 开始。
- 根工程构建：`dotnet build .\CCZModStudio.sln --no-restore -v:minimal -m:1 /p:UseAppHost=false` 通过，0 警告，0 错误。
- 集成版工程构建：`dotnet build .\CCZModStudio集成版\CCZModStudio.sln --no-restore -v:minimal -m:1 /p:UseAppHost=false` 通过，0 警告，0 错误。
- 旧 `--legacy-all-smoke` 仍含 `SV004.E5S` 历史联动断言，不能作为本次地图/地形变量尺寸改动的失败标准。



#### 2026-05-31 12:40 战场制作文案收敛验证

- `rg "SV结构|SV 关卡|未知 SV|SV 命令|SV/Map|地图/SV|EEX/E5/SV|跳到SV"` 检查主工程和集成版核心文件：无残留。
- 根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。

#### 2026-05-31 12:45 剧本制作读写边界

- 核心剧本制作页 保存选中文本 已确认直接写入 RS\\*.eex 当前项目文件，并执行备份和复读校验。
- --write 继续以 RS_WRITE_TEXT_OK 验证测试副本 R/S eex 文本短写回闭环。


#### 2026-05-31 12:40 地图底图替换烟测

- --write 新增 MAP_IMAGE_WRITE_SMOKE_OK：验证 Map\\M*.jpg 在测试副本中可整文件替换、自动备份、生成结构化报告，并用 SHA256 与图片尺寸复读。


#### 2026-05-31 12:45 战场制作文本写入烟测

- --write 新增 BATTLEFIELD_TEXT_WRITE_SMOKE_OK：验证 BattlefieldEditorService.SaveTitleAndConditions 在测试副本中可写 R/S eex 标题/胜败条件文本、自动备份、生成结构化报告并按原偏移复读。


#### 2026-05-31 12:55 兵种矩阵写入烟测

- --write 更新 JOB_MATRIX_WRITE_SMOKE_OK：验证 6.5-3-3 兵种相克 与 6.5-4-2 兵种成长内的详细兵种可装备类别在测试副本中可写回、自动备份并复读。


#### 2026-06-06 旧版树中文显示烟测

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- SmokeTests：`dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 新增 `LegacyScenarioCommandDisplayFormatter` 样例断言，覆盖 `05`、`25`、`36` 的旧版中文摘要显示；通过。
- `--rs-smoke` 通过，确认 R/S 读取、旧版完整树读取、战场候选、资源预览等主线未回归。

#### 2026-06-06 旧版右键菜单迁移验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- SmokeTests：`dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj` 通过，0 警告，0 错误。
- `--legacy-script-edit-smoke` 通过：测试副本读取 `R_00.eex`，新增普通命令、修改参数、删除命令并三次完整保存/复读/备份/报告均成功。
- `--legacy-mfc-dialog-smoke` 通过：旧式对话框 目录、资源解析、模板和中文摘要样例仍正常。
- `--rs-smoke` 通过：R/S 索引、旧版完整树读取、战场候选、资源预览等主线未回归。
-界面行为需人工点验：剧本制作页旧版树右键菜单应显示 `IDR_POPUP_EDIT` 同序菜单；`添加/添加子事件/粘贴` 在当前指令前插入；`批量修改` 保持禁用。

#### 2026-06-06 战场 S 剧本树中文映射修正验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--rs-smoke` 通过，确认战场旧版候选、地图预览、资源预览主线未回归。
- `--legacy-mfc-dialog-smoke` 通过，确认旧版 对话框 资源与中文摘要 formatter 未回归。
- 备注：首次并行执行构建和 smoke 时命中 MSBuild `obj\Debug\net8.0-windows\CCZModStudio.dll` 写锁；改为顺序执行后全部通过。

#### 2026-06-06 剧本制作页右侧布局验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- 静态检查：剧本制作页不再创建右侧 `remarkEditPanel`，不再挂载 `BuildScriptEditorPage.ParameterText`；右侧只保留 `BuildScriptEditorPage.ImageParameter` split。
-界面需人工点验：右侧下半部分应完全由参数 对话框 占据；图片预览和参数区之间的分隔条可上下拖动。

#### 2026-06-06 剧本制作页右侧标题收敛验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 通过，确认旧式对话框 资源和控件生成未回归。
- 静态检查：`CreateTitledPanel("图片预览"...`、`MakeHeader("参数")`、`imagePreviewPanel`、`_scriptImagePreviewInfoBox` 的右侧挂载点均无残留。

#### 2026-06-06 旧式对话框 中文控件裁剪修正验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 通过，确认旧式对话框 资源解析、初始化/提交逻辑和中文摘要样例未回归。
-界面修正点：`LegacyMfcDialogHostControl` 在初始化后实测中文标签和下拉项宽度，扩宽同一行控件并顺移后续控件；必要时通过宿主横向滚动兜底。
-界面需人工点验：变量测试、变量运算等包含“变量类型 / 数值 / 操作 / 关系”的旧式对话框 不应再截断中文标签或下拉框当前项。

#### 2026-06-06 R 场景制作页中文映射验证

- `--legacy-mfc-dialog-smoke` 新增 R 场景视觉命令候选断言：构造 30 武将出现命令后，`RSceneDraftService` 通过旧版 formatter 输出人物/坐标/方向/动作摘要，且不出现 `P0=` 裸参数。
- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 顺序执行通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 通过，确认旧式对话框、旧版中文摘要和 R 场景候选中文显示未回归。
- `--rs-smoke` 通过，确认 R_00.eex 旧版完整树读取、R/S 索引、战场候选和资源预览主线未回归。
- 备注：首次并行执行主工程 build 与 smoke 时命中 `obj\Debug\net8.0-windows\CCZModStudio.dll` 写锁；改为顺序执行后通过。
