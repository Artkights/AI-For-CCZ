# 二次行动机制详解

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：逆向工程文档，需以本地样本和调试记录复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认写入路径；如需适配，必须另建版本护栏和样本验证。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：逆向工程文档，需以本地样本和调试记录复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

#### 概述

**二次行动**是一种特技，效果为：角色行动结束后可额外行动一次，每回合仅生效一次。

> ⚠️ **专属性说明**：本文档涉及的所有函数（`0x412D52`、`0x421175`、`0x406690` 在二次行动流程中的调用、`0x508B00` 状态数组）**仅服务于二次行动这一个特技**。引擎不会为没有二次行动特技的单位自动恢复行动力——没有该特技的单位在攻击、施法、移动等行动结束后，行动标志位不会被清除，不会获得额外行动机会。

#### 特技参数

| 参数 | 值 |
|------|-----|
| 装备特效号 | 90 (0x5A) |
| 个人特效号 | 56 (0x38) |
| 效果值标志 | 1 (不需要效果值) |
| 叠加标志 | 1 (不可叠加) |

#### 核心机制

二次行动的核心机制围绕**状态数组 0x508B00**展开，确保每回合只触发一次。

##### 状态数组 0x508B00

每个单位占用1字节，存储二次行动的使用状态：

| 值 | 含义 |
|----|------|
| `0x00` | 本回合尚未触发，等待激活 |
| `0x01` | 单位不拥有该特技，不可用 |
| `0xFF` | 本回合已触发过，已使用（受保护） |

##### 状态转换流程

```
回合开始 → 重置循环将所有 0x508B00[i] 设为 0x00
    ↓
行动处理 → 调用点2判定：有特技→写0x00，无特技→写0x01
    ↓
行动结束 → 调用点1检查=0x00 → 判定通过 → 恢复行动 → 写0xFF
    ↓
再次行动 → 调用点1检查=0xFF → ≠0 → 跳过（不再触发）
```

#### 完整调用时序

##### 1. 回合开始 - 重置循环

**函数**：0x44AF8F

在新回合开始时，遍历所有单位，重置二次行动状态。

```asm
void reset_turn() {
    for (int i = 0; i < 250; i++) {
        Unit* unit = get_unit_ptr(i);       // 0x4061F9
        unit->field_23 = 0;                 // 清除回合状态
        restore_action(unit, 0x06);          // 0x406690: 恢复行动
        state_flags[i] = 0;                  // 0x508B00[i] = 0（重置为可用）
    }
    global_turn_flag = 0xFF;                // 0x501BF9 = 0xFF
}
```

##### 2. 行动处理 - 调用点2

**地址**：0x43D522

在行动处理流程中，初始化/更新二次行动的激活状态。

```asm
0043D522: call 0x421175             ; 调用二次行动桩函数
0043D527: test al, al
0043D529: jz 0x43D52D               ; 判定失败 → al保持0
0043D52B: mov al, 0x01              ; 判定成功 → al=1
0043D52D: xor al, 0x01              ; 翻转：成功→0, 失败→1
; ...
0043D53C: cmp byte [edx], 0xFF      ; 已是0xFF（已使用）？
0043D540: jz 0x43D544               ; 是 → 跳过，保护已使用状态
0043D542: mov byte [edx], al        ; 写入新状态
```

**逻辑说明**：
- 判定通过 → al=0 → 写入 0x508B00[unit_id] = 0（标记"已激活"）
- 判定失败 → al=1 → 写入 0x508B00[unit_id] = 1（标记"不可用"）
- 如果已经是 0xFF，调用点2不会覆盖

##### 3. 行动结束 - 调用点1

**函数**：0x412D52

在角色行动结束后，检查并恢复二次行动能力。

```c
bool try_second_action(byte unit_index) {
    unit = get_unit_ptr(unit_index);           // 0x4061F9
    if (unit == INVALID) return false;
    if (unit->hp == 0) return false;          // 死亡单位不触发
    if (state_flags[unit_index] != 0)         // 0x508B00[index]
        return false;                         // 本回合已用过

    if (!stub_second_action())                 // call 0x421175
        return false;                         // 特技判定失败

    restore_action(unit, 0x06);               // call 0x406690
    update_action_ui(unit, 0x88, 0x06);      // call 0x43E6AA
    state_flags[unit_index] = 0xFF;           // 标记已使用
    return true;
}
```

##### 4. 再次行动 - 调用点1重复检查

调用点1会检查 state_flags[index] 是否为0x00，只有状态为"等待触发"时才会恢复行动能力。

#### 关键函数详解

##### 桩函数 - 0x421175

二次行动特技判定入口。

```asm
00421175: push ebp
00421176: mov ebp, esp
00421178: push 0x01        ; 效果值标志 = 1
0042117A: push 0x01        ; 叠加标志 = 1
0042117C: push 0x5A        ; 装备特效号 = 90
0042117E: push 0x38        ; 个人特效号 = 56
00421180: call 0x00600819  ; 调度函数
00421185: pop ebp
00421186: ret
```

##### 行动恢复函数 - 0x406690

通过位清除操作恢复单位的行动能力。

```asm
00406690: push ebp
00406691: mov ebp, esp
00406693: movzx eax, byte [ebp+0x08]  ; 取参数（行动类型位）
00406697: not eax                      ; 取反（如 0x06 → 0xFFFFFFF9）
00406699: and byte [ecx+0x0D], al     ; 清除 unit[0x0D] 的对应位
0040669C: mov esp, ebp
0040669E: pop ebp
0040669F: ret 0x04
```

**公式**：`unit[0x0D] &= ~参数`

**重要说明**：
- 参数是**位掩码**，不是"位号"
- 函数会**清除**参数指定的位（即恢复行动能力）
- 参数 = 0x00 时，`~0x00 = 0xFF`，`unit[0x0D] &= 0xFF`，**所有位不变**

**常用参数映射**：

| 参数 | 二进制 | 清除的位 | 效果 |
|------|--------|----------|------|
| `0x01` | `00000001` | bit 0 | 恢复移动能力 |
| `0x02` | `00000010` | bit 1 | 恢复攻击能力 |
| `0x03` | `00000011` | bit 0 + bit 1 | 恢复移动+攻击 |
| `0x06` | `00000110` | bit 1 + bit 2 | 恢复特殊行动能力 |

**示例**：
```asm
; 恢复攻击能力
push 0x02              ; 参数 = 0x02
mov ecx, ebx           ; ecx = 单位指针
call 0x406690          ; 执行后：unit[0x0D] 的 bit 1 被清除
```

##### UI更新函数 - 0x43E6AA

更新行动UI状态，让玩家看到行动能力已恢复。

#### 关键数据结构

| 地址/偏移 | 说明 |
|-----------|------|
| `0x508B00[index]` | 二次行动状态数组（最多250个槽位） |
| `0x501BF9` | 全局行动类型/回合标志 |
| `unit+0x04` | 单位ID（1字节，用作0x508B00的索引） |
| `unit+0x0D` | 行动状态位掩码（1字节，每位对应一种行动类型） |
| `unit+0x23` | 回合相关状态标志（1字节，新回合清零） |

#### 相关函数速查

| 地址 | 名称 | 作用 |
|------|------|------|
| `0x421175` | 桩函数 | 二次行动特技判定入口 |
| `0x600819` | 调度函数 | 设置EDX=0x37A0后调用核心引擎 |
| `0x406690` | restore_action | 位清除恢复行动能力（unit[0x0D]） |
| `0x412D52` | try_second_action | 行动恢复判定与执行 |
| `0x43E6AA` | update_action_ui | 更新行动UI状态 |
| `0x44AF8F` | reset_turn | 新回合重置循环（遍历0~249） |

#### 相关文档

- [单位结构体](../02-数据结构/unit-structure.md) - 了解unit[0x0D]的含义
- [全局变量](../02-数据结构/global-variables.md) - 了解0x508B00的作用
