# 04 待补充知识清单

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认写入路径；如需适配，必须另建版本护栏和样本验证。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

本页只保留仍会影响实现、写入边界或实机验证的缺口。已经沉淀到 `01/03/13/19/20/21/22` 等知识页的旧条目，不再重复列为待补；需要追溯历史时查 `00-总览与规范/更新记录.md`。

#### 2026-06-05 清洗后的已解决口径

- `.E5S` 已降级为存档信息/旧兼容探针，不再作为 R/S 剧本主格式。
- `.eex` 已确认是 R/S 剧本主线格式；旧版完整树 读取器/写入器 已能读取并重建 `EEX\0` 头、场景 偏移表、场景/段落/CommandBlock、子块长度和 `0x76` 跳转位移。
- “R/S eex 只能短文本写回、不得扩容”是旧口径；现在应区分两条路径：兼容短写回仍受原 GBK 容量限制，旧版完整树文本参数写回可通过 `LegacyScenarioWriter` 重建结构。
- B 形象指定器的 `FileHead=D2800` / `RFileHead=E1000` 已确认为 6.5 `Ekd5.exe` 中人物 S/R 编号表文件偏移，当前表项按 `UInt16` 风格读写。
- 人物图像资源链已固定为 `Face.e5`、`Pmapobj.e5`、`Unit_atk.e5`、`Unit_mov.e5`、`Unit_spc.e5` 的 E5 `0x110` 索引表；`RS\R_*.eex/S_*.eex` 不再作为人物图像封包。
- `Tr.e5` / `信息传送28` 已建立独立知识页：文件可由 `Ts.e5` 复制改名，0 图号用于取消/预留，R 插图最多 8 个，图片尺寸和槽位内存上限已有 6.5 发布贴口径。
- 写入功能不再一律要求测试副本前置；测试副本用于隔离验证。所有正式写入仍必须走目标路径校验、备份、复读和结构化报告。

#### A. R/S eex 与剧本命令

- 将 `全剧本指令详解（2023）.docx` 的 60 个指令章节逐条和 `ScenarioCommandParameterTemplateService` 模板比对，标注“模板已覆盖 / 名称不一致 / 参数槽缺失 / docx 未验证”。
- 将外部 `曹操传mod之s剧本完全教程——基础篇` 的战前/战中/战后三段式制作习惯，与本地 `S_*.eex` 样本和旧剧本编辑器树结构逐关比对；只生成只读流程模板，不生成写回规则。
- 将 `如何做MOD` 和 97 经验技巧帖中关于 R/S 跳转、即时判定、子事件承接命令、无条件跳转、修改后复读的排错口径，逐条标注“已被旧源码证实 / 需样本验证 / 仅经验提醒”。
- 对 `72 信息传送`、`77/78/79 整型变量`、`58 障碍/地形`、`71 特效请求`、`4D 战场状态` 等高风险命令建立单独知识页；没有样本和实机验证前不生成写回操作。
- `46/47` 出场记录当前只开放已有记录的受控坐标/人物/已确认 人工智能 槽写回；仍需继续补齐等级、隐藏、阵营、方向和其它槽位的真实语义。
- `4B` 我军出战排列、`06/4A` 强制/禁止出战列表、`4057` 出战位置预览已经可读可叠加，但仍需更多 R/S 样本验证跨关写法和异常边界。
- 跨 段落 移动命令、批量复制子事件、重排 场景、直接手填跳转、修改可变数组等结构性操作仍保持只读研究。
- 建立 R/S 流程核对模板：R 入 S 出战测试与 `11` 跳转、S 回 R 胜利/失败测试后的 `11` 跳转、`5A` 战场操作开始、三段式 S 场景、即时判定唯一性和 `0x76` 跳转目标复读。
- 若导入非当前 6.5 未加密基底的 `.eex`，仍需核对是否存在额外压缩、加密、校验或封包外层差异。
- 建立 6.x 整型变量验证矩阵：`4017/4018/4020/4021/4023/4031/4038/4080` 分别记录来源版本、默认值、作用域、是否跨命令影响、是否有本地脚本样本和是否完成实机验证。
- 对 `4031` 单独验证上下文：确认它在 `32` 武将移动、`72 信息传送1`、复制人相关命令中的 Data 号/战场编号切换范围，以及重置为 `0` 的必要性。

#### B. R 场景、R 插图与人物动作

- 对齐 `剧本编辑器v0.24` 的 R 场景交互：验证 `Ctrl` 吸附模式、普通滚轮改朝向、`Shift+滚轮` 改动作在旧工具中的具体行为；当前整合包滚轮主要用于缩放，后续应决定兼容方案并补 界面/烟测。
- 为 R 场景姿态写回补烟测：在测试副本中修改 `0x30` 方向/动作槽、`0x33` 转向槽和 `0x34` 动作槽，完整保存后复读确认；未验证前不把所有姿态操作视为同等安全写回。
- 解析或实测 `剧本编辑器v0.24` 的 `导出为JSON文件...`、`启动HTTP服务器`、`停止HTTP服务器` 输出结构和安全边界；当前只确认菜单存在，不复刻为默认能力。
- 对 `信息传送28` 做实机验证：图号 0 取消、预留图、透明度 0..100、绘制层次、坐标越界、多个插图同时存在时的遮挡和内存上限。
- 验证当前 6.5 基底无 `Tr.e5` 时是否只需复制 `Ts.e5 -> Tr.e5`，以及成品目录是否需要补齐配套文件和版本护栏。
- 补齐 `Pmapobj.e5` R 动作帧的完整语义表；当前只确认常见 `48x64 * 20` 帧切分和 R 场景草稿预览，不等于完整重封包规则。
- 核对 R 插图与普罗 R 半身像对话技术共存时的绘制层次和遮挡规则。
- 验证 `4020/4021` 对自由 R 鼠标变手和名字显示的影响，区分 6.5 当前行为与 6.6 修正后的行为。

#### C. 地图、战场与 Hexzmap

- P0：当前本地烟测样本 `Hexzmap.e5=44840`，而 `ProjectVersionGuardService` 的 6.5 写入护栏基准仍为 `45254`。未补齐样本来源、旧工具对照、目录表解释和写后复读证据前，所有 Hexzmap 直写必须继续拒绝，不能通过 界面、Smoke 或 MCP 绕过。
- 继续对照地形编辑器确认 `Hexzmap.e5` 块顺序、缺失地图编号、段头 2 字节、尾部字节和尺寸表。
- 对照 2026-06-08 新增外部来源：`关于曹操传如今唯一的地图问题的一个想法`、`Ccz地形编辑器`、`发布地形预测器`，确认旧工具/帖子中的 `hexmap.e5`、`Hexzmap.e5`、地形文件导入与当前 6.5 基底文件是否同源。
- 解释当前样本 `Hexzmap.e5=44840` 与写入护栏基准 `45254` 的差异；未确认前不放开对应样本的直写。
- 补齐 `Map\Mxxx.jpg`、`Hexzmap.e5`、R/S eex 战场脚本之间的权威引用链，尤其是地图编号破百、自由 R/S 跳转和战场障碍联动。
- 地图/地形成套导出、批量底图替换、地形块复制粘贴和发布前一致性报告仍需继续完善。

#### D. 人物、兵种、宝物与平衡表

- 角色 `Army` 字段、人物加入状态和战场身份之间的传统编辑习惯仍需旧工具或实机确认。
- 宝物/物品 `类型` 字段已建立候选枚举，但未知类型码、辅助装备类别 `2/3`、宝物图鉴显示规则仍需继续核对。
- 装备特效号与特效值的逐项实际效果仍需结合 B 形象指定器、CczRSX 6.5、旧教程和实机验证。
- 兵种相克矩阵、兵种属性 8 行、人物专属/套装专属表、兵种特效名称区真实结构仍需转成创作者友好界面和烟测。

#### D2. 6.5 注入特效包

- 对 `03-机制详解/6.5特效注入索引.md` 中 17 个本地注入文本建立安装前复读项：确认目标 `Ekd5.exe` 地址原字节、Hook 覆盖长度、代码洞空闲状态和回跳地址。
- 2026-06-09 已生成第一批静态复读证据：`CCZModStudio_Reports\DebugEvidence\debug-baseline-20260609-163804`。下一步必须用 x32dbg MCP 对 `0x4101D9/0x4061F9/0x43C2xx/0x4259xx` 等断点做实机命中，采集寄存器、栈、调用栈、附近内存和反汇编后再升级证据等级。
- 当前 `基底\加强版6.5未加密版\Ekd5.exe` 中 `0x418335` 和 `0x43C2D5` 静态原字节显示已有跳转，需判别是基底自带、历史补丁残留，还是与本地补丁文本一致；未判别前不得把它们当作“原始 6.5 未安装”状态。
- `0x43C2B0` 与 `0x43C2B5` 静态读取为连续 NOP，需在动态调试中确认它们是否只是策略链附近的空洞/跳板，而不是会自然命中的 Hook 点。
- `0x601A9A-0x602500` 当前 PE 映射失败，必须追溯旧口径来源版本；未完成前从可写空洞候选中移除。
- 为策略类补丁建立组合烟测：`噬心毒咒 -> 策略偷钱` 链式回跳、`策略冲锋/聚势伐谋/策略保底限伤/无视策略减伤` 的伤害计算顺序、策略暴击后的最终伤害。
- 护卫与殭屍大法原样代码区重叠，需要重排代码洞或合并逻辑后再验证；未处理前不得作为可同时安装组合。
- 两个“回MP攻击”文件是同地址变体，需要在当前基底反汇编 `0x418335` 附近原指令后确定使用 171B 版还是 136B 版。
- 验证剧本整型变量 `4003` 到运行地址 `0x505E8C` 的同步方式、取值范围、是否只读低 1 字节，以及保底伤害是否吃增伤和暴击加成。
- 对大杀四方补丁单独验证 `0x508B00` 状态数组与原生二次行动、击杀判定、回合重置、物理/策略双路径是否冲突。

#### E. E5 图片、动态链接库图标与音频资源

- 通用 Ls/E5 重封包、整体重排和批量导入仍未确认；当前只开放可读取 `0x110` 索引表的单条图片替换。
- `Unit_atk.e5 / Unit_mov.e5 / Unit_spc.e5` 的完整动作帧序列、朝向规则、批量三文件同步替换仍需继续整理。
- 对照 `曹操传非攻略问答整理` 与 6.1 发布资料，确认三文件在旧工具中的导入依赖、真彩/256 色兼容边界和 6.5 当前 RAW 帧条之间的差异；没有帧序列前不开放整套 S 形象批量替换。
- 对照 `MEff文件的初步研究`、Meff.e5 编辑器和 6.1 发布资料，验证 `Meff.e5 / Mcall.e5` 的 LS 封包、策略动画帧宽高、调用参数和真彩化跨版本差异；原帖估测内容只能作线索。
- `Itemicon.dll / Mgcicon.dll / Cmdicon.dll` 当前只读预览；PE 资源表写回、语言号、位图格式和备份复读规则尚未开放。
- 音频引用表、内置音乐、`WAV` / `SoundTrk` / CD 音轨等资源链仍未进入结构化知识页。

#### G. 版本分支与外部资料

- 为 6.5p/6.5pl 建立独立分支适配清单：样本下载来源、核心文件尺寸、HexTable、装备/道具扩展、特技书/策略书资源、4083/4087 等变量与默认 6.5 的差异。
- 任何 6.5p/6.5pl、6.6/6.6x、旧 EXE 变量资料都必须先写入版本差异页和待验证项；没有本地样本与实机验证前，不得进入 6.5 未加密默认写回路径。
- 维护联网资料复核记录：每个外部来源至少记录 URL、标题、关键词命中、版本范围、证据等级、是否已被本地样本/旧工具/烟测验证。

#### F. MCP 与证据链

- MCP 当前保留知识检索、制作读写、地图专用索引、图片资源和特效包能力；下一步是把实机截图、报告和本地结论继续整理为可检索知识。
- `search_knowledge_entries` 可检索 Markdown，但 docx 原始资料、网页来源和本地旧工具源码仍需要持续转成短结论、证据路径和版本边界。
- 成品整理记录应继续覆盖核心文件哈希、地图尺寸、R/S 剧本可读性、E5 图片索引和最近备份。
- x32dbg MCP 动态采证矩阵：先用 `start-x32dbg-6.5.ps1` 打开目标，确认插件监听 `127.0.0.1:27042`；再按 `breakpoints-6.5-baseline.json` 设置软件断点；每个断点至少用两个实机场景复核，并把导出的寄存器/内存/调用栈证据写入 `CCZModStudio_Reports\DebugEvidence`。
- x32dbg MCP 当前实测状态：17 个计划断点已经布设；`0x00484002 get_char_data_ptr` 在标题/初始化路径动态命中。点击标题入口“星汉灿烂”后又命中 `0x0042518F ability_check_wrapper`、`0x0041301E dual_channel_check`、`0x00413009 get_effect_value`，形成 `0x42518F -> 0x41301E -> 0x413009` 的 fallback 证据链。下一步必须进入战场或带有效特效的场景，继续验证 `0x4101D9` 主分支和单位/策略/暴击路径。

#### F1. x32dbg 下一轮实机触发矩阵

目标 EXE 固定为 `基底\加强版6.5未加密版\Ekd5.exe`，SHA256 `84E3A1DC085AE6F9900D1E8C388A9CD6766379832DDF51BC7BDF780C6615B4A3`。以下项目在动态证据闭环前不得作为写入依据。

| 优先级 | 地址 | 名称 | 触发场景 | 当前证据等级 | 下一步 |
|--------|------|------|----------|--------------|--------|
| P0 | `0x4061F9` | get_unit_ptr | 进入战场后选择/移动/攻击任一单位；回合开始遍历单位 | 已命中 1 次，但为 R 场景/UI 哨兵 `ECX=0xFFFF` | 地址形态已验证为 `low8(index) * 0x30 + 0x4A7B20`；仍需正式战斗中记录有效编号、返回指针、`unit+0x10/+0x14/+0x1E` |
| P0 | `0x4101D9` | core_engine | 装备或个人特效判定：攻击、被攻击、行动结束、回合开始 | 静态复读 | 记录 4 个参数、`ECX/EDX`、调用者，核对装备/个人双渠道 |
| P1 | `0x413009` / `0x41301E` | get_effect_value / dual_channel_check | 与 `0x4101D9` 同场景，优先选带特效装备/个人特技单位 | 已命中 fallback 链路 1 次 | 继续记录不同效果号、效果值；需要能走 `0x4101D9` 的有效特效场景 |
| P0 | `0x43C242` / `0x43C2D5` | strategy_reduce_check_hook / strategy_floor_cap_hook | 策略攻击、策略减伤、策略保底/限伤 | 静态复读；`0x43C2D5` 已有跳转需判别 | 命中后确认是否进入当前基底已有跳转逻辑，区分基底自带与补丁残留 |
| P0 | `0x4259AF` / `0x4259B4` | strategy_random_status_hook / strategy_money_on_hit_hook | 策略伤害后、多目标策略 | 静态复读 | 验证策略后伤害链、目标单位指针、是否可串接 |
| P1 | `0x41B500` / `0x40728F` | get_unit_hp / get_max_mp | 单位面板、战斗计算、策略消耗 | `0x40728F` 已在 R 场景/UI 查询命中 1 次；`0x41B500` 未命中 | `0x40728F` 已确认读取 `ecx+0x1F` 并做最大 MP 计算；仍需正式战斗或人物面板复核入参单位指针和返回值 |
| P1 | `0x42518F` | ability_check_wrapper | 任意包装型特技触发 | 已命中 1 次，已确认预检失败回退 `0x41301E` | 继续验证预检通过后调用 `0x4101D9` 的分支 |
| P1 | `0x450986` | generic_event_dispatch | HP 变化、击杀、事件处理 | 静态复读 | 确认 `ECX=0x100` 等事件语义 |
| P1 | `0x418335` | mp_recover_on_hit_hook | 物理攻击命中、回 MP 攻击候选 | 静态复读；首字节已是 `E9` | 判断当前跳转是否为基底自带或历史补丁，不得直接覆盖 |
| P2 | `0x43C2B0` / `0x43C2B5` | strategy_charge_hook / strategy_group_power_hook | 策略伤害计算链 | 静态复读；连续 NOP | 判别它们是自然执行地址、跳板空洞还是补丁落点 |
| P2 | `0x472D92` | overbearing_on_crit_hook | 暴击触发盛气凌人/属性下降 | 静态复读 | 需要具备可控暴击场景后再采证 |

推荐采集命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "工具整合包\MCP配置\run-x32dbg-until-planned-hit.ps1" -DisableAddress 0x00484002 -ContinueAfterUnplannedStop -MaxRuns 20 -PollsPerRun 240 -PollIntervalMs 250
```

#### F2. 功勋模式与战场操作采证

- 2026-06-09 已建立功勋模式首个实机样本：颍川之战中曹操攻击张梁，张梁反击被曹操格挡，UI 显示武力、敏捷、统率功勋各增加 1 点。证据目录：`CCZModStudio_Reports\DebugEvidence\merit-human-operator-20260609-192350`。
- 自动功勋写断点链路已具备离线预检、布点实验、命中汇总和知识库草稿：`start-6.5-merit-auto-experiment.ps1`、`run-6.5-merit-watchpoint-experiment.ps1`、`summarize-6.5-merit-watchpoint-hits.ps1`。最新 DryRun 报告为 `CCZModStudio_Reports\DebugEvidence\merit-auto-experiment-20260610-001237`，知识库草稿为 `CCZModStudio_Notes\DebugKnowledgeDrafts\merit-knowledge-draft-20260610-001248.md`，结果 `HitCount=0`，不得升级候选证据等级。
- 下一步必须重新定位五维功勋实际存储结构。既有候选中，`0x00492F9C` 已证伪为战场输入/选择状态，`0x00492C20` 已改判为防具 EXP 缓存，`0x004B1B1A` 已证伪为对话链写入 `[0x004B1B18]` 的覆盖范围，`0x00496E79` 已证伪为资源路径字符串缓冲。
- 将功勋事件拆成单因子样本：物理攻击但不格挡、只被攻击、格挡、双击、暴击、策略使用、被策略命中、策略格挡、击退和过关奖励；每个样本至少两次复核。
- 建立战场操作自动化状态机：空闲、单位选中、紫色移动格、红色攻击范围、攻击指令、目标预览、动画中、敌军回合、回到可操作状态。
- 自动点击前必须验证敌军是否在攻击范围内；若不在范围内，先通过单位坐标和紫色移动格选择无单位占据的位置，再验证移动后红色攻击范围。
- 2026-06-09 用户纠错：当前颍川之战没有郭嘉、荀彧，曹操可以释放策略；在编号体系闭环前，不得将 `unit+0x04` Data 号低字节直接映射为当前画面人物名。
- 2026-06-09 实机确认：除二次行动、再次移动等特技外，人物一回合通常只能执行一次“移动 + 行动”；行动包括攻击、策略、道具或待命，行动后形象变灰，点击提示“已经行动完毕”。

#### F3. 战场自动化操控待闭环

本节记录测验1/测验1续后仍阻止“无人战场操控”的关键缺口。已确认的物理伤害、HP 写入、反击执行链和 EXP 缓存写入已经沉淀到 `物理攻击.md`、`战斗数据结构.md`、`函数速查表.md`；此处只列未闭环项。

| 优先级 | 缺口 | 当前状态 | 通过标准 |
|--------|------|----------|----------|
| P0 | 移动坐标写入函数 | 只有 `0x0040659B -> 0x004065A4` 候选，未做只移动样本 | 单独移动曹操，命中坐标字段写断点，记录旧/新坐标、EIP、调用栈 |
| P0 | UI 状态机 | 只能人工识别单位选中、移动格、攻击范围、弹窗 | 找到内存字段或可复读信号，区分空闲、选中、菜单、目标选择、动画、自动阶段 |
| P0 | 屏幕格坐标映射 | 缺镜头偏移、窗口缩放和格子到像素映射闭环 | 给定战场坐标能点击到正确格子，并由单位数组验证移动/选中结果 |
| P0 | 攻击/结束回合按钮门禁 | 人工知道按钮位置，但自动点击缺禁用状态识别 | 点击前能确认按钮可用，点击后能确认进入目标选择或阶段切换 |
| P0 | 可复现读档/战场夹具 | 当前大量样本依赖人工推进 | 能从固定存档进入同一战场同一状态，供自动采样重复执行 |
| P1 | 内部 action dispatcher ABI | 已有攻击入口和自动阶段调用栈，但不能直接内部调用 | 明确移动、攻击、待命、结束回合的函数入口、参数和恢复规则 |
| P1 | 异常恢复 | 右键取消移动、对话弹窗、撤退台词会打断预期流程 | 自动化能识别取消/对话/弹窗并恢复到安全状态 |
| P1 | 战斗 AI 决策 | 只有观测链，没有路径规划和目标选择规则 | 能根据单位坐标、HP、阵营、攻击范围选择合法动作 |
| P1 | 功勋反馈 | 五维功勋字段仍未定位 | UI 功勋变化、内存字段、写入函数三者闭环 |

阶段性结论：

- 可以继续做“自动采证”和“固定场景半自动操作”。
- 不应宣称已经完成“全自动战场操控”。
- 在 P0 项闭环前，任何真实点击器都必须带 `allow_input=true` 类显式开关，并默认只做 dry-run/计划输出。

#### 2026-06-11 测验1后续待验证项

以下项目来自曹操攻击黄巾兵并触发连击的实机调试。已确认的 HP 写入闭环已沉淀到单位结构体、物理攻击、连击、战斗公式和函数速查表；本节只保留尚未闭环的工作。

| 优先级 | 待验证项 | 当前状态 | 推荐验证方式 |
|--------|----------|----------|--------------|
| P0 | 移动坐标写入函数 | 本次未调试移动动作；不能确认具体函数 | 对曹操移动前后坐标候选字段设写断点，记录写入者、调用栈、旧/新坐标 |
| P0 | 坐标字段精确定义 | `unit+0x06/+0x07` 曾作为坐标候选，但本次未动态闭环 | 用单步移动样本验证画面格坐标、内存值、镜头偏移和单位数组槽位 |
| P0 | 伤害公式上游 | 本次只确认 HP `118->59->15` 和最终 setter | 从 `0x00405966` 入口结果记录反向追踪 `59`、`44` 的写入来源 |
| P0 | 连击判定函数 | 用户确认发生连击，但判定分支未命中 | 追踪第二段伤害记录的生成点，区分连击、反击、暴击和追击 |
| P1 | 攻击结果缓冲结构 | 已观察 `0x00405966` 循环处理多段结果，但结构未解析 | 对结果缓冲区及其候选地址写入点设断，记录段数、目标、伤害、标志位 |
| P1 | 行动状态位 `unit+0x0D` | 攻击后相关函数命中，但各 bit 语义未全闭环 | 分别采集移动、攻击、待命、策略、回合开始、二次行动样本 |
| P1 | 跨版本定位规则 | 当前地址只适用于本 6.5 未加密样本 | 为单位数组、HP setter、结果循环建立字节特征、调用关系和字段差分三重定位 |
| P2 | 事件派发语义 `0x00450986` | 已确认本轮不是 HP setter | 用击杀、状态、剧情触发样本区分 `ECX=0`、`ECX=0x100` 等事件类型 |

#### 2026-06-11 Hook 注入后续定位清单

以下项目来自测验1续。物理伤害应用点和 HP 写入链已闭环；本节只保留仍不能写成最终 Hook 的地址。

| 优先级 | Hook/主题 | 当前状态 | 下一步验证方式 |
|--------|-----------|----------|----------------|
| P0 | `on_double_attack_check` 连击判定 | 已确认连击后的两段伤害结果；未连击样本确认 `0x00406555 -> 0x004E3000 -> 0x0040655C` 返回 `EAX=0` 时不进入追加攻击；成功连击分支仍未闭环 | 在确认连击样本中追踪 `0x004E3000` 返回值、`0x00406520` 的 `+0x608` 递增和 `+0x84` 第二段结果数组写入点；`0x00406181` 保留为旧版线索 |
| P0 | `on_counter_check` 反击判定 | 已观察反击执行链 `0x0041797E -> 0x004064DA -> 0x00406581`，未确认谓词 | 构造可反击/不可反击/射程不符/击杀无反击样本，定位分支条件 |
| P0 | `on_hit_check` 命中/闪避/格挡判定 | 未命中样本不足 | 分别采集命中、未命中、格挡、被格挡样本，区分“未造成伤害”和“造成 0 伤害” |
| P0 | `on_damage_calc` 伤害公式上游 | `0x00405AD5` 只能读最终伤害；`0x00410F1C` 已在曹操暴击和自动阶段多次命中，但公式内部未解析 | 在 `0x00410F1C` 内部单步，记录攻击、防御、地形、兵种、命中、暴击和随机数参数；对比普通/暴击/未命中/连击样本 |
| P0 | `on_strategy_settle` 策略结算 | `0x004229E8/0x004229F5/0x004229FC` 与 `0x004259AF/0x004259B4` 均为候选 | 用曹操策略样本采集策略伤害、策略命中、策略击杀、策略多目标 |
| P0 | `on_strategy_kill` 策略击杀 | 只能从策略结算新 HP 派生，原生击杀函数未定位 | 以策略击杀单目标样本设置 HP 写入和事件派发断点 |
| P1 | `on_action_end` 普通行动结束 | `0x0044EA34/0x0041C107/0x00442718` 仍需区分驱动、UI 通知、状态写入 | 分别采集攻击、策略、道具、待命后的行动状态位写入 |
| P1 | `on_turn_start` 回合开始 | `0x0044AF8F` 和 `0x00406690` 有强线索，仍需第二回合样本补证 | 在回合切换前布设行动位、二次行动状态、回合计数写断点 |
| P1 | 个人 EXP/武器 EXP/防具 EXP 写函数 | 曹操主动普通攻击样本已确认 `0x00406043/+0x428`、`0x00406054/+0x42C`、`0x00406074/+0x430+slot*4`；旧写点 `0x00406113/0x00406153/0x00406171` 本样本未命中 | 继续采集击杀、反击、连击第二段、策略伤害和升级显示样本，验证是否复用同一写点以及最终角色/装备永久字段写入位置 |
| P1 | 五维功勋写函数 | 旧候选 `0x00492F9C/0x00492C20/0x004B1B1A/0x00496E79` 已全部证伪或改判 | 重新从 UI 功勋变化前后快照、角色永久数据和战后结算路径生成候选；候选必须关联 UI 功勋变化 |
| P1 | 经验字段写入时机 | 已确认攻击开始先清 `+0x428/+0x42C/+0x430`，随后在 `0x0040654A -> 0x00452807 -> 0x00406043/54/74` 写入本次结算缓存；最终落盘/升级显示仍未定位 | 对角色永久经验、装备永久 EXP、升级显示函数和战后结算继续设写断点；区分战斗上下文缓存和持久化字段 |
| P2 | 跨版本签名 | 当前地址仅适用于本 6.5 未加密 EXE | 为 HP setter、伤害应用点、单位数组建立字节特征、调用栈特征和字段差分定位器 |
