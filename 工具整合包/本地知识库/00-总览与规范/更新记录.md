# 99 更新记录

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认写入路径；如需适配，必须另建版本护栏和样本验证。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

#### 2026-06-11

- 测验1实机调试归档：曹操 `unit[0]` 攻击黄巾兵 `unit[77]` 并触发连击。x32dbg live 与 HP 写断点确认战场单位数组 `0x004A7B20`、stride `0x30`、当前 HP 字段 `unit+0x10`，目标 HP 地址 `0x004A89A0` 先后写入 `118->59`、`59->15`。最终 HP 写入链确认为 `0x00405966 -> 0x00405ADC -> 0x0043F6DB -> 0x0043F70C`，其中 `0x0043F70C` 执行 `mov [ecx+0x10], edx`。
- 将本次知识写入 `02-数据结构/单位结构体.md`、`03-机制详解/物理攻击.md`、`03-机制详解/连击.md`、`03-机制详解/战斗公式.md`、`04-函数速查/函数速查表.md`、`05-教程指南/战场操作自动化与调试流程.md` 和 `00-总览与规范/待验证清单.md`。移动函数、伤害公式上游、连击判定函数和 `unit+0x0D` 位语义仍保持待验证，不得从本次 HP 写入闭环外推。
- GameDebug MCP protocol surface expanded to 53 tools with `debug_title_wndproc_dispatch_scan`. The tool is offline/read-only and scans `Ekd5.exe` `.text` for WndProc-style `WM_*` / `VK_*` compare sites. Validation found 78 compare candidates, 73 containing-function candidates, and 30 probe targets. These records are `pending-breakpoint` / `needs-live-run` only; they must not be promoted to a verified title-menu dispatcher ABI until Start/Load/Settings/Exit live route hits include CIP, registers, stack, disassembly, and internal state changes.

#### 2026-06-09

- 建立“人类操作员 + 调试采证员”战场采证流程：新增 `05-教程指南/战场操作自动化与调试流程.md`，记录紫色移动格、红色攻击范围、右侧最上方攻击指令和单动作前后快照协议。
- 新增 `03-机制详解/功勋模式与五维功勋.md`：根据功勋模式说明图和颍川之战实机样本记录五维功勋获取规则；本轮曹操攻击张梁并格挡反击后，UI 显示武力、敏捷、统率功勋各增加 1 点。
- 归档功勋首轮动态证据到 `CCZModStudio_Reports\DebugEvidence\merit-human-operator-20260609-192350`；前后 diff 显示 `char_data_004A3E77` 无变化、曹操 `unit[0]` 行动状态改变、张梁候选 `unit[61]` HP 从 168 降到 122，并列出 `0x00492F9C/0x00496E79/0x004B1B1A` 等待写断点验证的 `+1` 候选地址。
- 新增 `工具整合包\MCP配置\watch-6.5-battle-events.ps1`：常驻只读战场事件监控器，默认用 `ReadProcessMemory` 轮询 `Ekd5.exe` 战术单位数组和关键全局区，自动记录坐标、HP/MP、行动状态和属性档位变化；x32dbg bridge 在线时额外记录 bridge 状态和暂停点证据。该脚本不设置断点、不暂停游戏、不写游戏文件，适合替代高频人工问答采证。
- 修正 `start-x32dbg-6.5.ps1` 默认目标定位：按正式 6.5 未加密 EXE 哈希查找并排除 `CCZModStudio_TestCopies`、导出和备份目录，避免误打开同哈希测试副本。
- 接入 x32dbg MCP 调试链路：下载 x64dbg snapshot `2026.05.27` 到 `CCZModStudio_Exports\DebugTools`，安装 `x64dbg_mcp.dp32` v2.3.0，生成项目级 `.codex/config.toml` 双 MCP 配置，并新增 `capture-6.5-debug-baseline.ps1` 与 `start-x32dbg-6.5.ps1`。
- 对 `基底\加强版6.5未加密版\Ekd5.exe`（SHA256 `84E3A1DC085AE6F9900D1E8C388A9CD6766379832DDF51BC7BDF780C6615B4A3`）生成静态调试基线 `CCZModStudio_Reports\DebugEvidence\debug-baseline-20260609-163804`，复读 17 个函数/Hook 地址原字节和 4 个代码空洞区间。
- 修正代码空洞证据等级：`0x601A9A-0x602500` 在当前 6.5 未加密目标 PE 节表中不可映射，降级为脏数据/跨版本线索；`0x418335`、`0x43C2D5` 静态显示已有跳转，需继续动态判别是否为基底自带或补丁残留。
- x32dbg 动态采证继续推进：`apply-x32dbg-breakpoints.ps1` 已对 17 个计划地址布设软件断点成功；新增 `run-x32dbg-until-planned-hit.ps1`，可继续运行、归一化计划断点地址、导出寄存器/断点/反汇编/调用栈/栈/内存证据，并支持临时禁用高频断点。
- 首个动态命中闭环：`0x00484002 get_char_data_ptr` 在标题/初始化链路命中，证据 `x32dbg-hit-00484002.json` 显示 `ECX=0x13`、`imul ecx, ecx, 0x61`、`add ecx, 0x4A3E77` 和调用栈 `00484002 -> 0041B867 -> 0041B664 -> 00404CAD -> 0042BFB8`；知识库升级为“已命中，语义初步验证”，仍需更多 Data 号场景复核。
- 禁用 `0x00484002` 后在标题画面继续运行未触发其它计划断点，确认下一轮动态采证必须进入战场/攻击/策略/暴击/回合开始等实机场景；`待验证清单.md` 已新增 x32dbg 下一轮触发矩阵。
- 继续动态采证后补充 `0x42518F -> 0x41301E -> 0x413009` fallback 证据链：标题入口“星汉灿烂”进入 R 场景前触发 `ability_check_wrapper`，预检 `0x40B922` 返回失败后走 `dual_channel_check`，随后命中 `get_effect_value`。
- 纠正现场判定：标题入口后的画面是 R 场景/模式选择，不是正式战场；需要与右侧许子将对话并选择游戏模式后再进入后续游戏流程。该阶段采到的 `0x4061F9 get_unit_ptr` 与 `0x40728F get_max_mp` 均标为 R 场景/UI 证据，不能直接当作正式战斗语义。
- `0x4061F9` 动态命中确认公式 `low8(index) * 0x30 + 0x4A7B20`，但本次 `ECX=0xFFFF` 属于哨兵/探针；`0x40728F` 动态命中确认最大 MP 计算读取 `ecx+0x1F`，但仍需正式战斗或人物面板复核。
- GameDebug MCP 继续推进无鼠标/无截屏启动路径：协议面扩展到 33 个工具，新增 `debug_r00_mode_route_analyze` 与 `game_rscene_text_anchor_scan`。前者只读解析 `RS/R_00.eex`，确认许子将前置 `0x2D 武将点击测试 person=157`、模式选择框 `0x002E97`、常规模式配置选择框 `0x003273` 和候选键序列 `enter,down,down,down,down,down,enter`；后者只读扫描进程内 GBK 文本锚点作为 R 场景进度信号。验证输出 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=33`、`GAME_DEBUG_MCP_RSCENE_ANCHOR_SCAN_OK`、`GAME_DEBUG_MCP_R00_ROUTE_OK`。当前仍未完成无鼠标触发 person 157 actor-click 子事件，也未动态验证攻击前/攻击后/回合结束函数。
- GameDebug MCP 协议面扩展到 35 个工具，新增 `game_rscene_script_window_scan` 与 `debug_rscene_command_handler_scan`。前者只读抽取 `RS/R_00.eex` 中许子将 `0x2D`、模式选择 `0x12`、常规配置 `0x12` 周边字节窗口，并在运行时扫描 `Ekd5.exe` 可读内存及可选指针引用；后者离线扫描 `Ekd5.exe` `.text` 中 `0x2D/0x12/0x07/0x13` 命令号比较模式并生成 `rscene-command-handler-probe-plan.json/md`。这两项只提供 R 场景解释器/handler 动态探针计划，不证明 actor-click 已被无鼠标触发，也不提升攻击/回合函数语义。
- GameDebug MCP 协议面扩展到 36 个工具，新增 `debug_r00_startup_route_probe_run`。该入口把 R_00 路线分析、脚本窗口扫描、handler 静态候选、可选 x32dbg 探针和可选 keyboard-only 触发整合为一次可审计启动路线实验；安全验证模式 `start_debugger=false, run_probes=false, allow_input=false` 只写计划和 summary，不启动调试器、不发送输入、不截图、不写进程内存。当前仍未完成无鼠标触发 `person=157` actor-click 或正式进入 `battle_loaded`，攻击/回合函数动态语义仍保持待验证。
- `debug_r00_startup_route_probe_run` 增加 `probe_before_startup_continue=true`，实机无输入探针可先布设 R 场景 handler 断点，再续跑 x32dbg；该改动用于避免先 `continue_startup` 导致早期解释器命中丢失，安全边界仍是不发键鼠、不截图、不写进程内存、不改游戏文件。
- GameDebug MCP 协议面扩展到 37 个工具，新增 `debug_r00_actor_route_analyze`。该工具确认许子将 person `157` 的 R_00 actor 路线和 `0x2D` 点击测试偏移 `0x002DF5`，并把最新 no-input startup probe 负向结果写入证据。实机无输入探针 `game-auto-20260610-212925-698` 可自主启动 x32dbg/Ekd5、先布 16 个 R 场景 handler 断点、再续跑，但未命中，最终 `process_no_battle_signal` 且 `script-windows-not-found`；下一轮应查标题入口/R 场景加载/解释器脚本指针，而不是继续盲发按键或解释攻击/回合候选。
- GameDebug MCP 协议面扩展到 38 个工具，新增并增强 `debug_rscene_load_transition_scan`。该工具离线扫描 `Ekd5.exe` 内 `RS\R_%s.EEX` / `RS\S_%s.EEX` 路径锚点、`.code` 引用、包含锚点的函数入口和选定导入 API 调用点，并可附带运行时只读 R_00 文本/脚本窗口扫描；验证输出 `GAME_DEBUG_MCP_RSCENE_LOAD_TRANSITION_OK status=transition-candidates-found anchors=4 candidates=2 targets=20`。当前候选包括直接引用 `0x004164C7` / `0x00417F16`、函数入口 `0x0041648E` / `0x00417EEA` 和若干 `GetFileSize`/`CloseHandle`/`wsprintfA` 调用点。实机无输入复测仅命中 `0x00406A31 CloseHandle`，但无 R/S 路径或 R_00 script-window 证据；函数入口候选 45 秒内未命中。上述均仍是标题到 R 场景加载的 probe 候选，不得提升为 R_00 loader/interpreter 已验证语义。
- GameDebug MCP 协议面扩展到 39 个工具，新增 `debug_keyboard_exploration_run`。该工具是受控 keyboard-only 启动探索编排：可选启动已校验 SHA256 的 `Ekd5.exe`、可选启动/续跑 x32dbg，按分号分隔尝试候选键序列，并在每个序列后只读运行 `game_runtime_state_classify`、`game_rscene_text_anchor_scan`、`game_rscene_script_window_scan` 和 `game_battle_state_match`；一旦观察到 R_00 文本/脚本驻留、`battle_loaded` 或颍川 profile 即停止。验证输出 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=39` 与 `GAME_DEBUG_MCP_KEYBOARD_EXPLORATION_OK status=plan-ready stop=dry-run-input-disabled`；安全验证模式 `allow_launch=false, allow_input=false` 不启动游戏、不发输入、不截图、不写进程内存、不改游戏文件。该工具提升了无鼠标启动探索的编排能力，但仍未证明已无鼠标触发 `person=157` actor-click 或进入正式 `battle_loaded`。
- GameDebug MCP 修复 R 场景默认文本锚点路径：`game_rscene_text_anchor_scan(anchors="")` 现在统一走内置 Unicode 默认值，不再在内部 keyboard exploration 使用乱码锚点；`game_runtime_anchor_sweep` 为每个命中输出 `region_kind` 并收紧阶段推断，单独 `许子将`/人物名堆命中不再升级为 R_00 runtime。验证输出 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=40`。实机只读复测可自主启动校验 SHA256 的 `Ekd5.exe`，`game_rscene_text_anchor_scan` 在 `heap_candidate` 找到 `许子将`，但 `game_rscene_script_window_scan` 仍为 `script-windows-not-found`、runtime 为 `process_no_battle_signal`；该结果仅证明资源驻留，不证明模式选择、actor-click、战场进入或攻击/回合函数语义。
- 标题开始界面右侧菜单完成用户校准：画面从上到下实际功能为开始游戏、读取存档、环境设置、退出游戏。`game_click_ui` 新增语义键 `title_start_game`、`title_load_game`、`title_settings`、`title_exit`，并把 `menu_top_1..menu_top_4` 改为对应兼容别名；实机坐标修正为 `640x440` 客户区基准并按当前客户区缩放，避免把 `961x599` 截图像素误当游戏客户区坐标；真实输入仍受 `allow_input=true` 安全门控，且越界点会被阻断。
- GameDebug MCP 协议面扩展到 46 个工具，新增 `debug_full_auto_run`、`debug_runtime_invoke_plan`、`debug_runtime_invoke_run`、`debug_menu_route_run`、`debug_address_verify_run`、`debug_knowledge_promote`。安全验证模式通过，输出 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=46`，并确认新增工具可生成 `runtime-invoke-plan.json`、`menu-route-summary.json`、`address-verify-summary.json`、`knowledge-promotions.json` 与 `full-auto-summary.json`。当前实现把调试态内部调用、临时注入和持久补丁转换纳入可审计计划/门控；默认不启动游戏、不注入、不跑断点、不写正式知识库。固定读档样本缺失时记录 `blocked: missing-save-fixture`，攻击/回合地址仍需 live 断点命中、触发动作、寄存器/栈/反汇编和战场 diff 后才能推广为正式知识。
- GameDebug MCP 证据结构补齐：`debug_full_auto_run`、`debug_runtime_invoke_run`、`debug_menu_route_run`、`debug_address_verify_run` 写入 `action-chain.jsonl`，`debug_address_verify_run` 与 live probe hit 写入 `probe-hits.jsonl`。验证脚本已断言这些文件存在，最新 dry-run 验证通过 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=46`。该结果只证明协议和证据链平台可用；无鼠标/无截屏地触发许子将 actor-click、进入 `battle_loaded`、完成曹操攻击张梁并动态确认攻击/回合函数地址仍未完成。
- GameDebug MCP 暂停态临时调用基础设施补齐：`RuntimeInvokeAction` 新增 `invoke_strategy`、`calling_convention`、`register_setup`、`stack_arguments` 和 `requires_paused_debuggee`；`debug_runtime_invoke_run` 现在只有在本机 bridge 在线、debuggee 已暂停、`allow_debug_invoke=true`、`allow_runtime_injection=true`、并且候选为有效 `Ekd5.exe` VA 时，才会执行 `debugger_stub_call`。执行器会生成 `alloc/asm/eip/run` stub、在 `int3` 后采证并尝试恢复/释放；R_00 脚本偏移继续保持 `needs_rscene_handler_entry`，不会误调用。验证脚本已确认新增计划字段存在且 safe validation 不会发出 runtime stub。
- GameDebug MCP 协议面扩展到 47 个工具，新增 `debug_r00_runtime_invoke_candidate_plan`，并把 `debug_runtime_invoke_plan` 的 R_00 路线从脚本偏移占位升级为 command-handler 候选计划。安全验证通过 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=47`；常规 R_00 路线输出 16 个 handler 候选，示例 `0x00408AFF` 对应 `0x2D` actor-click compare、`0x00414EB3` 对应 `0x12` choice-box compare。该工具现在同时写出 `r00-runtime-handler-probe-plan.json/md`，可直接作为 `debug_internal_probe_run` 的 live 输入。当前 `latest_verified_handler_hit_count=0`，因此这些 action 仍是断点候选，`requires_runtime_injection=false`，不能作为已验证内部调用或攻击/回合函数语义。
- GameDebug MCP 协议面扩展到 48 个工具，新增 `debug_r00_runtime_handler_probe_run`，把 R_00 handler candidate plan 与 `debug_internal_probe_run` 串成独立 live-ready 入口。安全验证通过 `GAME_DEBUG_MCP_VALIDATE_OK ... tools=48`，新增输出 `GAME_DEBUG_MCP_R00_RUNTIME_HANDLER_PROBE_OK status=plan-ready candidates=16 targets=16 runProbes=False hits=0`；`debug_full_auto_run` 现在也包含 R_00 handler candidate 子步骤，safe mode 走轻量计划，live mode 才运行完整 probe wrapper。该改动仍未证明 actor-click、模式选择、battle_loaded、曹操攻击张梁或 attack/turn 函数语义，所有候选继续保持待 live 命中审核。
- 新增 R_00 handler wrapper 后完成一次 no-input live 复测：`debug_r00_runtime_handler_probe_run(start_debugger=true, continue_startup=true, run_probes=true, probe_before_startup_continue=true, max_hits=1, timeout_ms=15000)` 生成证据 `CCZModStudio_Reports\DebugEvidence\game-auto-20260611-063656-949`，结果 `probe-timeout-no-hit`、`hit_count=0`、`process_no_battle_signal`、`script-windows-not-found`、`process-running-no-profile`。该负向证据已同步到 MCP 接入页和战场自动化流程页，确认下一步仍应优先定位标题入口到 R_00 script-window/解释器状态，而不是升级 attack/turn 候选。
- GameDebug MCP 协议面扩展到 49 个工具，新增 `debug_rscene_load_transition_probe_run`，并给 R 场景加载转换计划增加 `candidate_filter`。验证链使用 `anchor_functions,direct_refs,no_imports`，聚焦 `0x0041648E`、`0x00417EEA`、`0x004164C7`、`0x00417F16` 等 R/S anchor 函数和直接引用，默认排除 `CloseHandle` 等泛 API 断点。实机 no-input 复测 `CCZModStudio_Reports\DebugEvidence\game-auto-20260611-064827-570` 只命中 `0x00406A31 CloseHandle`，且无 R_00 script-window、路径/缓冲区、战场 profile 证据；该命中记录为 weak negative，不得提升为 R_00 loader/interpreter、actor-click、battle_loaded 或 attack/turn 函数语义。
- Focused transition no-input 复测 `CCZModStudio_Reports\DebugEvidence\game-auto-20260611-070334-771` 只探测 `0x0041648E`、`0x004164C7`、`0x00417EEA`、`0x00417F16`，结果 `probe-timeout-no-hit`、`hit_count=0`、`script-windows-not-found`、最终 `process_no_battle_signal`。该负向证据将缺口收敛到标题菜单派发/内部选择。
- GameDebug MCP 协议面扩展到 50 个工具，新增 `debug_title_menu_dispatch_scan`。静态扫描证据 `CCZModStudio_Reports\DebugEvidence\game-auto-20260611-072611-778` 生成 11 个标题菜单派发候选：`0x00401491`、`0x004014BE`、`0x00401126`、`0x004011D1`、`0x004011F6`、`0x00401854`、`0x00401883`、`0x00401E5D`、`0x00401EBE`、`0x0040582F`、`0x004058BA`。`debug_runtime_invoke_plan(stage="menu", route="full_menu")` 现在输出 `title_menu_dispatch_breakpoint_probe`，保持 `requires_runtime_injection=false`、`writes_process_memory=false`；这些地址仍是 pending/needs-live-run，不得提升为已验证菜单、R_00、battle_loaded、attack 或 turn 函数。
- GameDebug MCP 协议面扩展到 51 个工具，新增 `debug_title_menu_dispatch_probe_run`。该入口把标题 dispatch 静态扫描、探针计划、可选 x32dbg 启动/续跑、可选 keyboard-only trigger 和前后只读状态扫描串成 `title-menu-dispatch-probe-summary.json/md` 与 `action-chain.jsonl`；safe validation 默认不启动游戏/调试器、不下断点、不发输入。它为后续标题四项路线差异 live 命中提供固定入口，但尚未证明菜单 ABI、R_00 actor-click、battle_loaded、曹操攻击张梁或 attack/turn 函数。
- GameDebug MCP 协议面扩展到 52 个工具，新增 `debug_title_menu_dispatch_matrix_run`。该入口为 Start/Load/Settings/Exit 四路线生成共享 dispatch 扫描和每路线 probe summary，统一写 `title-menu-dispatch-matrix-summary.json/md` 与 `action-chain.jsonl`；safe validation 默认不启动、不下断点、不发输入，且真实输入时 `title_exit` 默认需要额外 `allow_exit_route=true`。当前仍是路线对比编排能力，不等同于菜单函数 ABI 或 R_00/战斗语义已验证。
- 整理 `D:\Downloads` 中 17 个 6.5 注入补丁文本，新增 `03-机制详解/6.5特效注入索引.md`，记录 `.1.0.6.5` 注入格式、内联桩参数顺序、Hook 地址、代码区、可改参数地址、关键变量和冲突关系。
- 同步 `README.md`、`01-核心引擎/桩函数.md`、`01-核心引擎/代码空洞与安全补丁.md`、`02-数据结构/全局变量.md`、`04-函数速查/函数速查表.md`，补入盛气凌人、噬心毒咒、大杀四方、多多益善、回MP攻击、殭屍大法、策略冲锋、聚势伐谋、敌潮逆噬、策略偷钱、强化攻击穿透、策略保底/限伤、无视策略减伤、信息传送 29 和护卫的索引口径。
- 修正 `03-机制详解/策略附带随机状态.md` 到 v3 样本口径：支持多目标遍历、使用 `edi` 保存特效值、概率比较使用 `jae`，并记录与策略偷钱的链式回跳。
- 新增待验证项：护卫与殭屍大法原样代码区重叠、两个回MP攻击变体二选一、策略类补丁叠加顺序、整型变量 `4003 -> 0x505E8C` 运行值同步和大杀四方与二次行动状态数组共用风险。

#### 2026-06-08

- 静态对照 `老版游戏制作工具\剧本编辑器v0.24`：确认其只有原生 MFC exe 和配置、无源码；PE 资源相对 v0.23 增加菜单/对话框/字符串表，主菜单新增 JSON 导出和 HTTP server 入口。
- 从 v0.24 `说明.txt`、PE 字符串和导入表确认 R 场景预览行为：加载 `E5\Mmap.e5` 与 `Pmapobj.e5`，`Ctrl` 为吸附模式，滚轮改朝向，`Shift+滚轮` 改动作；当前整合包滚轮用于缩放，是后续 R 场景交互优化点。
- 新增 `08-剧本与战场/剧本编辑器v0.24静态对照.md`，并同步工具索引、来源映射和待验证清单；v0.24 静态取证只作为 界面/预览和字典补充，`.eex` 写回规则仍以 v0.23 源码、样本复读和烟测为准。
- 清理知识库迁移脏数据：移除 Markdown 中的 `$sourceRel` 模板残留、NUL 字符、错误 `待证清单.md` 路径和部分旧平铺文件名引用；统一改为 `待验证项` / `00-总览与规范/待验证清单.md` 口径。
- 联网复核 6.5 发布贴、6.4 系统笔记、6.6 修正版发布贴、工具集补档、教程导航和 Meff.e5 编辑器资料；外部页面标题和关键字均可访问确认。
- 补强引擎知识：`01-核心引擎/引擎深入分析.md`、`02-数据结构/全局变量.md`、`09-版本与外部资料/6X版本差异与适配.md`、`09-版本与外部资料/联网深度专题.md` 新增 6.x 整型变量、`4017/4018/4020/4021/4023/4031/4038/4080`、`DT.e5`、R 文字和跨版本资源边界；全部按外部佐证/待验证处理，不开放新写回。
- `00-总览与规范/待验证清单.md` 新增 6.x 整型变量验证矩阵和 `4031` Data 号/战场编号上下文验证任务；根 README 增加 6.x 引擎变量导航。
- 全局设定撤回此前对联网地址线索和本地机器码上下文的数字项解释；等级阈值、升级经验、装备经验、功勋、装备等级上限等旧窗口左侧全局数值项回调为不可读取、不可写入。
- 全局参数页恢复为只读证据状态展示；当前安全写回仍仅限 `6.5-3 兵种系`、`6.5-4 详细兵种` 和 `Ekd5.exe@0x8D3C4` 游戏标题。
- `--global-settings-write-smoke` 恢复为只在测试副本中验证兵种名、职业名和游戏标题保存、备份、结构化报告和复读一致。
- 知识库同步更新 `07-数据表与资源/人物形象-RS-形象指定器.md` 与 `09-版本与外部资料/联网深度专题.md`，记录数字项撤回边界。
- 扩大联网检索范围：新增复核 Hexzmap/地形、S 形象三文件、Meff/Mcall/LS、S 剧本教程、6.5p/6.5pl 分支和旧 EXE 变量资料；页面均可访问并命中相关关键词。
- 知识库同步更新 `09-版本与外部资料/联网资料索引-曹操传模组6.5.md`、`09-版本与外部资料/联网深度专题.md`、`07-数据表与资源/地形编辑.md`、`07-数据表与资源/图片处理-E5资源.md`、`08-剧本与战场/全剧本指令详解2023归纳.md`、`09-版本与外部资料/6X版本差异与适配.md` 和 `00-总览与规范/待验证清单.md`。
- 新增外部资料全部按“外部佐证/跨版本/待验证”处理：没有本地样本、旧工具源码、复读、烟测或实机验证前，不开放 Hexzmap 写入、S 形象批量替换、Meff/Mcall 重封包、6.5p/6.5pl 变量或命令模板写回。
- 继续学习 R/S 剧本教程：复核 `曹操传mod之s剧本完全教程——基础篇` 1-3 页、`如何做MOD`、97 经验技巧帖和 Star175 5.6 教程 R/S 章节；新增 R/S 命名、场景/段落 分层、S 剧本三段式、R/S 跳转、子事件承接、即时判定、`0x76` 跳转和修改后复读的只读知识结论。
- 同步更新 `08-剧本与战场/剧本与战场制作.md`、`08-剧本与战场/全剧本指令详解2023归纳.md`、`09-版本与外部资料/联网资料索引-曹操传模组6.5.md`、`09-版本与外部资料/联网深度专题.md` 和 `00-总览与规范/待验证清单.md`；所有新增 R/S 教程内容继续按“外部教程口径/待验证”处理，不开放结构自动写回。

#### 2026-06-07

- MCP 新增 人工智能 绘图素材能力：`list_ccz_image_asset_presets`、`build_ccz_image_prompt`、`prepare_ccz_generated_image`、`draw_ccz_image_asset`，按 图像工作室 兼容上游无头生成 R 背景、动态链接库图标、头像、R 形象和 S 形象。
- 人工智能 绘图流程只写 `CCZModStudio_Exports\AiImageAssets` 并调用现有 E5/动态链接库 替换预览；不直接写入游戏资源。上游配置只读 `IMAGE_STUDIO_*` 环境变量。
- 新增知识库专题 `07-数据表与资源/人工智能绘图-图像工作室-模型上下文协议.md`，记录 OpenAI Images/Responses 接口 请求形态、五类素材尺寸、提示词模板、编号映射、后处理规则、v1 边界和来源等级。
- 新增 `--ai-image-asset-smoke`，离线验证 试运行、Face/R/S 映射、背景/头像/动态链接库/R/S 后处理尺寸、替换预览和不修改 `Unit_mov.e5`。
- 图片处理页新增 E5 批量导入、批量删除/清空按钮；批量导入可按选中行顺序或文件名数字匹配图号，批量清空写入 1x1 透明 PNG 预留，不重排索引。
- `E5ImageReplaceService` 新增批量预览/写入，写前备份，写后逐条复读索引和条目字节，生成结构化报告。
- 新增 `E5ImageRenderService`，统一标准图、RAW 帧条和未知字节图候选预览；兵种策略动画预览现在能显示 `Meff.e5` 的 LS/非标准条目候选图。
- 新增 `IconResourceReplaceService`，支持 `Itemicon.dll` / `Mgcicon.dll` / `Cmdicon.dll` 按字段编号替换/清空 `RT_BITMAP` 位图资源；写入使用 Win32 `BeginUpdateResourceW` / `UpdateResourceW` / `EndUpdateResourceW`。
- 本地知识库更新 `07-数据表与资源/动态链接库图标资源.md`、`09-版本与外部资料/联网资料索引-曹操传模组6.5.md`、`07-数据表与资源/图片处理-E5资源.md`，记录 动态链接库图标写回与标准 `RT_GROUP_ICON`/`RT_ICON` 的区别，以及 6.6 图标资源迁移边界。
- 验证：`dotnet build .\工具整合包\CCZModStudio.sln` 通过；`--e5-image-replace-smoke` 通过，输出 `E5_IMAGE_BATCH_AND_DLL_ICON_SMOKE OK batch=2 clear=1 strategyPreview=160x160 iconIds=100,101`。

#### 2026-06-06

- R 场景制作读旧版源码确认 `0x27` 背景图号、`0x30` 武将出现和 `0x32` 武将移动槽位；R 场景页现在只把 `0x30` 自动预加载为画布角色，右键进入编辑态后可拖拽同步 X/Y 到内存中的 R 剧本命令，双击角色定位左侧对应指令，点击 `完整保存R剧本` 后才落盘。
- `--rs-write-smoke` 新增 `RSCENE_POSITION_WRITE_SMOKE_OK`，在测试副本中修改 `R_00.eex` 的 `0x30` 坐标槽并通过完整保存、备份、报告和复读校验。
- 剧本制作单条指令修改入口对齐旧版 ItemData 链路：`eex -> LegacyScenarioDocument -> LegacyScenarioItemData -> 旧式 Dialog -> IntData/LongCharData -> LegacyScenarioWriter 按 code_instruct 重编码保存`。
- 新增旧版 MFC 对话框 资源加载与统一 WinForms 渲染：解析旧 `cczEditor2.rc` 的 71 个 `DIALOGEX`、控件文案、`DLGINIT` 下拉项、`CBS_SORT` 样式，并抽取 `Dialog_114` 的 34 个模板文本。
- 新增 `LegacyMfcDialogCatalog`，为旧 `OnEditModify()` 覆盖的 71 个对话框建立显式行为目录；`LegacyCommandEditDispatcher` 与旧源码 101 个命令号分派对齐。
- 修正旧式对话框 细节：`Dialog_2` OK 写回 `LongCharData`，`Dialog_78.IDC_COMBO9` 按旧源码直接存列表索引，`Dialog_89` 负等级初始化归零并保留旧物品 sentinel 规则，`Dialog_6/Dialog_70` 列表高度按旧 自绘 口径处理。
- 新增 `--legacy-mfc-dialog-smoke`：从旧源码解析分派表，校验 71 个资源、71 个显式行为、101 个命令映射、34 个模板，并逐个初始化/提交旧式对话框。
- 验证更新：主项目构建和 SmokeTests 构建均通过；`--legacy-mfc-dialog-smoke` 输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 sortedCombos=Dialog_33.IDC_COMBO1`；`--legacy-script-edit-smoke` 输出 `LEGACY_SCRIPT_EDIT_SMOKE_OK ... count=8528->8529->8528`。
- 旧版剧本编辑器界面和对话框修改入口继续统一：剧本制作、战场制作 S 剧本、R 场景制作 R 剧本共用旧式可视树 Presenter，读取成功时只显示 `文件路径 -> Scene -> Section -> 真实指令节点`；战场和 R 场景双击命令也进入旧式对话框，OK 后完整保存分别走 `LegacyScenarioWriter`。
- 剧本制作右侧收敛为大图片预览、内嵌旧式对话框 参数区和项目侧 `备注`；移除对象预览、详情大块、批量修改按钮，并暂时隐藏新增、删除、复制、移动、粘贴预览等结构编辑入口。

#### 2026-06-05

- 6.X 适配：新增 `09-版本与外部资料/6X版本差异与适配.md`，整理 6.3/6.4、6.5、6.6 已复核差异与实现策略；代码侧改为 6.X HexTable 语义读表、6.5 写入护栏。按当前需求，6.6/其它 6.X 缺表时暂时仍允许内置 6.5 表只读兜底，后续再取消。
- 6.6x 形象指定器本地解析：读取 `B形象指定器\6.6xEXE\Ekd5 6.6.exe`、`6.6x形象指定器\System.ini` 和 `形象指定器66x.exe` 元数据；确认 R/S 指定表偏移仍为 `RFileHead=E1000`、`FileHead=D2800`，`SCount=368`，并归档 6.6x 策略扩展地址键。
- 知识库二次清洗：重写 `00-总览与规范/待验证清单.md`，移除已被旧版完整树 读取器/写入器、E5 `0x110` 索引、B 形象指定器偏移、MCP 能力和 6.5 发布贴证实的过期待补项；当前只保留会影响写入边界、实机验证或发布流程的真实缺口。
- 联网资料补强：`09-版本与外部资料/联网资料索引-曹操传模组6.5.md` 与 `08-剧本与战场/Tr-e5-R插图.md` 补入 6.5 发布贴对 `信息传送28`、`Tr.e5`、R 插图槽位、0 图号取消、宽度和面积上限的口径；仍把透明/遮挡/新增 `Tr.e5` 和实机图号映射列为待证。
- 知识库脏数据清理：`01_格式与文件后缀.md` 与 `08-剧本与战场/剧本与战场制作.md` 去掉“R/S eex 只能短写回”和“写入必须测试副本前置”的旧说法，改为兼容短写回、旧版完整树写回、正式写入护栏和测试副本隔离验证四类边界。
- 取消已确认写入功能的“必须测试副本”前置限制：补丁应用、资源整文件替换/还原、R/S 文本原地短写回均可直接作用于当前项目；保留路径越界、6.5 核心尺寸、GBK 容量、扩展名/格式检查、未知结构重封包和完整命令树写回等真实边界。恢复操作改为使用保存前备份文件手动处理。
- 工具链说明、资源说明和 MCP README 已同步为“当前项目可写；测试目录仅用于隔离验证”的口径。
- 主界面启动尺寸改为按当前显示器工作区自适应计算，保留 1280x820 作为大屏优先尺寸，但会在小屏/高缩放环境下自动收进可见区域；最小尺寸也会按屏幕裁剪，避免窗口超出任务栏或屏幕边界。
- 历史 `ui-layout.json` 中保存的窗口位置/尺寸恢复时会重新按当前显示器裁剪，解决跨电脑、跨分辨率或多显示器切换后窗口过大、偏移、不可见的问题；分栏比例保存逻辑继续保留。
- 项目声明 WinForms `PerMonitorV2` 高 DPI 模式，主窗体和自建大弹窗启用 DPI 自动缩放；宝物特效、E5 图片条目选择、资源替换预览等弹窗改为按父窗口所在屏幕自适应尺寸。
- 多个创作页顶层工具栏允许换行，窄屏下按钮会自动下折，避免操作按钮被挤出可视范围。
- 验证更新：`dotnet build .\CCZModStudio.sln` 通过，0 警告，0 错误；默认 `CCZModStudio.SmokeTests` 通过，输出 `RS_SMOKE OK`。

#### 2026-06-04

- 角色暴击/撤退台词映射纠偏：新增 `RoleQuoteMappingService`，撤退台词按人物 ID 0..48 对齐 `6.5-0-3 撤退台词`；暴击台词先查 `Ekd5.exe @ 0x89C30` 的 21 组特殊人物表，未命中时按 `21 + 暴击台词类型 * 3` 取三条普通候选。
- `角色设定` 右侧文本区改为显示真实会用到的台词行：特殊暴击单行编辑，普通暴击三条候选空行分隔并拆回保存；撤退台词不再用人物表字段值跳转。
- 数据表跨表解释/诊断同步去除 `暴击台词/撤退台词` 的直接单表行引用，避免继续误导到错误文本行；字段说明改为解释真实引擎规则。
- 知识库补充联网资料：`07-数据表与资源/角色设定.md` 与 `09-版本与外部资料/联网资料索引-曹操传模组6.5.md` 已记录轩辕春秋教程/问答线索、本地 6.5 `Ekd5.exe @ 0x89C30` 特殊人物表和样例验证。
- 验证更新：`dotnet build .\CCZModStudio.sln` 通过，0 警告，0 错误；默认烟测 `RS_SMOKE OK`；全量只读烟测已执行到 `ROLE_QUOTE_MAPPING retreat0=#0 critical0=#0 critical1=#1 caoren=24/25/26 liubei=20` 并通过新增断言，后续失败在既有 `SV\SV004.E5S` 路径缺失，非本次台词映射问题。
- `形象设定` 主入口改为 `图片处理`，并拆成 `图片资源` 与 `人物R/S指定` 两个子页；旧入口文本仍兼容跳转到新模块。
- 新增通用图片资源目录：`ImageResourceCatalogService` 可汇总 `Face.e5 / Pmapobj.e5 / Unit_*.e5 / Hitarea.e5 / Effarea.e5 / Logo.e5 / Mmap.e5 / U_select.e5 / Gate.e5 / Weather.e5 / Tr.e5` 等非战场地图图片资源，按 E5 `0x110` 索引表读取条目、预览、定位、导出 CSV，并对可写 E5 执行单条替换/还原。
- 图片处理边界已写入 `07-数据表与资源/图片处理-E5资源.md`：战场地图底图仍归 `地图制作`；`Data.e5 / Imsg.e5 / Star.e5 / Hexzmap.e5` 不作为图片资源替换；`Mark.e5` 当前样本不是 `0x110` 索引封包，只定位不替换；`Itemicon.dll / Mgcicon.dll / Cmdicon.dll` 当前只读预览，不做 PE 资源表写回。
- 文件名别名校正：用户清单中的 `efffare.e5` 对应当前样本 `Effarea.e5`，`gete.e5` 对应 `Gate.e5`，`warther.e5` 对应 `Weather.e5`；当前样本未发现 `fb.e5`，后续若项目提供可读 `0x110` 索引 E5，图片处理页会按其它图片资源自动发现。
- 验证更新：`dotnet build .\CCZModStudio.sln` 通过，0 警告，0 错误；`--e5-image-replace-smoke` 新增图片资源目录烟测并通过，输出 `IMAGE_RESOURCE_CATALOG files=18 face=293 markIndex=False hit=33 eff=12` 与 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`。
- `兵种设定/详细兵种` 右侧新增范围预览：点击 `攻击范围` 时读取 `E5\Hitarea.e5`，点击 `穿透` 时读取 `E5\Effarea.e5`，均按字段值 + 1 映射到 0x110 图片索引表图号。
- 本地样本验证：`Hitarea.e5` 可读取 33 张 64x64 PNG，`Effarea.e5` 可读取 12 张 64x64 PNG；当前工具只读预览，不写范围图片资源。
- `07-数据表与资源/兵种设定.md` 已补充攻击距离/穿透资料、旧资料口径和实机验证边界。
- 项目侧制作留痕入口已从 MCP 与主界面移除；当前只保留各制作页内必要的核对记录或草稿文件，不提供独立留痕工具。
- 核对记录和草稿只用于制作辅助，不修改游戏文件。
- 验证更新：`dotnet build .\CCZModStudio.sln -v:minimal` 通过，0 警告，0 错误；临时 SDK client 连接本地 MCP Server，`tools/list` 返回 32 个 tools，并完成 `upsert_creator_note`、`list_creator_notes`、`export_creator_notes_csv`、`delete_creator_note` 制作留痕闭环烟测。
- MCP 补齐 Hexzmap 地形块只读入口：新增 `list_hexzmap_blocks`、`read_hexzmap_block`，可无头列出 `Hexzmap.e5` 地形块、目录偏移、地图尺寸、主地形/高频地形、可写状态，并读取单个 `Mxxx` 的地形格矩阵。
- Hexzmap MCP 写入边界不变：新增读取工具只用于先定位和核对坐标，`write_hexzmap_block` 仍执行版本护栏、地图格数匹配、备份、复读和结构化报告；当前 `44840` 与护栏 `45254` 的样本尺寸差异不会被 MCP 绕过。
- MCP 保留地图资源读取入口；全项目资源索引和独立资源检查入口已移除，资源格式核对改由对应制作页预览、读取和保存复读承担。
- `replace_resource` 的核心文件禁写边界不变。
- 验证更新：`dotnet build .\CCZModStudio.sln -v:minimal` 通过，0 警告，0 错误；临时 SDK client 连接本地 MCP Server，`tools/list` 返回 32 个 tools，`list_hexzmap_blocks keyword=M000 limit=3`、`read_hexzmap_block map_id=M000 max_rows=2`、`list_project_resources category=地图图片 keyword=M000 limit=3`、`run_resource_diagnostics severity=Warn,Error limit=5` 和制作留痕四工具均可返回。
- MCP 扩展 E5 图片资源能力：新增 `list_e5_image_entries`、`preview_e5_image_replace`、`replace_e5_image_entry`，用于无头读取、预览和替换 `Face.e5 / Pmapobj.e5 / Unit_*.e5` 等 E5 图片资源的 `0x110` 单条索引项。
- `E5ImageReplaceService` 增加从备份 E5 指定图号抽取来源条目的重载，便于 MCP/自动化流程还原或跨 E5 搬运同类图片条目。
- MCP E5 写入仍拒绝 `Data.e5`、`Imsg.e5`、`Star.e5`、`Hexzmap.e5` 等核心文件；写入沿用备份、复读和结构化报告，不绕过项目根目录护栏。
- 验证：`CCZModStudio.McpServer` 与 `CCZModStudio.SmokeTests` 均 `dotnet build` 通过，0 警告，0 错误；`--e5-image-replace-smoke` 通过；临时 SDK client 连接本地 MCP Server，E5 与安全制作闭环阶段 `tools/list` 返回 18 个 tools，`list_e5_image_entries Unit_mov.e5 limit=3` 返回 `totalEntries=556`。
- 联网复核 6.5 发布贴、模组 资源区资料索引和 6.6 发布贴，新增 `09-版本与外部资料/联网资料索引-曹操传模组6.5.md`；明确 6.5 的 R 插图/`Tr.e5`、剧本整数变量、全剧本指令资料仍需转为本地样本和命令模板，6.6 新增规则不得混入 6.5 默认写入路径。
- MCP 安全制作闭环已收敛：不再提供项目审查、目录对比或成品目录生成工具；写入流程继续依赖服务层备份、复读和结构化报告。
- 隔离验证目录由用户按需要手动准备，不作为 MCP 独立功能维护。
- MCP 知识检索与剧本指令模板补齐：新增 `search_knowledge_entries`、`list_scenario_command_templates`、`read_scenario_command_template`，用于无头检索本地知识库、按分类/关键字查 R/S eex 命令模板、读取单条指令参数槽详情。
- 剧本指令模板 MCP 只读暴露“参数槽候选、创作者提示和风险边界”，不证明完整命令长度，不作为 R/S eex 命令树写回依据。
- MCP 补齐 R/S 剧本结构只读入口：新增 `list_scenario_files`、`read_scenario_commands`、`search_scenario_scripts`，可无头列出 R/S eex、读取旧版完整树命令摘要/参数、按命令或文本关键字定位剧本内容。
- R/S 剧本结构 MCP 工具只读复用 `LegacyScenarioReader`，目标路径必须位于项目 `RS` 目录且匹配 `R_*.eex / S_*.eex`；命令结构写回仍不通过 MCP 开放。
- `08-剧本与战场/全剧本指令详解2023归纳.md` 从前三关摘要扩展为结构化指令索引：已记录 docx 解析状态、0x00-0x79 常用指令族、R/S 常用规则、特殊整型变量、`58` 障碍区域操作和 `72` 信息传送子功能；高风险命令仍标记只读核对。
- `09-版本与外部资料/联网资料索引-曹操传模组6.5.md` 补充与 `19` 号指令归纳的对照：`信息传送26/28`、`Tr.e5`、剧本整型变量和 6.6 资料边界均已标注为只读研究/待验证项。
- 新增 `08-剧本与战场/Tr-e5-R插图.md`：从旧剧本编辑器源码确认 `信息传送28` 的 R 插图参数说明，并用本地 `Tr.e5` 样本验证 E5 `0x110` 索引结构、24 个图片条目、JPG/BMP/PNG 混合载荷和尺寸列表。
- 知识库轻量自检：扫描常见编码异常标记、重复 Markdown 标题和过期“当前 18 个 tools”表述，未发现需要立即清理的编码异常/重复标题；`SV剧本/关卡探针` 仅保留在历史更名说明中。
- 上一阶段验证：临时 SDK client 再次连接本地 MCP Server，`tools/list` 返回 24 个 tools；`audit_project` 返回 `totalItems=22`；`search_knowledge_entries keyword=4076` 返回 8 条命中，`list_scenario_command_templates keyword=变量`、`read_scenario_command_template command=0x72` 均可返回；`list_scenario_files kind=S limit=3` 返回 S 剧本总数 59，`read_scenario_commands RS/S_00.eex command_filter=0x5A` 返回 `Scene=3 / Section=17 / Command=819`，`search_scenario_scripts keyword=胜利 relative_path=RS/S_00.eex` 返回 4 条命中。

#### 2026-06-03

- 创建 `基底\刘备传加强版6.5` 派生项目，来源为 `基底\加强版6.5未加密版`；MCP 已识别新项目并确认核心文件、RS 与 Map 资源目录存在。
- 新增 `17_刘备传6.5制作方案.md` 与 `18_刘备传6.5实施记录.md`，记录刘备传 MVP 路线、MCP 写入范围、安全边界、初始化改动和复读结果。
- 通过 MCP direct 模式初始化刘备传人物表前 10 行，并对 `R_01` 到 `R_04`、`S_01`、`S_02` 做同容量短文本称呼替换；所有游戏文件改动仅发生在 `基底\刘备传加强版6.5`，GitHub 只提交工具整合包知识库记录。
- 复查人物 R/S 形象读取方式并再次纠偏：人物 R/S 编号来自 `Ekd5.exe` 指定表，R 图像本体定位 `Pmapobj.e5`，S 图像本体定位 `Unit_atk.e5 / Unit_mov.e5 / Unit_spc.e5`；`RS\R_*.eex/S_*.eex` 改按 `R剧本EEX/S剧本EEX` 分类，不再作为人物图像资源。
- 删除已停用且口径错误的 `ImageResourceReplaceService` 人物 R/S EEX 整文件替换链；`--write` 烟测保留 `Ekd5.exe` R/S 指定表写入验证，不再尝试把 `Pmapobj.e5` 当 `RS\R_XX.eex` 替换。
- 同步修正 EEX 探针和图片处理文案：R/S 剧本 EEX 不再误跳到人物 R/S 形象页；人物 R/S 缺失核对改查 `Pmapobj.e5 / Unit_*.e5`。
- 继续复查“R/S 图像与人物设定不匹配”：确认 `Ekd5.exe @ 0xE1000/0xD2800` 编号表按人物行对齐，错配来自旧预览对 `Pmapobj.e5 / Unit_*.e5` 做裸 JPEG/BMP 魔数扫描；已停用“裸扫出现顺序=编号”的候选图。
- 实现 E5 索引表形象预览：形象 E5 文件从 `0x110` 开始读取 12 字节大端索引项（size、size副本、offset）；R=n 取 `Pmapobj.e5` 图 `2n+1`；S 按紧凑编号映射，`S=0` 由职业和预览阵营取默认兵种图，`S=1..32` 取 Unit 图 241..336 三转特殊三联，`S>=33` 从 Unit 图 337 起取一转特殊单张；头像取 `Face.e5` 索引图。BMP/JPG/PNG 直接解码并剔除洋红透明底，原始帧条按固定尺寸裁代表帧并套用普罗工具 `tsb` 256 色调色板。
- S 预览图片内部去除动作名/图号文字，只保留动作帧图片；映射编号仍在信息区和烟测输出中显示。
- 实现 `导入/替换E5`：新增 `E5ImageReplaceService`，按 E5 `0x110` 图片索引表替换单个图号条目；来源支持 BMP/JPG/PNG，也可从备份 E5 抽取同图号条目还原。若来源大于原条目则追加到文件末尾并更新该索引项，不移动其它图片。写入前自动备份目标 E5，写入后复读索引和条目字节，并生成 TXT 与结构化 JSON 报告。
- 形象设定页的 `导入/替换E5`、`还原E5条目` 已从预留弹窗改为实际流程：先按当前 R/S 编号和 S 预览阵营列出候选 E5 文件/图号，再由用户选择具体条目，避免一次性待证假设替换全部动作帧。
- `CCZModStudio.SmokeTests` 新增 `--e5-image-replace-smoke`：在测试副本中复制 `Unit_mov.e5`，生成小 PNG 替换 `#554`，验证备份、复读和结构化报告。`--rs-smoke` 同步新增只读 `E5_IMAGE_REPLACE_PREVIEW`。当前本地 `--write` 会在既有 Hexzmap 写入烟测处被尺寸护栏拒绝：`Hexzmap.e5=44840`，护栏期望 `45254`，与 E5 图片替换无关。
- 地图制作右侧素材库表格隐藏技术列 `Width`、`Height`、`SizeBytes`、`FilePath`，只保留创作时需要快速扫描的素材字段；筛选后会重新应用隐藏规则。
- 新增 MCP 接入：`CCZModStudio.Runtime` 链接服务层源码，`CCZModStudio.McpServer` 提供本地 stdio MCP Server，首批开放项目检测、表格读写、R/S 剧本文本读写、Hexzmap、地图底图、资源替换和本地知识库读取工具。
- MCP 写入支持 `direct` 与 `test_copy` 两种模式；直写当前项目时仍强制使用现有服务层的备份、结构化报告、复读/哈希校验和版本护栏，不绕开 `ProjectVersionGuardService`。
- `ResourceReplaceService` 新增正式项目可用的 `Replace(...)` 入口；`ReplaceInTestCopy(...)` 保留为兼容旧调用的别名，当前不再用测试副本标记阻断已确认资源整文件替换。MCP 的 `replace_resource` 仍通过服务层执行路径、格式和核心文件护栏。
- 新增 `16_模型上下文协议接入.md`，记录 MCP tools、Codex Desktop stdio 接入方式、安全边界和本轮验证结果。
- 宝物设定页新增 `宝物特效` 编辑器，入口位于 `读取宝物/物品`、`保存宝物/物品` 旁边；表头为 `特效号`、`特效名`、`特效说明`。
- 新增 `ItemEffectCatalogEntry` / `ItemEffectCatalogService`：宝物特效改为项目侧 UTF-8 JSON 目录，保存到 `CCZModStudio_Notes\{项目名}_ItemEffectCatalog.json`，支持重复特效号、变长中文特效名和中文说明，并自动生成 JSON 备份。
- 宝物页 `装备特效名` 改为优先读取项目侧宝物特效目录，未命中时回退 `6.5-1-2/6.5-1-3 装备特效名称` 基础表；同一特效号多条记录按 `名称1 / 名称2` 合并显示。
- 数据表字段说明、跨表引用解释和表格引用诊断中的 `装备特效号` 规则已同步到新口径：优先使用项目侧目录；`0` 保持 `无`，`255` 保持 `普通装备/无扩展特效`。
- 明确不再尝试把新特效目录写回 `Ekd5.exe` 固定宽度特效名称区，避免破坏“重复编号、变长中文、跨设备无编码异常”三项要求。
- `CCZModStudio.SmokeTests` 新增 `ITEM_EFFECT_CATALOG_OK` 烟测：验证长中文、重复特效号和 UTF-8 JSON 保真。
- 剧本制作页重新挂载完整工作区：左侧旧版事件树，右侧命令列表、文本线索、搜索结果和详情面板可同时联动查看，便于创作者按 场景/段落/命令 修改。
- 旧版完整树模式下新增保守节点编辑工具栏：可从 `CczString.ini` 选择普通命令并添加到正文末尾、前插、后插、追加到已有子块，或删除无子块且未被 `0x76` 引用的普通命令；结构/跳转命令继续保护。
- 剧本参数表新增普通数值参数编辑：选中非结构/非跳转命令的 `Word16` 或非 `0x76` 的 `Dword32` 参数槽后，可输入十进制或 `0x` 十六进制并应用；文本参数仍走文本节点编辑，可变数组保持只读。
- `CCZModStudio.SmokeTests` 新增/扩展 `--legacy-script-edit-smoke`：在测试副本中验证旧版完整树命令追加、普通参数修改、完整保存复读、命令删除恢复，以及三次保存的备份和结构化报告。

#### 2026-06-02

- 新增旧版完整剧本读写主线：`LegacyScenarioReader` / `LegacyScenarioWriter` / `LegacyScenarioDocument`，按 CczSceneEditor2 v0.23 规则读取和重建 R/S eex。
- 剧本制作页优先绑定旧版真实树；文本挂到命令文本参数下，`完整保存剧本` 会重建场景偏移表、段落/子块长度和 `0x76` 跳转。
- 旧版文本参数保存改走完整结构写回，可随段落长度扩容；旧探针和原地短写保留为兼容路径。
- 剧本页核对目标键统一为 `RS/R_00.eex#Scene=...#Section=...#Command=...#Offset=...`，文本键统一为 `#TextIndex=...#Offset=...`。
- `--rs-smoke` 新增 `LEGACY_SCENARIO_READ`；`--rs-write-smoke` 新增 `LEGACY_SCENARIO_WRITE_OK`，并验证 `R_00.eex` 未修改完整写回 `changedBytes=0`。
- 新增 `13_1_旧剧本编辑器源码对照.md`，直接记录 `a新剧本编辑器v0.23` C++ 源码已确认的 eex 头部、场景/段落、子事件和 `0x76` 跳转回填语义。
- 剧本制作页 `BuildScriptTree(...)` 已按旧源码规则收敛为 `测试条件 -> 正文根 -> 事件正文/子事件载体` 组织方式，不再把 `0x01` 直接当作子树主体。
- 剧本制作页文本线索进一步收敛到 `Section` 级：左树新增 `Section文本线索` / `Scene补充文本线索` / `未归属文本线索` 三层归属，`Scene`/`Section` 节点本身也改为严格上下文文本显示，不再把邻近偏移文本粗暴混入。
- 修正剧本页联动细节：从文本树节点选中时会恢复所属 段落/场景 上下文；文本无明显关联命令时会清空旧命令残留；详情容量统计改为以当前选中文本为准。
- 剧本命令详情新增“旧工具对照”说明，重点补充 `0x00 / 0x01 / 0x0C / 0x0D / 0x76` 的真实角色和当前工具边界。
- 剧本制作页最终同步旧版编辑器界面框架：主界面只挂载旧版事件树和当前树节点面板，命令表、文本表、搜索结果表退出可见 界面，并移除它们的 `SelectionChanged` 导航入口。
- 修复剧本制作页“读取剧本后选择具体剧本卡死”：普通 R/S eex 完整生成旧版命令树，`R_00.eex` 的 8528 条命令不再靠命令表联动承载；单个 段落 超过 20000 条命令时才触发异常折叠保护。
- 修复剧本页 `System.StackOverflowException`：唯一用户导航入口收敛为 `_scriptTree.AfterSelect`；内部显式定位通过 `_bindingScriptDocument` 和 `_selectedScriptCommandRow` / `_selectedScriptTextEntry` 保存当前对象，避免树/表互相触发；旧版命令枚举改为迭代式，读取器/写入器 增加嵌套深度上限，界面 超深子块折叠显示；新增 `--legacy-scenario-depth-smoke`。
- 剧本搜索改为显式定位：搜索后只定位第一条命中并在右侧显示摘要，清空搜索不再恢复默认 段落 或重绑全量命令/文本表。
- 修复战场制作页 S 关卡切换和命令定位：战场命令候选改为优先使用 `LegacyScenarioReader` 旧版完整树生成，`TargetKey` 与剧本页旧版树保持同一套 `Scene/Section/Command/Offset/Id`；旧探针只作兜底。
- 战场 S 关卡异步切换增加待重载标志，加载中发生的下拉框切换会在当前加载结束后补读当前选择，避免下方“出场/坐标候选 / 战场命令定位”停留在上一关。
- `--rs-smoke` 新增 `BATTLEFIELD_LEGACY_CANDIDATES`，验证 `S_00.eex` 可生成战场候选并定位回旧版命令树，同时确认 `S_00.eex` 与 `S_01.eex` 候选签名不同。
- 重写 `ScenarioCommandProbeReader`，清理历史编码异常中文，统一 R/S eex 探针说明和置信度文案。
- 宝物设定页按旧版格式收敛：可见表头顺序为 `ID`、`名称`、`图标`、`类别`、`初始能力`、`能力成长`、`价格（/100）`、`特效号`、`特效名`、`特效值`、`介绍`；隐藏分段/大类/图鉴/类型说明/价格显示/实际效果/特效提示/来源文件等冗余列；`类型` 以 `类别` 短名下拉显示，保存仍写原始类型码；`装备特效名` 只读并随特效号映射中文名称。
- 宝物页类型下拉覆盖 0-255，已知类型显示 `普通剑系` 等短名，未知类型保留 `类型N`，避免跨版本数据无法编辑。
- 本轮验证：`dotnet build .\\CCZModStudio.sln` 通过，0 警告，0 错误。

#### 2026-05-31

- 人物头像/R/S 形象读取口径校正：新增 `CharacterImageResourceService`，把 Data 头像号映射为 Face.e5 小头像号（0 号使用 1-8 候选，n>=1 映射为 n+8），并给出 Tou.dll 真彩资源号=小头像号+300（lang=2052）的解释提示。
- R/S 形象状态列不再误判为 RS\\R_XX.eex / S_XX.eex 缺失，而是改为：R -> Pmapobj.e5 正/反图号（2n+1/2n+2）定位提示；S -> Unit_atk/Unit_mov/Unit_spc.e5 普通/特殊区间解释与文件定位提示。
- 修正默认项目识别路径：优先识别 `基底\\加强版6.5未加密版` 与 `老版游戏制作工具\\CczRSX 6.5\\ConfigTable\\HexTable.xml`。
- 同步修正 `ResourceReferenceDiagnosticService` 的人物形象诊断文案与证据路径，不再引导去补齐 RS\\*.eex。
- 本轮构建验证：主工程与集成版工程均已 `dotnet build` 通过（0 警告 0 错误）。
- 地图制作与地形编辑已统一为变量尺寸模型：`Map\Mxxx` 图片分辨率除以 48 得到地形格宽高，替代旧固定 30x30 解释。
- `Hexzmap.e5` 目录结构更新为当前确认口径：文件绝对偏移 `0x110` 开始，目录项 12 字节大端读取，字段为 `segmentLength`、长度副本、`fileOffset`，地形数据从 `fileOffset + 2` 开始。
- 已验证 `M000 960x960 -> 20x20`、`M056 1776x1440 -> 37x30` 等样例；对应段长分别为格数加 2。
- 主工程和集成版已同步本轮地图/地形尺寸逻辑；两边构建均通过，0 警告，0 错误。
- 新增 `07-数据表与资源/动态链接库图标资源.md`，记录 `Itemicon.dll` 的 `RT_BITMAP ID=100-439` 解析结论与当前 `图标字段 -> 资源ID` 候选映射。
- `宝物设定` 页新增右侧图标预览：选择物品行时按 `图标` 字段从 `Itemicon.dll` 显示候选图标；短剑图标烟测已导出 PNG。
- 新增 `ItemIconPreviewService`，支持标准图标枚举失败时解析 PE `RT_BITMAP` 资源。
- `--rs-smoke` 新增 `ITEM_ICON_PREVIEW` 验证；根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。
- 新增 `08_开发执行规则.md`，固定“先查知识库、再查旧工具、最后改代码”的流程，并把 `.E5S`/`.eex`/人物 R/S 形象指定器校正规则列为不可回退项。
- 在 `plan.md` 顶部新增 `00E` 最高优先级章节，明确本地知识库制度化、E5S/eex 边界、B形象指定器校正规则和后续执行要求。
- 复核 `B形象指定器\形象指定器6.5\System.ini`、`CczRSX 6.5\ConfigTable\HexTable.xml` 与 RS 目录数量；构建、`--rs-smoke`、`--write` 均通过。
- 新增 `05_工具查询索引.md`，把用户指定的查询来源固化为开发规则：
  - 人物形象优先查 `B形象指定器`。
  - 剧本编辑优先查 `a新剧本编辑器v0.23`。
  - 兵种设定和宝物设定优先查 `B形象指定器`、`CczRSX 6.5`。
  - 地形编辑优先查 `地形编辑器`。
- 记录本轮界面卡死修复方向：核心页互跳不再调用旧同步读取兜底，改为后台异步读取和显式加载。
- 新增 `--rs-write-smoke` 测试副本写入验证：覆盖 R/S eex 文本原地短写回、备份、结构化报告、复读校验，以及人物 R/S 指定表写回 `Ekd5.exe` 的复读校验。
- 标记旧 `--write` 全量烟测仍含 `SV004.E5S` 历史断言；后续应拆成 E5S 存档/旧探针兼容测试与 R/S eex 核心读写测试。
- 人物 R/S 预览新增真实头像确认：从人物表 `头像` 字段读取编号，从 `E5\Face.e5` 内扫描 PNG 候选并显示在预览左侧；R/S eex 战斗帧仍保持安全字节预览。
- 新增 `06_验证与烟测.md`。
- `CCZModStudio.SmokeTests` 默认入口和 `--write` 已切到 R/S eex 主线；旧 E5S 检查改为显式 `--legacy-e5s-smoke`，历史全量探针为 `--legacy-all-smoke`。
- 主界面高级页 `SV剧本/关卡探针` 已改名为 `R/S eex高级探针`；读取入口改为 `读取 RS/*.eex`，工作台入口改为 `去R/S探针`。
- 核实 `CczRSX 6.5\ConfigTable\HexTable.xml` 中人物 R/S 表：
  - `6.5-0-4 R形象` = `Ekd5.exe:0xE1000`，与 `B形象指定器` 的 `RFileHead=E1000` 一致。
  - `6.5-0-5 S形象` = `Ekd5.exe:0xD2800`，与 `B形象指定器` 的 `FileHead=D2800` 一致。
- 明确人物形象读取链路（2026-06-03 已再次更正）：`Ekd5.exe` 编号指定表 -> R=`Pmapobj.e5`，S=`Unit_atk.e5 / Unit_mov.e5 / Unit_spc.e5`；`RS\R_XX.eex / RS\S_XX.eex` 只作为 R/S 剧本 EEX。
- 记录剧本读取校正：`.E5S` 是存档信息；核心剧本列表阶段只扫描 `RS\R_*.eex/S_*.eex`，并只读 eex 文件头，选择具体项后再后台解析。
- 修复方向记录：右侧形象预览必须读取真实 `B形象指定器\形象指定器6.5\System.ini`，不再使用被编码破坏的路径。
- 新增只读 `--rs-smoke` 验证：
  - R/S eex 索引：119 个文件，R=60，S=59，E5S=0。
  - R_00.eex 命令候选 40 条，文本线索 21 条。
  - 人物 R/S 表：`Ekd5.exe:0xE1000/0xD2800`。
  - 曹操首行 R=0、S=1，可读到 B形象指定器 `FileHead/RFileHead` 配置。

#### 2026-06-01

- 剧本制作：左侧 场景 树界面增加“事件”分组，并引入子事件嵌套树（启发式：`0x0001` 子事件设定入栈，`0x0000` 事件结束出栈/结束事件体）。
- 人物形象：明确 `R=0/S=0` 为“使用普通形象（与兵种/初始设定相关）”的合法值；本轮已将 S=0 改为按人物职业与预览阵营计算默认兵种 Unit 图号，R=0 默认兵种预览后续再单独处理。
- 修正人物 R 形象只读预览的切片索引：`R号 n -> Pmapobj.e5 正面图 2n+1 (1-based)` 对应切片下标应为 `2n (0-based)`；该裸扫预览在 2026-06-03 复查后已停用，因为它没有解析 Ls12 目录/解压，仍会错配真实人物图像。
- 修正 SmokeTests 对旧工具目录结构的假设：
  - `MaterialLibraryIndexer` 不再强依赖 `普罗-综合工具v0.3\\素材库`，缺失时降级为空素材列表；并增加对 `老版游戏制作工具` 下素材库的候选定位。
  - SmokeTests 对 `CczString.ini` 改为多候选路径（优先 `老版游戏制作工具\\a新剧本编辑器v0.23\\CczString.ini`）。
  - SmokeTests 建议使用独立 `OutDir` 构建以避免 动态链接库 被占用导致的 `MSB3027/MSB3021`。








#### 2026-05-31 12:55

- `兵种设定` 新增 `相克/属性矩阵` 子页，支持读取和保存 `6.5-3-3 兵种相克`、`6.5-3-4 兵种属性`。
- 相克矩阵和属性矩阵列头引用 `6.5-3 兵种系` 名称；属性 8 行语义未确认，当前按原始属性行显示。
- `--write` 新增 `JOB_MATRIX_WRITE_SMOKE_OK`，验证测试副本写回、备份和复读。
- 主工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。
#### 2026-06-05 R 场景制作

- 新增 `R场景制作` 页：左侧读取 R 剧本树和视觉命令候选，右侧提供人物列表、R 形象帧预览、`Mmap.e5` 背景、网格坐标画布、拖放摆放、方向和站姿组调整。
- 新增 `RSceneDraftService` 与 R 场景草稿模型，草稿保存到 `CCZModStudio_Notes/*_RSceneDrafts.json`，不直接写回 `RS\R_XX.eex`。
- `ImageAssignmentPreviewService` 新增 R 场景帧预览：按 `Pmapobj.e5`、`R=n -> 2n+1` 和 `48x64` 帧切分显示角色。
- 本地知识库补充 R 场景视觉命令、`Mmap.e5` 背景、`Pmapobj.e5` 切帧和验证记录。
- 构建和 `--rs-smoke` 通过。

#### 2026-05-31 12:45 战场制作

- `--write` 新增 `BATTLEFIELD_TEXT_WRITE_SMOKE_OK`，验证战场制作页标题/胜败条件保存服务的 R/S eex 文本写回、备份、结构化报告和复读。
- 修正烟测校验方式：保存后按原文本偏移复读，避免自动标题待证假设选到其他短文本导致误判。
- 根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。
#### 2026-05-31 12:40 地图制作

- 新增 `14_地图制作.md`。
- `--write` 测试副本新增 Map 底图复制和 `MAP_IMAGE_WRITE_SMOKE_OK`，验证 `Map\M*.jpg` 整文件替换、备份、结构化报告、SHA256 复读和尺寸复读。
- 根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。
#### 2026-05-31 12:45

- 新增 `08-剧本与战场/剧本与战场制作.md`。
- 复核核心剧本制作页写入路径：`保存选中文本` 直接写入当前项目 `RS\*.eex`，保存前备份、保存后复读；高级探针页的批量文本写回也按当前项目可写口径执行，仍受原容量和结构边界限制。
- 记录下一步仍需对照 `a新剧本编辑器v0.23` 拆清命令边界和可写参数。
#### 2026-05-31 12:40

- 战场制作页可见文案继续从旧 `SV` 收敛为 `R/S eex`：按钮、提示、战场命令候选说明和资源联动命名已同步调整。
- 主工程与集成版构建通过；`--rs-smoke` 通过。
#### 2026-05-31 12:35

- 新增 `12_地形编辑.md`。
- 当前 `--write` 测试副本复制范围加入 `Hexzmap.e5`。
- 新增 `HEXZMAP_WRITE_SMOKE_OK`，验证 Hexzmap 首个地形块两个格子的测试副本写回、备份、结构化报告和复读校验。
- 根工程构建与 `--write` 通过。
#### 2026-05-31 12:30

- 新增 `07-数据表与资源/角色设定.md` 与 `07-数据表与资源/兵种设定.md`。
- 新增 `ItemTypeCatalogService` 并同步到集成版；修正宝物类型 8/10/12/58 的关键解释。
- `--rs-smoke` 新增 `ITEM_TYPE_CATALOG`。
- `--write` 新增 `ROLE_WRITE_SMOKE_OK`、`JOB_WRITE_SMOKE_OK`、`JOB_TERRAIN_WRITE_SMOKE_OK`、`JOB_EFFECT_WRITE_SMOKE_OK`，将角色、宝物、兵种核心读写纳入当前主线测试副本验证。
- 根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。
#### 2026-05-30

- 新增本地知识库。
- 写入用户最新校正：`.E5S` 是存档信息，`.eex` 才是 R/S 剧本/资源相关格式。
- 将 `B形象指定器\形象指定器6.5\System.ini` 作为人物 R/S 形象读取校准来源。
- 记录 6.5 形象指定器关键配置：`FileHead=D2800`、`RFileHead=E1000`、`CountSV=900`、`UserPath2=...\RS\`。
- 标记当前程序中“SV/E5S 剧本读取”属于需要重构/更名/校正的高优先级问题。

#### 2026-05-31 08:05

- 修复 `ScenarioCommandProbeReader` 遗留编码异常，并明确 R/S eex 与 E5S旧探针的解释边界。
- 新增/调整剧本核对范围：`R/S命令`、`R/S文本`、`R/S命令模板` 为新主线；旧 `SV命令`、`SV文本`、`SV命令模板` 仅作兼容别名。
- 主界面、命令引用核对清单和模板目录逐步改为 R/S eex 命名。
- 构建与主线烟测通过：根工程、集成版、`--rs-smoke`、`--write`、`--legacy-e5s-smoke`。

#### 2026-05-31 08:30

- 新增 `07_宝物物品设定.md`。
- 新增 宝物设定 创作页：合并物品基础表、说明表和装备特效名解释，支持筛选、编辑、备份、复读校验。
- --write 烟测新增 ITEM_WRITE_SMOKE_OK，验证 Data/Star/Imsg 中宝物/物品字段测试副本写回。
- 进一步把 .E5S 相关 界面/诊断/备份文案降级为 E5S存档信息/旧兼容，避免与 R/S eex 主线混淆。

#### 2026-05-31 08:40

- 按 `B形象指定器\形象指定器6.5\System.ini` 的 `DefID=70`、`AssID=109`，给 `宝物设定` 新增 `大类` 解释。
- `宝物设定` 新增 `类型说明`、`价格显示`、`特效提示`，筛选范围同步扩展。
- 修改类型、图鉴、价格、特效号、效果值或成长后，解释列会同步刷新。
- 修正 `07_宝物物品设定.md` 中 `Star.e5` 偏移显示，补充 B形象指定器分段规则。
#### 2026-05-31 13:20

- 将地图/地形变量尺寸、剧本/战场耦合降优先级、人物 R/S 形象错误映射、辅助装备特效号=2 误读列为最高优先级。
- 明确后续先修宝物字段解释，再重构地图/地形 48px/格模型，再修正人物形象页。

#### 2026-06-02 跨设备迁移

- 新增 `15_跨设备迁移.md`。
- 重构 `ProjectDetector`：`HexTable.xml`、`CczString.ini`、旧工具目录、素材库、B形象指定器和普罗搬运配置改为项目级 portable search，避免跨设备复制或从输出目录启动后仍依赖旧绝对/固定相对路径。
- `CczProject` 新增路径诊断信息；主界面在缺少 `HexTable.xml` 时输出迁移搜索诊断。
- 烟测新增 `--migration-smoke`，把最小游戏目录复制到临时位置后重新定位 `HexTable.xml` 与 `CczString.ini`。
- 构建、`--migration-smoke`、`--rs-smoke`、`--rs-write-smoke` 均通过。

#### 2026-06-02 项目级主动读取

- `CczProject` 扩展旧工具资源路径：剧本字典、旧剧本编辑器目录、B形象指定器目录与 `System.ini`、素材库、普罗补丁/搬运配置目录。
- `ProjectDetector` 在创建项目时集中解析上述路径，并把结果写入项目诊断；字段失效时先校验存在性，再回退 portable search。
- 主界面加载项目后主动读取 `CczString.ini` 与素材库索引；手动按钮保留为刷新入口，剧本命令模板使用已加载字典生成覆盖情况。
- 素材库索引器、人物 R/S 形象预览、宝物大类边界、普罗补丁/搬运选择改为优先使用项目级路径。
- 烟测同步使用 `CczProject` 读取字典与素材库；`--migration-smoke` 额外输出旧工具目录、素材库、补丁配置目录和素材数量。

#### 2026-06-06 旧版剧本树中文显示对齐

- 新增 `LegacyScenarioCommandDisplayFormatter`，按旧版 `UpdateShow()` 风格生成命令行中文摘要。
- 剧本编辑器、战场编辑器 S 剧本树、R 场景编辑器 R 剧本树统一使用该格式器，树节点不再优先暴露裸参数。
- `Section` 节点显示收敛为 `Section N`。
- 扩展 `--legacy-mfc-dialog-smoke` 覆盖树显示格式器，构建、旧对话框 smoke 与 `--rs-smoke` 均通过。

#### 2026-06-06 旧版右键编辑菜单迁移

- 剧本制作页旧版树新增旧版 `IDR_POPUP_EDIT` 同序右键菜单：修改、添加、添加子事件、步进复制、批量修改、删除、移动、剪切/复制/粘贴、全部展开、跳转。
- `添加/添加子事件/粘贴` 改为旧版 `FindLast(cur_item)` 语义，即在当前指令前插入，并保护 `0x01 + 子事件承载命令` 配对。
- 子事件候选和 段落 顶层新增限制接入旧版 `code_test[124]`；移动禁止触碰 `0/1` 和 段落 顶层头部指令。
- 批量修改菜单保留但禁用，继续遵守“大规模修改暂缓”的边界。
- 更新本地知识库源码对照与验证记录；主工程构建、SmokeTests 构建、`--legacy-script-edit-smoke`、`--legacy-mfc-dialog-smoke`、`--rs-smoke` 均通过。

#### 2026-06-06 战场 S 剧本树中文映射修正

- 修复战场制作页 `ApplyBattlefieldScriptPreviewToNode()` 覆盖旧版中文摘要的问题；地图预览刷新现在优先保留 `LegacyScenarioItemData.Command` 的旧版 `UpdateShow()` 中文映射。
- `RefreshBattlefieldScriptPreviewTree()` 同时识别旧版节点 `LegacyScenarioItemData` 与兼容探针节点 `ScenarioStructureRow`。
- 主工程构建、`--rs-smoke`、`--legacy-mfc-dialog-smoke` 顺序执行通过。

#### 2026-06-06 剧本制作页右侧布局收敛

- 删除剧本制作页右侧备注编辑区和顶部“备注”按钮。
- 右侧改为“图片预览 / 参数修改”两段式布局，参数区占用原备注区空间。
- 图片预览与参数修改之间改用可上下拖动并持久化比例的 `SplitContainer`。
- 主工程构建通过。

#### 2026-06-06 剧本制作页右侧标题与说明框移除

- 去掉右侧外层“图片预览”和“参数”标题。
- 去掉图片预览下方说明框，图片控件直接铺满上半区。
- 参数 对话框 host 直接横向铺满下半区，底部保留应用/重置按钮。
- 主工程构建和 `--legacy-mfc-dialog-smoke` 通过。

#### 2026-06-06 旧式对话框 中文显示不全修正

- 修复旧式 MFC 对话框 迁到 WinForms 后中文标签和下拉框内容被固定资源宽度裁剪的问题。
- 对话框 host 现在会在数据源初始化后测量中文文本，自动扩宽标签、下拉框、复选框和按钮，并按行顺移后续控件。
- 超宽 对话框 通过内层滚动区域兜底；数据绑定、确认/取消、`ItemData` 写回和完整保存链路不变。
- 主工程构建和 `--legacy-mfc-dialog-smoke` 通过。

#### 2026-06-06 R 场景制作中文映射补齐

- 自检 R 场景制作页后确认：左侧旧版树已经使用共享旧版树组件，但视觉命令候选列表仍显示裸参数摘要，和剧本编辑器体验不一致。
- `RSceneDraftService` 新增旧版命令显示/参数显示回调；R 场景读取与旧式对话框 修改刷新后都使用 `LegacyScenarioCommandDisplayFormatter`。
- R 场景候选命令现在显示旧版 `UpdateShow()` 风格摘要，例如 0x30 武将出现显示人物、坐标、方向和动作，不再把裸参数串作为主要显示。
- 主工程构建、`--legacy-mfc-dialog-smoke`、`--rs-smoke` 通过。

#### 2026-06-08 全局设定入口

- `核心创作/角色设定` 和 `角色设定` 页新增 `全局设定` 入口。
- 新增 `GlobalSettingsDialog` 与 `GlobalSettingsService`，按现代分页界面提供全局参数证据、兵种名、职业名、游戏标题和证据面板。
- 已开放安全写回：`6.5-3 兵种系`、`6.5-4 详细兵种`、`Ekd5.exe@0x8D3C4` 游戏标题；保存前备份，保存后复读，并生成结构化报告。
- 旧形象指定器全局设置左侧数值项当前只显示待验证状态；未找到 6.5 可靠偏移前不向未知地址写入。
- 新增 `--global-settings-write-smoke`，验证测试副本中兵种名、职业名、游戏标题保存、备份、报告和复读。

#### 2026-06-09 颍川之战全程自动采证归纳

- 对 `CCZModStudio_Reports\DebugEvidence\merit-auto-live-20260609-202936` 的自动监控日志完成归纳，新增报告 `first-stage-summary.md`。
- 本次样本覆盖第一关进入战场到通关清场：记录 `58` 个 `UnitDiff` 事件，初始 `30` 个单位，`event 58` 中剩余单位全部消失。
- 更新 `战场操作自动化与调试流程.md`：记录自动监控已能捕获单位出现/消失、坐标、HP、行动状态候选和临时属性字节变化。
- 更新 `功勋模式与五维功勋.md`：明确本次全程监控只能证明战场状态变化；五维功勋存储字段、写入函数和过关奖励仍需写断点闭环。

#### 2026-06-09 自动地址定位实验台

- 新增 `工具整合包\MCP配置\auto-locate-6.5-addresses.ps1`，可从 `events.jsonl` 与前后快照差分生成候选地址评分和 x32dbg 硬件写断点计划。
- 默认只做离线只读分析，不写游戏进程、不修改 EXE；可选 `-ApplyFirstBatch` 仅下第一批调试器硬件写断点。
- 输出 `auto-locate-summary.md`、`auto-locate-candidates.json`、`watchpoint-plan.json`、`x32dbg-watchpoint-plan.txt`，为后续功勋字段、行动状态和战斗写入函数定位提供自动化入口。
- 知识库同步记录自动定位边界：高分候选仍为待证，必须由写断点命中、调用栈和 UI 语义共同闭环后才能升级为已验证。
- 脚本升级为双计划输出，新增 `merit-watchpoint-plan.json` 与 `x32dbg-merit-watchpoint-plan.txt`；功勋优先计划会将已知 `+1` 种子和 `battle_globals` 精确 `+1` 候选前置。
- 修正 x32dbg 硬件断点命令生成：`bphws ADDRESS,w,SIZE` 是设置硬件写断点，`bphwc` 是删除硬件断点；旧 `bphwc` 计划不得继续使用。
- 使用曹操攻击张梁且格挡反击的前后快照重新生成 `CCZModStudio_Reports\DebugEvidence\merit-physical-locator-20260609-222320`，功勋优先第一批写断点为 `0x00492F9C`、`0x00496E79`、`0x004B1B1A`、`0x00492C20`。
- 新增 `工具整合包\MCP配置\run-6.5-merit-watchpoint-experiment.ps1`，可读取 `merit-watchpoint-plan.json`，自动应用指定批次硬件写断点、运行/轮询 x32dbg，并在暂停时导出 CIP、反汇编、寄存器、栈和候选内存证据；支持 `-DryRun -ApplyOnly` 离线验证计划解析。
- 自动实验脚本新增旧计划自修复：读取旧 `bphwc` 命令时会按候选地址重新生成 `bphws`。DryRun 已验证旧计划 4 条命令全纠正，新计划 0 条纠正，报告分别位于 `merit-watchpoint-dryrun-old-plan-correction` 与 `merit-watchpoint-dryrun-new-plan-correction`。
- 新增 `工具整合包\MCP配置\start-6.5-merit-auto-experiment.ps1` 一键编排入口，负责定位最新功勋计划、检查 bridge/进程状态、可选启动 x32dbg、可选调用写断点实验脚本。
- 使用 `-DryRun -RunExperiment -ApplyOnly` 生成 `CCZModStudio_Reports\DebugEvidence\merit-auto-experiment-20260609-234546`，验证一键编排能读取 `merit-physical-locator-20260609-222320` 计划和 4 个功勋候选，并在 bridge 离线时生成可读预检报告。
- 新增 `工具整合包\MCP配置\summarize-6.5-merit-watchpoint-hits.ps1`，用于汇总 `merit-watchpoint-hit-YYYYMMDD-HHMMSS.json` 中的 CIP、反汇编、寄存器、候选内存和游戏动作标签；未命中时明确保持 `needs-write-breakpoint`，防止把离线候选误升为已验证。
- `start-6.5-merit-auto-experiment.ps1` 已接入命中汇总器。最新 DryRun 为 `CCZModStudio_Reports\DebugEvidence\merit-auto-experiment-20260609-235433`，确认候选计划可读、bridge 离线时不阻塞、命中汇总 `HitCount=0`。

#### 2026-06-10 功勋写断点草稿链路

- `summarize-6.5-merit-watchpoint-hits.ps1` 增加 `CCZModStudio_Notes\DebugKnowledgeDrafts` 知识库草稿输出；无命中时草稿只给出待证口径，有命中时列出人工审查清单，不直接改写正式知识库。
- 验证最新离线链路：`CCZModStudio_Reports\DebugEvidence\merit-auto-experiment-20260610-001237` 的汇总结果仍为 `HitCount=0`，并生成草稿 `CCZModStudio_Notes\DebugKnowledgeDrafts\merit-knowledge-draft-20260610-001248.md`。所有功勋候选继续保持 `needs-write-breakpoint`。

#### 2026-06-11 物理攻击动态调试与原版文档归档

- 实机调试第二次友方/敌方自动阶段：`0x00405AD5` 从暂停采样改为 log-only，hit_count 增长到 `25`；`0x00410F1C` hit_count 增长到 `12`。当前确认自动阶段物理伤害也复用 `0x00410F1C -> 0x00405AD5 -> 0x0043F6DB -> 0x0043F70C` 这条计算/应用链。
- 新增自动阶段样本：`unit[24] -> unit[65]` 普通击杀、`unit[25] -> unit[63]` 暴击、`unit[20] -> unit[68]` 暴击击杀、`unit[22] -> unit[74]` 普通伤害；均在 `0x00405AD5` 读取旧 HP、最终伤害、目标单位指针和 `0x004927F0` 标志。
- 归档 `90-来源归档/原始文档/解读KOEI曹操传代码.docx`：该文档是原版/旧代码解析，只作为当前 6.5 的断点选址线索。已把旧版 `0x004064F1` 攻击循环、`0x00405E03` 伤害/经验处理、`0x00406181` 连击判定、`0x0040639A` 反击处理、`0x00406113/0x00406153/0x00406171` 经验字段写入线索迁入对应专题，并明确不得替代当前动态证据。
- 更新 `物理攻击.md`、`战斗数据结构.md`、`暴击.md`、`连击.md`、`函数速查表.md` 和 `待验证清单.md`：区分已确认后伤害/暴击标志与仍待验证的连击判定、经验写函数、反击谓词、伤害公式内部。
