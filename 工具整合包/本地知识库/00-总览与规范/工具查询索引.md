# 05 工具查询索引

## 结论速览

- 本页由旧平铺/嵌套知识库迁移到统一知识库框架，保留原有细节并补齐统一阅读口径。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。
- 写入类操作必须以 6.5 未加密基底、本地样本、备份、复读和烟测报告为最终依据。

## 适用版本

- 默认适用：曹操传加强版 6.5（未加密）当前项目基底。
- 涉及 6.6/6.6x 或旧版本时，只作为版本差异、兼容提示或外部佐证，不自动进入 6.5 写入规则。

## 已确认事实

- 本页主题已纳入根目录分层知识库，不再依赖旧平铺编号文件或嵌套 knowledge-base 入口。
- 原文中的地址、偏移、结构、工具行为和烟测记录保留在“详细记录”；实施时优先采用已由本地样本、旧工具源码、复读或烟测确认的结论。
- 外部资料只用于补充能力范围、版本边界和制作习惯，不能替代本地证据。

## 实现/使用方法

- 从根目录 README.md 进入对应专题，再按本页详细记录定位具体工具、文件、地址或流程。
- 修改游戏文件前必须确认目标版本、目标路径、写前备份、写后复读和结构化报告。
- 遇到字段语义、命令参数、资源格式或跨版本能力未完全闭环时，先记录到本页“待验证项”或 `00-总览与规范/待验证清单.md`。

## 风险与边界

- 迁移正文中的历史不确定表述已统一为“待证/待查”等口径；这些内容不得直接作为写入或实现依据。
- 6.6/6.6x 能力不得混入 6.5 默认写入路径；如需适配，必须另建版本护栏和样本验证。
- 本页保留的偏移、表结构和命令写法需要与当前基底文件交叉核对。

## 证据来源

- 本地来源：旧平铺/嵌套知识库迁移内容；具体映射见 `90-来源归档/迁移索引.md`。
- 关联来源：../90-来源归档/迁移索引.md、../09-版本与外部资料/联网深度专题.md。
- 证据等级：已验证：本地项目记录，需以样本、旧工具源码或烟测项复核。

## 待验证项

- 若详细记录中出现“待证”“待查”或跨版本能力，实施前必须补本地样本、旧工具源码、复读结果或实机验证。
- 若本页没有额外未决项，本节仅作为保守护栏保留。

## 详细记录

本页是后续开发遇到格式疑问时的“先查哪里”索引。原则：先看本知识库和 `plan.md`，再查对应旧工具目录；没有确认的格式不做强写。

#### 用户指定的优先查询来源

| 问题/模块 | 必查工具文件夹 | 当前用途 | 实施约束 |
| --- | --- | --- | --- |
| 人物形象 | `B形象指定器` | 校对人物 R/S 编号指定表、`System.ini` 中的 `FileHead/RFileHead/UserPath2`、形象资源编号习惯 | 人物形象链路固定为 `Ekd5.exe R/S 指定表 -> Pmapobj.e5/Unit_*.e5/Face.e5 (资源封包)`；不得把 `.E5S` 当人物形象 |
| 剧本编辑 | `a新剧本编辑器v0.23`、`剧本编辑器v0.24` | v0.23 有源码，作为 `.eex` 读写、命令树、旧式 Dialog 的首要依据；v0.24 只有 exe/配置，作为 R 场景预览交互、JSON/HTTP 菜单和 `CczString.ini` 补充线索 | `.eex` 才是 R/S 剧本/资源相关格式；读取列表只扫描 `RS\R_*.eex/S_*.eex`；v0.24 静态取证不能替代源码级写回规则 |
| 兵种设定 | `B形象指定器`、`CczRSX 6.5` | 校对兵种/形象联动、`HexTable.xml` 表定义、数据偏移和字段含义 | 只写已确认表字段；保存前备份，保存后复读校验 |
| 宝物设定 | `B形象指定器`、`CczRSX 6.5` | 校对宝物表、装备/特效字段、旧工具字段命名 | 未确认特效位和扩展字段前只做只读解释或原字段写回 |
| 地形编辑 | `地形编辑器` | 校对 `Hexzmap.e5`、地图地形块、画笔和保存习惯 | 地形块写回必须保留未知字节；拖拽绘制要做缓存和节流，不能影响地图制作页稳定性 |

#### 当前最高优先级查询规则

1. 剧本/战场相关如果再次出现“读取卡死”或格式错判，先查 `a新剧本编辑器v0.23` 源码，再核对 `ScenarioFileReader` 是否仍只读 `.eex` 头部索引；R 场景预览交互再查 `剧本编辑器v0.24`。
2. 人物形象如果出现 R/S 资源错位，先查 `B形象指定器\形象指定器6.5\System.ini`，再核对 `ImageAssignmentService` 的 R=`0xE1000`、S=`0xD2800` 护栏。
3. 兵种、宝物、角色字段如果出现偏移疑问，先查 `CczRSX 6.5\ConfigTable\HexTable.xml`；旧工具显示习惯只作为字段命名和 UI 参考。
4. 所有写入功能必须经过：目标文件备份 -> 保留未知字节写回 -> 复读校验 -> 失败提示最近备份。

#### 仍需用户或旧工具进一步提供的信息

- `a新剧本编辑器v0.23` 对 `.eex` 的完整命令长度、Section 边界和插入/删除规则；`剧本编辑器v0.24` 的 R 场景吸附、滚轮朝向/动作和 JSON/HTTP 输出结构仍需继续验证。
- `B形象指定器` 对 R/S `.eex` 帧级图像解码、调色板和动作序列的真实规则。
- `CczRSX 6.5` 中宝物特效、兵种扩展位、人物扩展属性的字段解释。
- `地形编辑器` 对 `Hexzmap.e5` 变量尺寸地形段、段头 2 字节、地图编号映射和附加结构的解释。
