# 09 图标资源

本页记录物品/命令/策略等图标资源的已确认信息。当前重点用于 `宝物设定` 页右侧图标预览。

## 已确认：Itemicon.dll

文件：`加强版6.5未加密版\Itemicon.dll`

当前解析结论：

- `Itemicon.dll` 不是普通 `ExtractIconEx` 可枚举的标准图标 DLL；Windows 标准图标枚举返回 0。
- 它包含 PE 资源目录中的 `RT_BITMAP` 资源。
- 已解析到 `RT_BITMAP` 资源 ID `100-439`，共 340 个 Bitmap 资源。
- 这些资源按 16x16 / 32x32 成对出现，因此当前程序按如下候选规则预览：

```text
物品“图标”字段 N
-> 优先显示 RT_BITMAP 资源 ID = 100 + N * 2 + 1（32x32）
-> 若不存在，再退回 ID = 100 + N * 2（16x16）
```

示例：

- 物品 ID `0`，名称 `短剑`，图标字段 `0`
- 预览资源：`Itemicon.dll` 的 `RT_BITMAP ID=101`
- 烟测可导出短剑图标 PNG。

## 实现边界

- 当前已经能从 `Itemicon.dll` 读取并显示候选图标。
- 当前映射规则来自资源排列和字段值的对应推断，已能用于创作者快速确认，但仍建议结合旧工具或实机画面最终校验。
- 程序不会写回 `Itemicon.dll` 图像资源；目前只写物品表中的 `图标` 字段。
- 如果后续要支持替换图标资源，需要单独确认 RT_BITMAP 写回、调色板、尺寸、资源 ID 和 PE 资源表重建规则。

## 待补充

- `Cmdicon.dll` 命令图标资源映射。
- `Mgcicon.dll` 策略/法术图标资源映射。
- `E5\Mark.e5`、`E5\U_select.e5` 与界面标记/选择框的资源关系。
- 旧工具或实机截图用于校验物品图标字段 N 是否始终对应 `100 + N * 2 + 1`。
