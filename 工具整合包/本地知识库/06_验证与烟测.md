# 06 验证与烟测

本页记录当前项目的验证入口，避免旧的 `.E5S` 假设继续阻塞 R/S eex 主线。

## 当前主线验证

| 命令 | 用途 | 是否写入 | 说明 |
| --- | --- | --- | --- |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll` | 默认核心烟测 | 否 | 默认只跑 R/S eex 主线，只读验证剧本索引、人物 R/S 表、头像预览和 R/S E5 索引表预览，不再进入旧 SV004.E5S 全量断言 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-smoke` | R/S eex 只读烟测 | 否 | 明确验证 `RS\R_*.eex/S_*.eex` 索引不混入 E5S，验证 B形象指定器偏移、头像预览和 E5 `0x110` 索引表预览 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-scenario-depth-smoke` | 旧版完整剧本深度烟测 | 否 | 遍历全部 R/S eex，使用迭代方式统计旧版完整树命令数和最大子事件嵌套深度，防止递归枚举再次引入 StackOverflow |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --write` | R/S eex 核心写入烟测 | 只写测试副本 | 当前等价于 R/S 核心写入验证：复制测试副本，写 `RS\R_00.eex` 文本和 `Ekd5.exe` 人物 R/S 指定表，备份并复读校验 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --rs-write-smoke` | R/S eex 核心写入烟测 | 只写测试副本 | 与 `--write` 主线相同，名称更明确 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --e5-image-replace-smoke` | E5/DLL 图片资源目录与条目替换烟测 | 只写测试副本 | 先复制常见图片 E5 和图标 DLL 到测试副本，校验 `Face/Pmapobj/Unit_*/Hitarea/Effarea/Logo/Mmap/U_select/Gate/Weather` 等 E5 资源目录与 `Itemicon/Mgcicon/Cmdicon` DLL 图标编号预览，再生成小 PNG 替换 `Unit_mov.e5` 单个 `0x110` 索引图号，复读条目字节并验证备份/JSON 报告 |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-mfc-dialog-smoke` | 旧版 MFC Dialog 复刻烟测 | 否 | 从旧 `OnEditModify()` 解析 101 个命令号到 71 个 Dialog 的分派，校验 C# 分派表、旧 `.rc` 资源、71 个显式行为目录、`Dialog_114` 34 个模板，并逐个实例化旧式 Dialog 执行初始化/提交 |

## 2026-06-06 旧版 MFC Dialog 修改链路验证

- `dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj`：通过，0 警告，0 错误。
- `dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj`：通过，0 警告，0 错误。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --legacy-mfc-dialog-smoke`：通过，输出 `LEGACY_MFC_DIALOG_SMOKE_OK oldCommands=101 dialogs=71 templates=34 sortedCombos=Dialog_33.IDC_COMBO1`。
- `dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --legacy-script-edit-smoke`：通过，输出 `LEGACY_SCRIPT_EDIT_SMOKE_OK file=R_00.eex ... command=0x09/延时 param=12345 count=8528->8529->8528`。
- 2026-06-06 UI/入口统一增量：
  - 剧本制作、战场制作 S 剧本、R 场景制作 R 剧本均使用旧式可视树：`文件路径 -> Scene -> Section -> 真实指令节点`。
  - 静态检查重点：旧版完整树路径不再创建 `测试条件`、`正文根`、`事件正文`、`文本参数`、`子事件主体/载体` 包装节点；这些字符串只允许出现在兼容探针、历史说明或内部详情。
  - 三处双击命令均进入 `LegacyCommandEditDispatcher`，保存按钮分别为 `完整保存剧本`、`完整保存S剧本`、`完整保存R剧本`，均走 `LegacyScenarioWriter.Save`。
  - 剧本制作页右侧 UI 无对象预览、无详情大块、无批量修改按钮；参数区为内嵌旧式 Dialog，底部为项目侧 `备注`。
- 注意：不要并行执行 `dotnet build` 和 `dotnet run` 指向同一 SmokeTests 输出目录；Windows 会因 DLL 被占用出现 `CS2012/MSB3027`，应先构建再 `--no-build` 运行，或使用独立 `OutDir`。

## 2026-06-03 E5 索引表形象预览验证

- 2026-06-04 图片处理增量：`dotnet build .\CCZModStudio.sln` 通过，0 警告，0 错误。
- 2026-06-04 图片处理增量：`dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `IMAGE_RESOURCE_CATALOG files=18 face=293 markIndex=False hit=33 eff=12 itemIcons=170 mgcIcons=49 cmdIcons=41` 和 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`，验证图片处理页资源目录、`Mark.e5` 只定位边界、`Face.e5 #1` 预览渲染、DLL 图标编号预览与测试副本单条目替换。
- `dotnet build .\CCZModStudio.sln`：通过，0 警告，0 错误。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke`：通过，输出 `E5_IMAGE_REPLACE_PREVIEW file=Unit_mov.e5 entries=556 image=554 ...`、`RS_IMAGE_PREVIEW ... mapped=554 ... colorPixels=...` 与 `RS_SMOKE OK`。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`，验证测试副本单条目写入、备份和结构化报告。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj`：通过，默认核心烟测同样验证 `S=0 职业1 -> Unit#4/#5/#6`、`S=1 -> #241/#242/#243`、`S=32 -> #334/#335/#336`、`S=33 -> #337`、`S=250 -> #554`、`S=253 -> #557 越界`。
- `dotnet run --project .\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --write`：本地当前会在既有 Hexzmap 写入烟测处被版本护栏拒绝，原因是 `Hexzmap.e5` 当前大小 `44840`，护栏期望 `45254`；这不是 E5 图片条目替换链路失败。
- 视觉确认：生成的 `Smoke_RS_S250_UnitPreview_*.png` 仅包含三格彩色代表帧，图片内不叠加动作名/图号文字，按新映射读取 Unit 图 554；`Smoke_RS_R00_FacePreview_*.png` 中 R0 可显示 `Pmapobj.e5 #1`，JPG 洋红透明底已按阈值剔除。

## 2026-06-02 剧本页旧版 UI 同步验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，`LEGACY_SCENARIO_READ file=R_00.eex scenes=5 sections=7 commands=8528 jumps=462 texts=712`。
- `--rs-smoke`：通过，`BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=133 located=友军出场设定@0x000091 compare=S_01.eex`，用于防止 S 关卡切换后战场候选不刷新、TargetKey 无法回查旧版命令树。
- `--rs-write-smoke`：通过，`LEGACY_SCENARIO_WRITE_OK file=R_00.eex ... changedBytes=0`。
- `--legacy-scenario-depth-smoke`：通过，`files=119/119 failed=0 commands=107462 maxDepth=13 maxFile=R_00.eex uiFoldDepth=64`。
- 静态自检：剧本页不再订阅 `_scriptCommandGrid.SelectionChanged`、`_scriptTextGrid.SelectionChanged`、`_scriptSearchResultGrid.SelectionChanged`；用户导航入口收敛为 `_scriptTree.AfterSelect`。

## 2026-06-05 战场 S 读取同步与真实写回验证

- `dotnet build .\工具整合包\CCZModStudio.sln`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，`BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=329 deployment=友军出场/我军出场/敌军出场 located=友军出场设定@0x000091 compare=S_01.eex map=S_00.eex:M000:M000.JPG:True:True:0x3DA`。
- `--rs-smoke` 现在同时校验三类签名：不同 S 剧本的战场命令候选不同、出场候选不同、地图联动/预览签名不同。该项用于防止切换 `S_XX.eex` 后左侧 S 树、右侧候选和地图预览停留在上一关。
- `--rs-smoke` 同时校验我军候选出战位叠加：输出 `allySlots=S_01.eex:7 first=#1@(19,9) forced=2`，确认 `S_01.eex` 可从 `R 4057 + 4B` / `S 4B` 中解析 7 个候选位，首位顺序与坐标正确，并能从 `06/4A` 强制列表绑定 2 个强制出战角色。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_TEXT_WRITE_SMOKE_OK file=S_00.eex ...`，确认战场标题/胜败条件在 S 剧本测试副本中走旧版完整结构保存、备份、报告和复读。
- `--rs-write-smoke`：通过，输出 `BATTLEFIELD_DEPLOYMENT_WRITE_SMOKE_OK file=S_00.eex target=Scene=1;Section=1;Command=8;Offset=0x000091;Id=0x46;Record=0 person=32 coord=(14,1)->(0,1) ... changedBytes=1`，确认已有 `46/47` 出场记录可真实写回原 S 剧本测试副本并复读。`4B` 是我军出战排列，不作为直接地图坐标写回。
- 当前样本 `Hexzmap.e5` 为 `44840` 字节，版本护栏中 6.5 基准为 `45254` 字节；`--rs-write-smoke` 对 Hexzmap 地形写入和地图工作台地形发布输出 `*_SKIPPED`，这是保护性拒写，不绕过护栏。

## 2026-06-05 战场拖放左右指令预览同步验证

- `dotnet build 工具整合包\CCZModStudio.sln`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，仍输出 `BATTLEFIELD_LEGACY_CANDIDATES first=S_00.eex commands=155 units=329 deployment=友军出场/我军出场/敌军出场 ... compare=S_01.eex map=S_00.eex:M000:M000.JPG:True:True:0x3DA`，确认读取/切换 S 剧本的左侧 S 树、右侧候选和地图联动签名未回退。
- `--rs-write-smoke`：通过，仍输出 `BATTLEFIELD_TEXT_WRITE_SMOKE_OK` 与 `BATTLEFIELD_DEPLOYMENT_WRITE_SMOKE_OK ... Id=0x46;Record=0 ... coord=(14,1)->(0,1)`，确认 UI 预览同步改动没有破坏测试副本真实写回、备份和复读闭环。
- 交互边界：拖放绑定已有 `46/47` 脚本记录时，右侧出场候选、右侧命令表、左侧 S 树节点显示“地图预览已调整”；纯拖放新增没有原脚本记录，仍只保存项目侧 JSON，不改变左右指令。

## 2026-06-02 宝物页旧版 UI 同步验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，保留 `ITEM_TYPE_CATALOG`、`ITEM_ICON_PREVIEW`、`ITEM_ACCESSORY_EFFECT_MODEL`。
- `--rs-write-smoke`：通过，`ITEM_WRITE_SMOKE_OK low=0:短剑->烟测宝物 price=5->1 high=104:虎豹嘶风铠->烟测扩展 saves=4`，确认 UI 派生列收敛不影响 Data.e5、Star.e5、Imsg.e5 写回。
- 静态自检：宝物页表头顺序为 `ID`、`名称`、`图标`、`类别`、`初始能力`、`能力成长`、`价格（/100）`、`特效号`、`特效名`、`特效值`、`介绍`；隐藏分段/大类/图鉴/类型说明/价格显示/实际效果/特效提示/来源文件；`类型` 显示为 `类别` 下拉但底层仍绑定原始 `类型` 字段；`装备特效名` 查中文特效名称。

## 2026-06-03 宝物特效目录验证

- `dotnet build .\工具整合包\CCZModStudio.sln -p:BaseOutputPath=...\工具整合包\_BuildCheck\`：通过后，使用同一输出目录运行 `CCZModStudio.SmokeTests.dll --rs-write-smoke`。
- `--rs-write-smoke`：应额外输出 `ITEM_EFFECT_CATALOG_OK`，验证项目侧 UTF-8 宝物特效目录可读写、长中文可保真、重复特效号可合并显示。
- 静态自检：
  - `宝物设定 -> 宝物特效` 保存到 `CCZModStudio_Notes\{项目名}_ItemEffectCatalog.json`。
  - `装备特效名`、字段说明面板、跨表引用解释、资源诊断中的装备特效规则，均优先使用项目侧目录，未命中时再回退基础特效名称表。
  - `0` 解释为 `无`，`255` 解释为 `普通装备/无扩展特效`，不被项目侧目录覆盖。

## 2026-06-03 MCP 接入验证

- 2026-06-05 配置包验证：新增 `工具整合包\MCP配置`，运行 `powershell -NoProfile -ExecutionPolicy Bypass -File ".\工具整合包\MCP配置\generate-mcp-config.ps1" -Build` 通过，生成 Codex TOML、通用 `mcpServers` JSON、Claude Desktop JSON 和 direct-dotnet 备用配置到 `MCP配置\_generated`；该目录已加入 `.gitignore`。
- 2026-06-05 stdio 启动入口验证：`powershell -NoProfile -ExecutionPolicy Bypass -File ".\工具整合包\MCP配置\validate-mcp-config.ps1"` 通过；脚本经 `start-ccz-mcp.ps1` 启动 MCP Server，发送 `initialize`、`notifications/initialized`、`tools/list`，返回 `MCP_VALIDATE_OK server=CCZModStudio.McpServer protocol=2025-06-18 tools=32`。
- 2026-06-04 增量：`dotnet build .\工具整合包\CCZModStudio.McpServer\CCZModStudio.McpServer.csproj -v:minimal`：通过，0 警告，0 错误。
- 2026-06-04 增量：`dotnet build .\工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -v:minimal`：通过，0 警告，0 错误。
- 2026-06-04 增量：`dotnet run --project .\工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --e5-image-replace-smoke`：通过，输出 `E5_IMAGE_REPLACE_SMOKE OK target=Unit_mov.e5 image=554 kind=RAW->PNG`。
- 2026-06-04 增量：临时 SDK client 使用 `StdioClientTransport` 连接 `CCZModStudio.McpServer.dll`，`tools/list` 返回 32 个 tools，包含新增 `list_e5_image_entries`、`preview_e5_image_replace`、`replace_e5_image_entry`、`audit_project`、`create_test_copy`、`diff_test_copy`、`create_release_copy`、`search_knowledge_entries`、`list_scenario_command_templates`、`read_scenario_command_template`、`list_scenario_files`、`read_scenario_commands`、`search_scenario_scripts`、`list_hexzmap_blocks`、`read_hexzmap_block`、`list_project_resources`、`run_resource_diagnostics`、`list_creator_notes`、`upsert_creator_note`、`delete_creator_note`、`export_creator_notes_csv`；`list_e5_image_entries Unit_mov.e5 limit=3` 返回 `totalEntries=556`，`audit_project` 返回 `totalItems=22`，知识库 `4076` 关键词搜索返回 8 条命中，`变量` 指令模板筛选和 `0x72` 指令模板详情均可返回；`list_scenario_files kind=S limit=3` 返回 S 剧本总数 59，`read_scenario_commands RS/S_00.eex command_filter=0x5A` 返回 `Scene=3 / Section=17 / Command=819`，`search_scenario_scripts keyword=胜利 relative_path=RS/S_00.eex` 返回 4 条命中，`list_hexzmap_blocks keyword=M000 limit=3`、`read_hexzmap_block map_id=M000 max_rows=2`、`list_project_resources category=地图图片 keyword=M000 limit=3`、`run_resource_diagnostics severity=Warn,Error limit=5` 均可返回；制作留痕烟测完成 `upsert_creator_note`、`list_creator_notes`、`export_creator_notes_csv`、`delete_creator_note` 闭环，只写 `CCZModStudio_Notes` 和 `CCZModStudio_Exports/CreatorNotes`。
- `dotnet build .\工具整合包\CCZModStudio.McpServer\CCZModStudio.McpServer.csproj -v:minimal`：通过，0 警告，0 错误。
- `dotnet build .\工具整合包\CCZModStudio.sln -v:minimal -p:BaseOutputPath=..\_BuildCheck\`：通过，0 警告，0 错误。
- 2026-06-03 MCP stdio `initialize` + `tools/list`：通过，返回 11 个 tools。
- MCP `detect_project`：通过，能识别当前基底、HexTable 和旧工具资源路径。
- MCP `read_table`：通过，能读取 `6.5-0 人物` 指定列。
- MCP `write_table_rows`：通过，在测试副本写入 `6.5-0 人物` 的 `级别` 字段，生成 `Data.e5` 备份和 `数据表保存_WriteOperationReport.json`。
- `--rs-smoke`：通过，输出 `RS_SMOKE OK`。
- `--rs-write-smoke`：表格、剧本、宝物、兵种、地图底图等前置写入项通过；最终 `Hexzmap.e5` 写入被版本护栏拒绝，当前自动检测基底尺寸为 `44840`，护栏中的 6.5 基准为 `45254`。该失败不应通过 MCP 绕开，需要先确认 Hexzmap 样本来源或更新护栏依据。

## 旧 E5S 兼容验证

| 命令 | 用途 | 是否写入 | 说明 |
| --- | --- | --- | --- |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-e5s-smoke` | E5S 存档/旧探针兼容检查 | 否 | 只检查 `SV\*.E5S` 是否存在、`B形象指定器` 中 `CountSV`、文本线索和旧命令探针；输出会标注 `compat_only_not_rs_script` |
| `dotnet .\CCZModStudio.SmokeTests\bin\Debug\net8.0-windows\CCZModStudio.SmokeTests.dll --legacy-all-smoke` | 历史全量探针 | 可能按旧逻辑继续运行 | 仅用于追溯旧实现，不作为当前主线成功标准；其中仍可能包含未清理的 SV004.E5S 历史断言 |

## 规则

1. 后续提交或阶段自检优先运行：根工程 build、集成版 build、默认烟测、`--write`。
2. `.E5S` 只能作为“E5S 存档/旧探针兼容”检查对象，不能重新作为 R/S 剧本主格式。
3. 写入烟测必须只写测试副本，不能修改当前游戏本体；这只是测试策略，不代表工具功能必须以测试副本为前置条件。
4. 若某次旧全量探针失败，不得因此回退 R/S eex 主线；应拆分失败项并明确归入 E5S 兼容或高级调试。

## 2026-06-01 补充：SmokeTests 输出目录被占用的 build 失败规避

在 Windows 上，如果某个旧的 SmokeTests/调试进程仍占用 `bin\\Debug\\...` 下的 `CCZModStudio.dll`，`dotnet build` 可能出现：

- `MSB3027/MSB3021 无法复制 ... because it is being used by another process`

这不是代码错误。建议改用独立输出目录构建 SmokeTests，再用该目录的 DLL 运行烟测：

```powershell
dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -c Debug -p:OutDir=..\..\CCZModStudio_TestBins\SmokeTests\
dotnet CCZModStudio_TestBins\SmokeTests\CCZModStudio.SmokeTests.dll --rs-smoke
dotnet CCZModStudio_TestBins\SmokeTests\CCZModStudio.SmokeTests.dll --write
```

## 2026-06-05 R 场景制作页验证记录

- 根工程构建：`dotnet build 工具整合包\CCZModStudio.sln` 通过，0 警告，0 错误。
- R/S 主线烟测：`dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke` 通过，输出 `RS_SMOKE OK`。
- 本次未运行写回烟测作为 R 场景验收项，因为 `R场景制作` 页当前只保存项目侧 `CCZModStudio_Notes/*_RSceneDrafts.json` 草稿，不直接写回 `RS\R_XX.eex`。
- 需要人工 UI 复核的点：读取 R 剧情、选择 `Mmap.e5` 背景、拖放人物、切换方向和动作帧、保存草稿、重新读取后恢复草稿。

## 2026-06-05 R 场景制作页二次修正验证

- 根工程构建：`dotnet build 工具整合包\CCZModStudio.sln` 通过，0 警告，0 错误。
- R/S 主线烟测：`dotnet run --project 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj -- --rs-smoke` 通过，输出 `RS_SMOKE OK`。
- 覆盖点：主入口顺序调整、核心创作卡片新增 `R场景制作`、R 形象 20 帧缩略图选择、`640x400` 斜菱形坐标换算、R 场景命令与剧本制作页定位联动。
- 仍需人工 UI 复核：缩略图点击是否符合创作者预期、拖放到边缘半格时坐标是否与旧工具一致、`30/32/33/34` 指令参数槽确认后再开放真实写回。

## 2026-05-31 08:05 验证记录

- 根工程构建：通过，0 警告，0 错误。
- 集成版工程构建：通过，0 警告，0 错误。
- `--rs-smoke`：通过，确认 R/S eex 主线索引不混入 E5S。
- `--write`：通过，只在测试副本中验证 R/S eex 文本短写回与人物 R/S 表写回。
- `--legacy-e5s-smoke`：通过，仅为 E5S 旧兼容检查，输出仍标记 `compat_only_not_rs_script` 与 `old_probe_only`。
- `--legacy-all-smoke`：不作为当前主线验收；其中仍含旧 SV/E5S 地图联动假设，后续需单独降级或拆除。

## 2026-05-31 08:30 宝物/物品写入烟测

- --write 已扩展复制 Star.e5 与 Imsg.e5 到测试副本。
- 新增 ITEM_WRITE_SMOKE_OK：验证 Data.e5、Star.e5、Imsg.e5 中物品名称、价格和说明文本可写回、可备份、可复读。

## 2026-05-31 08:40 宝物解释列增强验证

- `宝物设定` 已新增 `大类`、`类型说明`、`价格显示`、`特效提示` 只读解释列。
- `dotnet build .\CCZModStudio.sln --no-restore -v:minimal /p:UseAppHost=false`：通过。
- `dotnet build .\CCZModStudio集成版\CCZModStudio\CCZModStudio.csproj --no-restore -v:minimal /p:UseAppHost=false`：通过。
- `--write`：通过，仍输出 `ITEM_WRITE_SMOKE_OK`。

## 2026-05-31 08:43 本地知识库制度化后复核

- 重新读取 `B形象指定器\形象指定器6.5\System.ini`：确认 `FileHead=D2800`、`RFileHead=E1000`、`CountSV=900`、`UserPath2=...\RS\`。
- 重新读取 `CczRSX 6.5\ConfigTable\HexTable.xml`：确认 `6.5-0-4 R形象` 为 `Ekd5.exe:0xE1000`，`6.5-0-5 S形象` 为 `Ekd5.exe:0xD2800`。
- 重新扫描 RS 目录：`*.eex=119`，其中 `R_*.eex=60`，`S_*.eex=59`。
- `dotnet build .\CCZModStudio.sln --no-restore -v:minimal /p:UseAppHost=false`：通过，0 警告，0 错误。
- `--rs-smoke`：通过，输出 `RS_SCENARIO_INDEX count=119 R=60 S=59 E5S=0` 与 `RS_SMOKE OK`。
- `--write`：通过，只写测试副本，输出 `RS_WRITE_SMOKE OK` 与 `ITEM_WRITE_SMOKE_OK`。

## 2026-05-31 08:55 宝物图标预览验证

- 新增 `ItemIconPreviewService`：优先尝试标准图标枚举；若 `Itemicon.dll` 无标准图标，则解析 PE 资源目录中的 `RT_BITMAP`。
- 已确认 `Itemicon.dll` 中 `RT_BITMAP ID=100-439`，共 340 个资源，按 16x16/32x32 成对出现，当前候选图标数为 170。
- `--rs-smoke` 新增 `ITEM_ICON_PREVIEW`，可导出短剑图标 PNG。
- 根工程构建：通过，0 警告，0 错误。
- 集成版工程构建：通过，0 警告，0 错误。
- `--rs-smoke`：通过，输出 `ITEM_ICON_PREVIEW item=短剑 icon=0 count=170`。
- `--write`：通过，仍只写测试副本，并保留 `ITEM_WRITE_SMOKE_OK`。

## 2026-05-31 12:30 当前主线写入烟测扩展

- `--rs-smoke` 新增 `ITEM_TYPE_CATALOG`：验证宝物类型目录关键修正。
- `--write` 新增 `ROLE_WRITE_SMOKE_OK`：验证人物基础字段和人物列传测试副本写回。
- `--write` 新增 `JOB_WRITE_SMOKE_OK`：验证详细兵种名称、说明、成长、穿透测试副本写回。
- `--write` 新增 `JOB_TERRAIN_WRITE_SMOKE_OK`：验证兵种系名称、地形发挥、移动消耗测试副本写回。
- `--write` 新增 `JOB_EFFECT_WRITE_SMOKE_OK`：验证兵种特效说明和分配测试副本写回。
- 2026-05-31 12:28 复跑：根工程 build、集成版 build、`--rs-smoke`、`--write` 均通过。



## 2026-05-31 12:35 地形编辑写入烟测

- `--write` 新增 `HEXZMAP_WRITE_SMOKE_OK`：验证 `Hexzmap.e5` 在测试副本中可按地图分辨率/48 读取变量尺寸地形块、修改两个地形格、自动备份、生成结构化报告并复读。



## 2026-05-31 地图/地形变量尺寸验证

- 手工尺寸校验确认：地图格数由 `Map\Mxxx` 图片分辨率除以 48 得出，`Hexzmap.e5` 地形数据长度为 `gridWidth * gridHeight`。
- 已验证样例：
  - `M000 image=960x960 grid=20x20 cells=400 expectedSegment=402 hexSegment=402 dataOffset=0x3E6`
  - `M003 image=1344x960 grid=28x20 cells=560 expectedSegment=562 hexSegment=562 dataOffset=0x89C`
  - `M004 image=960x960 grid=20x20 cells=400 expectedSegment=402 hexSegment=402 dataOffset=0xACE`
  - `M018 image=1536x1536 grid=32x32 cells=1024 expectedSegment=1026 hexSegment=1026 dataOffset=0x2A8A`
  - `M056 image=1776x1440 grid=37x30 cells=1110 expectedSegment=1112 hexSegment=1112 dataOffset=0x9E5A`
- `Hexzmap.e5` 目录解释已更新：目录表从文件绝对偏移 `0x110` 开始，每项 12 字节，大端字段为 `segmentLength`、长度副本、`fileOffset`；地形数据从 `fileOffset + 2` 开始。
- 根工程构建：`dotnet build .\CCZModStudio.sln --no-restore -v:minimal -m:1 /p:UseAppHost=false` 通过，0 警告，0 错误。
- 集成版工程构建：`dotnet build .\CCZModStudio集成版\CCZModStudio.sln --no-restore -v:minimal -m:1 /p:UseAppHost=false` 通过，0 警告，0 错误。
- 旧 `--legacy-all-smoke` 仍含 `SV004.E5S` 历史联动断言，不能作为本次地图/地形变量尺寸改动的失败标准。



## 2026-05-31 12:40 战场制作文案收敛验证

- `rg "SV结构|SV 关卡|未知 SV|SV 命令|SV/Map|地图/SV|EEX/E5/SV|跳到SV"` 检查主工程和集成版核心文件：无残留。
- 根工程构建、集成版构建、`--rs-smoke`、`--write` 均通过。

## 2026-05-31 12:45 剧本制作读写边界

- 核心剧本制作页 保存选中文本 已确认直接写入 RS\\*.eex 当前项目文件，并执行备份和复读校验。
- --write 继续以 RS_WRITE_TEXT_OK 验证测试副本 R/S eex 文本短写回闭环。


## 2026-05-31 12:40 地图底图替换烟测

- --write 新增 MAP_IMAGE_WRITE_SMOKE_OK：验证 Map\\M*.jpg 在测试副本中可整文件替换、自动备份、生成结构化报告，并用 SHA256 与图片尺寸复读。


## 2026-05-31 12:45 战场制作文本写入烟测

- --write 新增 BATTLEFIELD_TEXT_WRITE_SMOKE_OK：验证 BattlefieldEditorService.SaveTitleAndConditions 在测试副本中可写 R/S eex 标题/胜败条件文本、自动备份、生成结构化报告并按原偏移复读。


## 2026-05-31 12:55 兵种矩阵写入烟测

- --write 新增 JOB_MATRIX_WRITE_SMOKE_OK：验证 6.5-3-3 兵种相克 与 6.5-3-4 兵种属性 在测试副本中可写回、自动备份并复读。


## 2026-06-06 旧版树中文显示烟测

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- SmokeTests：`dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 新增 `LegacyScenarioCommandDisplayFormatter` 样例断言，覆盖 `0x05`、`0x25`、`0x36` 的旧版中文摘要显示；通过。
- `--rs-smoke` 通过，确认 R/S 读取、旧版完整树读取、战场候选、资源预览等主线未回归。

## 2026-06-06 旧版右键菜单迁移验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- SmokeTests：`dotnet build 工具整合包\CCZModStudio.SmokeTests\CCZModStudio.SmokeTests.csproj` 通过，0 警告，0 错误。
- `--legacy-script-edit-smoke` 通过：测试副本读取 `R_00.eex`，新增普通命令、修改参数、删除命令并三次完整保存/复读/备份/报告均成功。
- `--legacy-mfc-dialog-smoke` 通过：旧式 Dialog 目录、资源解析、模板和中文摘要样例仍正常。
- `--rs-smoke` 通过：R/S 索引、旧版完整树读取、战场候选、资源预览等主线未回归。
- UI 行为需人工点验：剧本制作页旧版树右键菜单应显示 `IDR_POPUP_EDIT` 同序菜单；`添加/添加子事件/粘贴` 在当前指令前插入；`批量修改` 保持禁用。

## 2026-06-06 战场 S 剧本树中文映射修正验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--rs-smoke` 通过，确认战场旧版候选、地图联动、资源预览主线未回归。
- `--legacy-mfc-dialog-smoke` 通过，确认旧版 Dialog 资源与中文摘要 formatter 未回归。
- 备注：首次并行执行构建和 smoke 时命中 MSBuild `obj\Debug\net8.0-windows\CCZModStudio.dll` 写锁；改为顺序执行后全部通过。

## 2026-06-06 剧本制作页右侧布局验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- 静态检查：剧本制作页不再创建右侧 `remarkEditPanel`，不再挂载 `BuildScriptEditorPage.ParameterText`；右侧只保留 `BuildScriptEditorPage.ImageParameter` split。
- UI 需人工点验：右侧下半部分应完全由参数 Dialog 占据；图片预览和参数区之间的分隔条可上下拖动。

## 2026-06-06 剧本制作页右侧标题收敛验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 通过，确认旧式 Dialog 资源和控件生成未回归。
- 静态检查：`CreateTitledPanel("图片预览"...`、`MakeHeader("参数")`、`imagePreviewPanel`、`_scriptImagePreviewInfoBox` 的右侧挂载点均无残留。

## 2026-06-06 旧式 Dialog 中文控件裁剪修正验证

- 主工程：`dotnet build 工具整合包\CCZModStudio\CCZModStudio.csproj` 通过，0 警告，0 错误。
- `--legacy-mfc-dialog-smoke` 通过，确认旧式 Dialog 资源解析、初始化/提交逻辑和中文摘要样例未回归。
- UI 修正点：`LegacyMfcDialogHostControl` 在初始化后实测中文标签和下拉项宽度，扩宽同一行控件并顺移后续控件；必要时通过宿主横向滚动兜底。
- UI 需人工点验：变量测试、变量运算等包含“变量类型 / 数值 / 操作 / 关系”的旧式 Dialog 不应再截断中文标签或下拉框当前项。
