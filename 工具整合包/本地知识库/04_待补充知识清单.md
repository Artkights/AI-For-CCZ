# 04 待补充知识清单

以下知识当前项目需要，但储备不足。后续用户可根据本清单提供对应工具、说明、样例文件或截图。

## A. R/S 剧本与 eex

- `.eex` 文件头、目录表、压缩/加密/校验规则。
- R 剧本与 S 剧本在文件命名、目录、用途上的区别。
- `a新剧本编辑器v0.23` 打开 eex 的流程、命令字典和写回规则。
- eex 重封包后游戏是否要求索引/长度/校验同步更新。

## B. E5S 存档信息

- `.E5S` 的真实用途和字段结构。
- E5S 是否只作为存档数量/配置/关卡进度信息，是否需要在本工具中保留单独“存档信息”模块。
- 现有项目中 `SV` 目录是否仍有读取价值，若有，应命名为“存档信息/索引”，不能叫“剧本”。

## C. 人物 R/S 形象

- B形象指定器的操作说明、界面截图或源码。
- `FileHead` / `RFileHead` 的精确定义：文件偏移、RVA、还是工具内部地址。
- 每个人物 R/S 指定项的字段宽度、排列顺序、编号基准。
- RS 目录 eex 资源与 R/S 编号的对应方式。

## D. 地图/战场/地形联动

- 战场剧本如何引用地图编号和地形块。
- `.eex` 剧本中的出场人物、坐标、胜败条件、对白等命令结构。
- 地形编辑器对 `Hexzmap.e5` 的写回规则和地图块数量/排序。

## E. 封包工具

- 解包/封包命令、参数和输出结构。
- 替换 eex 条目后是否需要更新总包或索引。
- 对 6.5 未加密版是否存在版本差异。

## 2026-05-31 08:05 新增待核实项

1. `a新剧本编辑器v0.23` 中 R/S eex 的真实命令边界、参数长度、文本引用方式。
2. R/S eex 内部是否允许安全扩容、重封包，或只能做原偏移短写回与整文件替换。
3. 旧 `SVxxx.E5S -> Map\Mxxx.jpg` 联动是否仍有实际制作价值；若仅用于旧探针，应从核心 UI 移出。

## 2026-05-31 08:30 宝物/物品待补充

- 物品 类型 字段完整枚举。
- 图标 编号到图像资源的定位方式：已初步确认 `Itemicon.dll` 内 `RT_BITMAP ID=100-439`，当前按 `100 + 图标字段 * 2 + 1` 预览 32x32 候选图；仍需旧工具或实机截图最终校验。
- 宝物图鉴 字段含义和游戏显示规则。
- 装备特效号与效果值参数的逐项解释，优先查询 B形象指定器 和 CczRSX 6.5。

## 2026-05-31 12:30 角色/兵种待补充

- 角色设定：Army 字段、暴击台词/撤退台词在创作页中的传统编辑习惯仍需旧工具或实机确认。
- 兵种设定：兵种相克矩阵、兵种属性矩阵已进入创作页；人物专属/套装专属表仍需转成创作者友好 UI。
- 兵种特效：6.5-7 兵种特效 名称区 Bytes=0，真实结构尚未拆清，暂不开放改名。


## 2026-05-31 12:35 地形编辑待补充

- 对照 地形编辑器 确认 Hexzmap.e5 块顺序、地图编号、尾部字节含义。
- 补齐地形 ID 到中文地形名称的权威映射。
- 确认 Map\\Mxxx.jpg、R/S eex 战场脚本和 Hexzmap 地形块之间的引用链。


## 2026-05-31 12:40 地图制作待补充

- 确认 Map\\Mxxx.jpg 与 R/S eex 战场脚本、Hexzmap.e5 块编号之间的权威引用链。
- 批量底图替换、尺寸一致性检查和地图/地形成套导出格式仍需补齐。


## 2026-05-31 12:55 兵种矩阵待补充

- 确认 6.5-3-3 兵种相克 数值的真实倍率/公式。
- 确认 6.5-3-4 兵种属性 8 行的真实语义。
## 2026-05-31 新增最高优先级待补充

1. `Hexzmap.e5` 变量尺寸结构：地图图片 48px/格已确认，但 Hexzmap 内块顺序、缺失地图编号、尾部字节/尺寸表仍需对照 `地形编辑器` 完整确认。
2. 人物 R/S 形象真实资源容器：`B形象指定器` 能确认 Ekd5.exe 指定表地址，`Face.e5 / Pmapobj.e5 / Unit_*.e5` 的 E5 `0x110` 图片索引表已可读取、预览并单条替换；原始帧条调色板已接入普罗工具 `tsb`，仍缺完整动作帧序列、朝向选择、批量动作替换和通用重封包回写格式。
3. R 插图/`Tr.e5`：已建立 `21_Tr_e5_R插图.md`，确认旧剧本编辑器 `信息传送28` 参数说明和一个本地 `Tr.e5` 样本的 `0x110` E5 索引结构；仍需实机验证图号映射、尺寸上限、透明规则、坐标越界和 6.5 基底无 `Tr.e5` 时的新增文件规则。
4. R/S eex 剧本/战场命令：对照 `a新剧本编辑器v0.23` 与 `全剧本指令详解（2023）` 确认命令 ID、参数长度、跳转、出场、坐标、AI、阵营后再扩展写回。
5. 辅助装备实际效果解释：对照 `B形象指定器` 与 `CczRSX 6.5`，确认辅助装备 `类型` 字段和类别标记 `2/3` 的完整语义。

## 2026-06-04 MCP 知识检索后仍待补充

- `CczString.ini` 命令字典和内置 R/S eex 参数槽模板已可通过 MCP 的 `list_scenario_command_templates` / `read_scenario_command_template` 查询；这些模板只用于解释、备注、核对和旧工具对照，不代表完整命令长度已确认。
- 本地知识库 Markdown 已可通过 `search_knowledge_entries` 检索；仍需继续把 `全剧本指令详解（2023）.docx` 转成结构化 Markdown，并逐条标注 6.5 适用性、参数长度、实机验证状态和与现有模板的差异。

## 2026-06-04 Hexzmap MCP 读取补齐后仍待补充

- MCP 已可用 `list_hexzmap_blocks` / `read_hexzmap_block` 读取地形块列表、单块地形格矩阵和高频地形统计；后续缺口不再是“无头不可读”，而是“地图块编号、段头 2 字节、缺失地图编号和 R/S eex 战场引用链仍未完全权威确认”。
- 当前 `write_hexzmap_block` 仍受版本护栏约束；`Hexzmap.e5` 样本大小差异未确认前，不应把 MCP 只读结果当作放开直写当前基底的理由。

## 2026-06-04 资源索引/诊断 MCP 补齐后仍待补充

- MCP 已可用 `list_project_resources` / `run_resource_diagnostics` 做无头资源索引和基础风险检查；后续缺口转为“资源诊断与人物 R/S 指定、关卡地图联动、发布证据和创作者备注的更完整联动”，而不是“无法列资源”。
- 资源诊断当前以文件分类、编号、命名、魔数和地图尺寸为主；E5 内部条目、Ls 通用重封包、音频引用表、R/S 剧本实际资源引用仍需继续结合旧工具和实机验证。
